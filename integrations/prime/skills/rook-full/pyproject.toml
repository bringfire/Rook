[project]
name = "prime-agent-skill-rook-full"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["mcp==1.28.1", "prime-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rook_full"]
