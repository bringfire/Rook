# Extracted SDK docs (local reference only)
docs/gh_sdk_extracted/
docs/gh_tests/
docs/brainstorm/
docs/plans/
docs/superpowers/
docs/roadmaps/
docs/engram-reference/
docs/DisCoPaper_DMSB_rg.pdf
docs/INTERACTION_MODEL.md
docs/KNOWLEDGE_SYSTEMS_EVOLUTION.md
docs/KNOWLEDGE_SYSTEMS_ARCHITECTURE.md
docs/FEATURE_ROADMAP.md
docs/knowledge/

# rook_docs/ imported 2026-04-26 (formerly a sibling repo). Track the
# design memos and our own promoted scripts, but exclude the external
# script-reference clones (~545 MB across 9 cloned repos) and the
# extracted-PDF artifact directory. The source PDF is tracked.
docs/rook_docs/script-reference/*/
!docs/rook_docs/script-reference/promoted/
docs/rook_docs/tencent_api_md/

# Build results
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
*.log

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.rsuser

# Visual Studio Code
.vscode/

# Rider
.idea/

# NuGet
*.nupkg
*.snupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# Environment files with secrets
.env
.env.local
.env.*.local
*.env
*.egg-info/
.eggs/
dist/
build/
*.egg

# uv
.uv/
uv.lock

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
*.swp
*.swo
*~

# Project specific
*.rhp.bak
.mcp.json
.codex/config.toml

# Runtime data (generated during operation)
knowledge/context_history.json
knowledge/mab_model.pkl
knowledge/scaler.pkl
knowledge/feature_flags.json
knowledge/metrics.json
knowledge/agent_tool_catalog.json

# Bootstrap local knowledge (unvalidated, session-specific)
knowledge/local/
!knowledge/local/.gitkeep
mcp_server/knowledge/local/

# Generated provider catalog caches (disposable; see Spec A)
knowledge/generated/

# Canonical knowledge is tracked (validated, production)
# knowledge/canonical/ is NOT ignored
# mcp_server/knowledge/canonical/ is also tracked

# Injection capture logs (testing/validation)
injection_capture.jsonl
*.capture.jsonl

# Images (only track README logo and installer icon)
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.ico
*.svg
*.webp
*.tiff
*.tif
*.psd
!docs/Images/Rook_02.png
!installer/rook-icon.ico
!src/Rook/UI/Vision/Resources/rook-logo.svg

# Temporary files
nul
.pytest_tmp/
.codex_tmp/
check_*.py

# Reference materials (large, not source)
references/
# But track skill reference docs
!skills/*/references/
!.claude/skills/*/references/
!.agents/skills/*/references/

# Claude local settings
.claude/settings.local.json

# Git worktrees
.worktrees/

# Archive (historical plans, conversations — not needed for public repo)
docs/archive/

# MAB model binaries (regenerable from observation data)
knowledge/commands/*.pkl
knowledge/commands/*.pkl.bak

# Session recordings (runtime artifacts, not reference data)
knowledge/gh/sessions/

# Installer output (distributed via GitHub Releases, not git)
installer/output/
installer/runtime/

# Release staging artifacts generated by scripts (repo-root only —
# the unanchored form also matched src/Rook/Artifacts source dirs).
/artifacts/

# Private drafts (announcements, marketing, internal notes)
drafts/

# Scratch / spike runtime trees (raw evidence stays out of the repo)
.scratch/

# Docs website now lives in the public rook-release repo, not here
/site/

# Runtime DSPy cache written by dev-mode MCP server (ROOK_DATA_DIR=knowledge/)
knowledge/dspy-cache/

# Subagent-driven-development scratch (briefs, reports, ledger, review diffs)
.superpowers/

# LM5K+ model probe evidence runs (raw evidence stays local; curated
# summaries are committed under docs/superpowers/probes/)
probe_runs/
