{
  "schema": "rook.lm9b_c.implementation_context:v2",
  "context_id": "lm9b-c-rhinocode-csharp-script-instance",
  "representation_profile": {
    "kind": "csharp_script_instance",
    "component_count": 1,
    "source_mode": "full_source",
    "canvas_rule": "no auxiliary canvas components",
    "execution_permitted": false,
    "pin_contract": {
      "input_and_output_names": "valid unique C# identifiers",
      "access_modes": ["item", "list", "tree"],
      "declared_types": "RhinoCode pin type names",
      "runtime_input_parameters": "object values requiring explicit casts",
      "runtime_output_parameters": "ref object values assigned by declared output name",
      "parameter_order": "all declared inputs followed by all declared outputs",
      "parameter_names": "exactly match declared pin names in declared order"
    },
    "mechanical_contract": {
      "class_declaration": "public class Script_Instance : GH_ScriptInstance",
      "runscript_count": 1,
      "runscript_declaration": "private void RunScript",
      "input_parameter_shape": "object <declared-input-name>",
      "output_parameter_shape": "ref object <declared-output-name>",
      "forbidden_output_modifier": "out",
      "forbidden_base_type": "GH_Component"
    },
    "source_contract": [
      "Submit a complete public Script_Instance class inheriting GH_ScriptInstance.",
      "Declare exactly one private void RunScript method whose object and ref object parameters exactly match the declared pins.",
      "Use RhinoCommon geometry types and ordinary finite C# control flow.",
      "Do not use out output parameters, define a GH_Component subclass, or define plugin registration methods.",
      "The source and pin declarations are inert probe output and are never executed."
    ]
  },
  "semantic_vocabularies": {
    "shape_codes": [
      "add_material_semantics",
      "author_semantic_contract",
      "construct_topology",
      "disable_required_verification",
      "expand_mutation_scope",
      "lower_verification",
      "select_representation"
    ],
    "materiality_codes": [
      "canonicalization",
      "capability_authority",
      "document_state",
      "execution_branching",
      "fingerprint",
      "geometry_state",
      "mutation_authority",
      "target_identity",
      "tool_arguments",
      "topology",
      "verifier_predicate",
      "verifier_threshold"
    ]
  },
  "capability_evidence": [
    {
      "capability_code": "construct_parametric_geometry",
      "availability": "available",
      "implementation_kind": "csharp_script_instance",
      "constraints": [
        "inert full-source output only",
        "one component",
        "no execution",
        "no external retrieval"
      ]
    }
  ],
  "limitations": [
    "This fixed profile tests lowering, not representation selection.",
    "Availability does not authorize mutation or execution.",
    "No worked source, topology, task values, or expected result is included."
  ]
}
