# GH Route Readiness Inventory

Date: 2026-05-14

Source spec: `docs/superpowers/specs/2026-05-14-gh-legacy-route-readiness-boundary-design.md`

Route source command:

```powershell
rg -n '"/gh/' src\RookNative\RookServer.cpp src\RookNative\Handlers\GrasshopperProxyHandler.cpp src\Rook\InternalBridge\NativeGhBridgeRegistrar.cs src\Rook\Handlers\GrasshopperHandler.cs
```

Registration verification command:

```powershell
rg -n '(ghGet|ghPost|cgpPost|navGet|navPost)\("/gh/' src\RookNative\RookServer.cpp
```

## Category Definitions

- Read-only status/library: may run without `ready_for_edit` if it has no document/canvas side effects.
- Read-only canvas inspection: requires `ready_for_edit`, fails closed.
- Mutation: requires `ready_for_edit`, fails closed.
- Imperative lifecycle: may change readiness only when explicitly reviewed and documented.

## Inventory

Note: `Expected not-ready response` entries describe the route contract. The `Implementation / test status` column distinguishes implemented guard/no-side-effect status from direct representative test coverage.

| HTTP method + path | Native handler | Managed callback / handler | Category | Requires ready_for_edit | Side effects allowed | Python tool exposure | Expected not-ready response | Implementation / test status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /gh/status` | `HandleGrasshopperStatus` -> `registration.gh_status` | `NativeGhBridgeRegistrar.HandleStatus` -> `Core.GetStatus` | Read-only status/library | No | Status query only; no document/canvas creation | `gh_status` via MCP and `BRIDGE_ROUTES`; in `gh_exploration` group | Endpoint may return `success: true`; `data.ready_for_edit: false` allowed | Read-only no-ready-gate; no canvas side effects |
| `GET /gh/document` | `HandleGrasshopperDocument` -> `registration.gh_document` | `NativeGhBridgeRegistrar.HandleDocument` -> `Handler.GetDocumentInfo` | Read-only document metadata | No, but must not create document/canvas | Document metadata only from existing active document/status; no document/canvas creation | Internal/session callers; no agent `BRIDGE_ROUTES` entry | Structured `success: false`, `verified: false`, `data.error: grasshopper_not_ready` if no active document/canvas | Guarded; representative managed test |
| `GET /gh/query` | `HandleGrasshopperQuery` -> `registration.gh_query` | `NativeGhBridgeRegistrar.HandleQuery` -> `Handler.QueryDocument` | Read-only canvas inspection | Yes | None | Internal legacy callers only; no Python agent tool | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `GET /gh/selection` | `HandleGrasshopperSelection` -> `registration.gh_selection` | `NativeGhBridgeRegistrar.HandleSelection` -> `Handler.GetSelection` | Read-only canvas inspection | Yes | None | `gh_selection` via MCP and `BRIDGE_ROUTES`; in `gh_canvas`, `gh_canvas_readonly` groups | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/categories` | `HandleGrasshopperCategories` -> `registration.gh_categories` | `NativeGhBridgeRegistrar.HandleCategories` -> `Handler.GetCategories` | Read-only status/library | No | Library/category discovery only; no document/canvas creation | `gh_categories` via MCP and `BRIDGE_ROUTES`; in `gh_knowledge` group | Endpoint may return `success: true`; `data.ready_for_edit: false` allowed if included | Read-only no-ready-gate; no canvas side effects |
| `GET /gh/library` | `HandleGrasshopperLibrary` -> `registration.gh_library` | `NativeGhBridgeRegistrar.HandleLibrary` -> `Handler.SearchLibrary` | Read-only status/library | No | Library search only; no document/canvas creation | `gh_library` via MCP and transform; in `gh_knowledge` group | Endpoint may return `success: true`; `data.ready_for_edit: false` allowed if included | Read-only no-ready-gate; no canvas side effects |
| `GET /gh/value` | `HandleGrasshopperGetValue` -> `registration.gh_get_value` | `NativeGhBridgeRegistrar.HandleGetValue` -> `Handler.GetValue` | Read-only canvas inspection | Yes | None | `gh_get_value` via MCP; no agent `BRIDGE_ROUTES` entry | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/value` | `HandleGrasshopperSetValue` -> `registration.gh_set_value` | `NativeGhBridgeRegistrar.HandleSetValue` -> `Handler.SetValue` | Mutation | Yes | Set slider/panel/toggle value only after readiness | `gh_set_value` via MCP/internal helper; no agent `BRIDGE_ROUTES` entry | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `POST /gh/script` | `HandleGrasshopperSetScript` -> `registration.gh_set_script` | `NativeGhBridgeRegistrar.HandleSetScript` -> `Handler.SetScript` | Mutation | Yes | Set script component code only after readiness | `gh_set_script` via MCP and transform; in `gh_canvas` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/script-params` | `HandleGrasshopperScriptParams` -> `registration.gh_script_params` | `NativeGhBridgeRegistrar.HandleScriptParams` -> `Handler.ScriptParams` | Mutation | Yes | Set script component parameters only after readiness | `gh_set_script_pins` local MCP helper | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/connections` | `HandleGrasshopperConnections` -> `registration.gh_connections` | `NativeGhBridgeRegistrar.HandleConnections` -> `Handler.GetConnections` | Read-only canvas inspection | Yes | None | Internal layout/exploration callers; no agent `BRIDGE_ROUTES` entry | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/delete` | `HandleGrasshopperDelete` -> `registration.gh_delete` | `NativeGhBridgeRegistrar.HandleDelete` -> `Handler.DeleteObjects` | Mutation | Yes | Delete canvas objects only after readiness | `gh_delete` via MCP; internal workflows | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/preview` | `HandleGrasshopperPreview` -> `registration.gh_preview` | `NativeGhBridgeRegistrar.HandlePreview` -> `Handler.SetPreview` | Mutation | Yes | Change preview visibility only after readiness | `gh_preview` via MCP and `BRIDGE_ROUTES`; in `gh_canvas`, `gh_exploration` groups | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/clear` | `HandleGrasshopperClear` -> `registration.gh_clear` | `NativeGhBridgeRegistrar.HandleClear` -> `Handler.ClearCanvas` | Imperative lifecycle | Requires lifecycle review | Clear/reset active document only after explicit classification | `gh_clear` via MCP and `BRIDGE_ROUTES`; in `gh_canvas` group | Classification-specific response; must be explicit | Lifecycle review pending |
| `POST /gh/document/open` | `HandleGrasshopperOpenDocument` -> `registration.gh_open_document` | `NativeGhBridgeRegistrar.HandleOpenDocument` -> `Handler.OpenDocument` | Imperative lifecycle | Requires lifecycle review | May open active document if retained as lifecycle | `gh_document_open` via MCP and transform; in `gh_document` group | Lifecycle-specific response with postcondition status | Lifecycle review pending |
| `POST /gh/document/new` | `HandleGrasshopperNewDocument` -> `registration.gh_new_document` | `NativeGhBridgeRegistrar.HandleNewDocument` -> `Handler.NewDocument` | Imperative lifecycle | Requires lifecycle review | May create active document if retained as lifecycle | `gh_document_new` via MCP and `BRIDGE_ROUTES`; in `gh_document` group | Lifecycle-specific response with postcondition status | Lifecycle review pending |
| `POST /gh/move` | `HandleGrasshopperMove` -> `registration.gh_move` | `NativeGhBridgeRegistrar.HandleMove` -> `Handler.MoveObjects` | Mutation | Yes | Move canvas objects only after readiness | `gh_move` via MCP and `BRIDGE_ROUTES`; in `gh_canvas` group; internal layout tools | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/group` | `HandleGrasshopperGroup` -> `registration.gh_group` | `NativeGhBridgeRegistrar.HandleGroup` -> `Handler.CreateGroup` | Mutation | Yes | Create group only after readiness | Legacy direct/internal callers if present | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/groups` | `HandleGrasshopperGroups` -> `registration.gh_groups` | `NativeGhBridgeRegistrar.HandleGroups` -> `Handler.GetGroups` | Read-only canvas inspection | Yes | None | `gh_groups` via `BRIDGE_ROUTES`; internal layout callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/group-resize` | `HandleGrasshopperGroupResize` -> `registration.gh_group_resize` | `NativeGhBridgeRegistrar.HandleGroupResize` -> `Handler.ResizeGroupToFit` | Mutation | Yes | Resize groups only after readiness | `gh_group_resize` via `BRIDGE_ROUTES`; internal layout callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/cluster` | `HandleGrasshopperCluster` -> `registration.gh_cluster` | `NativeGhBridgeRegistrar.HandleCluster` -> `Handler.CreateCluster` | Mutation | Yes | Create cluster only after readiness | `gh_cluster` via MCP and `BRIDGE_ROUTES`; in `gh_patterns` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/explore-selection` | `HandleGrasshopperExploreSelection` -> `registration.gh_explore_selection` | `NativeGhBridgeRegistrar.HandleExploreSelection` -> `Handler.ExploreSelection` | Mutation | Yes | Temporarily mutates canvas selection state by deselecting/selecting objects; only after readiness | Internal/legacy exploration callers if present | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/explore-cluster` | `HandleGrasshopperExploreCluster` -> `registration.gh_explore_cluster` | `NativeGhBridgeRegistrar.HandleExploreCluster` -> `Handler.ExploreCluster` | Read-only canvas inspection | Yes | None; exploration/knowledge read only | Internal/legacy exploration callers if present | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/batch-component-info` | `HandleGrasshopperBatchComponentInfo` -> `registration.gh_batch_component_info` | `NativeGhBridgeRegistrar.HandleBatchComponentInfo` -> `Handler.HandleBatchComponentInfo` | Read-only component metadata/catalog lookup | No, but must not create document/canvas | Component type metadata lookup only; no document/canvas creation | `gh_batch_component_info` via MCP; internal exploration/knowledge callers | No readiness failure required unless Grasshopper/component server is unavailable; must not create document/canvas | Read-only no-ready-gate; no canvas side effects |
| `POST /gh/create-component` | `HandleGrasshopperCreateComponent` -> `registration.gh_create_component` | `NativeGhBridgeRegistrar.HandleCreateComponent` -> `Handler.CreateComponent` | Mutation | Yes | Create component only after readiness | `gh_create_component` via MCP; internal exploration/fallback callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/create-slider` | `HandleGrasshopperCreateSlider` -> `registration.gh_create_slider` | `NativeGhBridgeRegistrar.HandleCreateSlider` -> `Handler.CreateSlider` | Mutation | Yes | Create slider only after readiness | `gh_create_slider` via MCP; internal exploration/fallback callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `POST /gh/create-panel` | `HandleGrasshopperCreatePanel` -> `registration.gh_create_panel` | `NativeGhBridgeRegistrar.HandleCreatePanel` -> `Handler.CreatePanel` | Mutation | Yes | Create panel only after readiness | `gh_create_panel` via MCP; internal exploration/fallback callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `GET /gh/component` | `HandleGrasshopperComponent` -> `registration.gh_component` | `NativeGhBridgeRegistrar.HandleComponent` -> `Handler.GetComponentInfo` | Read-only canvas inspection | Yes | None | Internal exploration callers; no agent `BRIDGE_ROUTES` entry | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/inspect-output` | `HandleGrasshopperInspectOutput` -> `registration.gh_inspect_output` | `NativeGhBridgeRegistrar.HandleInspectOutput` -> `Handler.InspectOutput` | Read-only canvas inspection | Yes | None | `gh_inspect_output` via MCP and transform; in `gh_canvas`, `gh_canvas_readonly` groups | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/errors` | `HandleGrasshopperErrors` -> `registration.gh_errors` | `NativeGhBridgeRegistrar.HandleErrors` -> `Handler.GetCanvasErrors` | Read-only canvas inspection | Yes | None | `gh_errors` via MCP and `BRIDGE_ROUTES`; in `gh_canvas`, `gh_canvas_readonly` groups | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `POST /gh/connect` | `HandleGrasshopperConnect` -> `registration.gh_connect` | `NativeGhBridgeRegistrar.HandleConnect` -> `Handler.ConnectComponents` | Mutation | Yes | Add wire only after readiness | `gh_connect` via MCP/internal helper; internal exploration/fallback callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; representative managed test |
| `POST /gh/disconnect` | `HandleGrasshopperDisconnect` -> `registration.gh_disconnect` | `NativeGhBridgeRegistrar.HandleDisconnect` -> `Handler.DisconnectComponents` | Mutation | Yes | Remove wire only after readiness | `gh_disconnect` via MCP/internal helper; internal exploration/fallback callers | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/set-reference` | `HandleGrasshopperSetReference` -> `registration.gh_set_reference` | `NativeGhBridgeRegistrar.HandleSetReference` -> `Handler.SetReference` | Mutation | Yes | Set persistent Rhino reference only after readiness | `gh_set_reference` via MCP and transform; in `gh_references` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `GET /gh/get-reference` | `HandleGrasshopperGetReference` -> `registration.gh_get_reference` | `NativeGhBridgeRegistrar.HandleGetReference` -> `Handler.GetReference` | Read-only canvas inspection | Yes | None | `gh_get_reference` via MCP and transform; in `gh_references` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/clear-reference` | `HandleGrasshopperClearReference` -> `registration.gh_clear_reference` | `NativeGhBridgeRegistrar.HandleClearReference` -> `Handler.ClearReference` | Mutation | Yes | Clear persistent Rhino references only after readiness | `gh_clear_reference` via MCP and transform; in `gh_references` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/solve` | `HandleGrasshopperSolve` -> `registration.gh_solve` | `NativeGhBridgeRegistrar.HandleSolve` -> `Handler.TriggerSolve` | Mutation | Yes | Trigger solution/recompute only after readiness | `gh_solve` via MCP/internal workflows; no agent `BRIDGE_ROUTES` entry | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/bake` | `HandleGrasshopperBakeOutput` -> `registration.gh_bake_output` | `NativeGhBridgeRegistrar.HandleBakeOutput` -> `Handler.BakeOutput` | Mutation | Yes | Bake GH output into Rhino only after readiness | `gh_bake_output` via MCP and `BRIDGE_ROUTES`; in `gh_canvas` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/snapshot` | `HandleGrasshopperSnapshot` -> `registration.gh_snapshot` | `NativeGhBridgeRegistrar.HandleSnapshot` -> `Handler.TakeSnapshot` | Read-only canvas inspection | Yes | None | `gh_snapshot` via MCP and `BRIDGE_ROUTES`; in `gh_canvas`, `gh_canvas_readonly` groups | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/edit` | `HandleGrasshopperEdit` -> `registration.gh_edit` | `NativeGhBridgeRegistrar.HandleEdit` -> `Handler.ApplyEdit` | Mutation | Yes | Batch canvas mutation only after readiness | `gh_edit` via MCP and `BRIDGE_ROUTES`; in `gh_canvas` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/undo` | `HandleGrasshopperUndo` -> `registration.gh_undo` | `NativeGhBridgeRegistrar.HandleUndo` -> `Handler.UndoCanvas` | Mutation | Yes | Undo canvas edit only after readiness | `gh_undo` via MCP and `BRIDGE_ROUTES`; in `gh_canvas` group | `success: false`, `verified: false`, `data.error: grasshopper_not_ready` | Guarded; covered by route category implementation |
| `POST /gh/canvas/focus` | `HandleGrasshopperCanvasFocus` -> `registration.gh_canvas_focus` | `NativeGhBridgeRegistrar.HandleCanvasFocus` -> `Handler.FocusCanvas` | Imperative lifecycle | Requires lifecycle review | UI navigation only if canvas exists; no document creation unless explicitly approved | `gh_canvas_focus` via MCP; in `gh_canvas` group | Classification-specific response; must be explicit | Lifecycle review pending |
| `POST /gh/canvas/zoom` | `HandleGrasshopperCanvasZoom` -> `registration.gh_canvas_zoom` | `NativeGhBridgeRegistrar.HandleCanvasZoom` -> `Handler.ZoomCanvas` | Imperative lifecycle | Requires lifecycle review | UI navigation only if canvas exists; no document creation unless explicitly approved | `gh_canvas_zoom` via MCP; in `gh_canvas` group | Classification-specific response; must be explicit | Lifecycle review pending |
| `GET /gh/canvas/image` | `HandleGrasshopperCanvasImage` -> `registration.gh_canvas_image` | `NativeGhBridgeRegistrar.HandleCanvasImage` -> `Handler.CaptureCanvasImage` | Imperative lifecycle | Requires lifecycle review | Capture only; no document creation unless explicitly approved | `gh_canvas_image` via MCP; in `gh_canvas`, `gh_canvas_readonly` groups | Classification-specific response; must be explicit | Lifecycle review pending |

## Verification Notes

- The inventory starts from `src/RookNative/RookServer.cpp` route registration, not Python tool metadata.
- `GET /gh/value` and `POST /gh/value` are listed as separate rows because they have different handlers and readiness categories.
- `/gh/query` remains a guarded compatibility route in this inventory and is not listed as a Python agent-facing tool.
