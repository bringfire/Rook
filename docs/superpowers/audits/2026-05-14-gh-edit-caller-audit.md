# GH Edit Caller Audit

Date: 2026-05-14

Spec: `docs/superpowers/specs/2026-05-14-gh-edit-partial-failure-contract-design.md`

## Contract States

| State | Semantics |
| --- | --- |
| Full success | success true, no partial_success, no top-level errors; safe to build on. |
| No-mutation failure | success false, verified false, top-level errors, no partial_success; safe to retry only after fixing input. |
| Partial mutation failure | Phase 1 success true + partial_success true + verified false + top-level errors; Phase 3 success false with same partial metadata; not automatically retryable. Inspect snapshot/edit summary, then remediate, undo, clean up, or deliberately retry with duplicate prevention. |

## Call Sites

| Area | File / Function | Current Behavior | Required Phase 1 Behavior | Required Phase 3 Behavior | Partial Mutation Happened? | Retry Allowed? | Tests / Migration Work |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Chat dispatcher/tool card | `mcp_server/src/rook/agent/tool_dispatcher.py::ToolDispatcher._dispatch_inner` lines 1547-1555 | Bridge routes call Rhino directly. For `gh_edit`, the result is passed through `apply_gh_edit_contract(result, strict_partial_success=False)`, so no-mutation edit errors become failure and mutation-evidence edit errors keep transport success with top-level partial metadata. | Keep applying the wrapper before returning to chat. Tool cards must render `partial_success`, `verified: false`, `errors`, and `verification_note` as an unverified partial edit even while top-level success remains true. | Flip the helper default or this caller flag so mutation-evidence edit errors return `success: false` while retaining the same top-level and `data` partial fields. | Yes when `edit_summary` includes mutation counts, `temp_id_map`, `instance_guids`, or `mutation_applied`. | No automatic retry. Agent must inspect snapshot and edit summary first. | `mcp_server/tests/test_dispatcher_safety.py::TestDispatcherVerification` currently covers no-mutation failure and Phase 1 partial promotion. Phase 3 must change the partial test to expect strict failure with retained `partial_success`. |
| Session history recording | `mcp_server/src/rook/server.py::_record_gh_to_session` lines 346-430 and `mcp_server/src/rook/server.py::call_tool` case `"gh_edit"` lines 12654-12674 | `_record_gh_to_session` merges top-level errors, data errors, and nested `data.edit_summary.errors`, then records `outcome` as `partial` when `success` is true with errors. The `gh_edit` call_tool branch records only inside `if result.get("success")`, so Phase 3 strict partial failures would not be preserved unless that branch changes. | Current Phase 1 partial mutations are recorded because success stays true. Keep nested edit errors promoted into session history and preserve warnings. | Record strict partial failures explicitly: `success: false` plus `partial_success: true` must still call `_record_gh_to_session`, and history should keep errors, warnings, verified=false, and partial outcome metadata. | Yes when the contracted result contains `partial_success: true`. | No. History is evidence for remediation, not a retry driver. | `mcp_server/tests/test_gh_edit_postmortem.py::test_record_gh_to_session_marks_edit_summary_errors_as_partial` covers partial classification. Add a Phase 3 branch test proving `call_tool("gh_edit")` records when `success` is false and `partial_success` is true. |
| Batched GH intent fallback / batch `gh_edit` path | `mcp_server/src/rook/server.py` lines 16022-16072 | Batch intent execution posts `edit_doc` with `edit_result = await call_rhino("/gh/edit", "POST", edit_doc, port=port)`. It branches on `edit_result.get("success")`; failure sets `batch_ok = False` and logs that it is falling back to sequential. Nested `edit_summary.errors` are appended only after a successful batch branch. | Apply the contract before branching or explicitly inspect `edit_summary.errors` and mutation evidence. If `partial_success` is true, skip blind sequential fallback and surface the partial result for inspection. | Treat `success: false` plus `partial_success: true` as non-retryable without inspection. Sequential fallback is valid only for no-mutation failures such as epoch mismatch or validation failure. | Yes if the batch created, connected, set values, or returned temp or instance GUID maps before an edit error. | No for partial mutation. Retrying the same sequential work can duplicate created components. | Add audit-driven tests for batch fallback: no-mutation failure can use sequential fallback; partial mutation must not. |
| Recipe replay path | `mcp_server/src/rook/server.py::case "gh_replay_recipe"` lines 14115-14145 and `mcp_server/src/rook/learning/recipe_extraction.py::recipe_to_edit` lines 866-881 | `recipe_to_edit` converts a recipe graph into a `/gh/edit` document. Replay posts that document directly to `/gh/edit` and records learning/history only when raw `result.get("success")` is true. It does not apply `apply_gh_edit_contract` to the replay result. | Apply the contract to the replay edit result before deciding whether to record the session or pattern application. Phase 1 partials should be recorded as partial, not learned as clean success. | Strict partial failures should return failure with retained partial metadata and should not apply learning based only on raw success. If replay remains separate, document the separate contract and test it. | Yes when the replayed edit creates or wires some recipe components before later edit errors. | No automatic replay. Inspect created components and wiring before undo, cleanup, or a deliberate duplicate-preventing retry. | Add tests for replay result contraction and recording behavior. |
| GH session learning/history | `mcp_server/src/rook/learning/gh_session_history.py::GHToolResult` lines 43-116 and `mcp_server/src/rook/learning/knowledge_injector.py` lines 78-81, 568-627 | Session entries store `success`, `outcome`, `errors`, and `warnings`, but not `verified`, `partial_success`, or `verification_note`. `knowledge_injector` maps `gh_edit` to operation `"edit"` and notes that `gh_edit` handles its own knowledge/session recording. | Store promoted errors and warnings for partials, and avoid classifying partial edit batches as clean success patterns. | Extend recorded data or entry metadata to preserve `partial_success`, `verified: false`, `verification_note`, and strict failure metadata so downstream learning can distinguish failure after mutation from full success. | Yes when session recorder receives contracted partial metadata. | No. Learning/history should preserve trace data for correction. | Add tests asserting recorded session data includes strict partial fields once Phase 3 changes the contract. |
| Direct MCP tool response | `mcp_server/src/rook/server.py::call_tool` lines 12654-12674 and response formatting lines 17721-17734 | The `gh_edit` branch applies `apply_gh_edit_contract(..., strict_partial_success=False)`. Response formatting emits only `json.dumps(result.get("data"))` for successful results. Current tests rely on the helper duplicating `partial_success`, `verified`, `verification_note`, `errors`, and `warnings` under `data`. | Continue duplicating every partial field under `data`, because successful MCP responses expose only `data`. | Keep the same duplication under `data` for strict failures, since failure formatting also serializes only `data` after the `Error:` prefix when it is a dict. Top-level-only partial fields would be lost to direct callers. | Yes when `data.partial_success` is true and edit summary carries mutation evidence. | No. Caller must inspect partial metadata before another edit. | `mcp_server/tests/test_gh_edit_postmortem.py` direct MCP tests currently cover Phase 1 visible fields. Phase 3 direct MCP tests should expect failure framing while preserving the same JSON fields under `data`. |
| Tests asserting success | `mcp_server/tests` grep evidence: `test_dispatcher_safety.py`, `test_gh_edit_contract.py`, `test_gh_edit_postmortem.py`, `test_server_component_deprecation.py` | Phase 1 tests intentionally allow top-level success for mutation-evidence partials. Examples include `test_gh_edit_partial_errors_are_promoted_for_chat_agents`, `test_gh_edit_surfaces_edit_summary_errors_as_warnings`, and `test_gh_edit_partial_mutation_response_contains_visible_contract`. | Compatibility assertions are allowed only for mutation-evidence partials. No-mutation edit errors must already assert `success: false` and no `partial_success`. | Update tests with non-empty `edit_summary.errors` plus mutation evidence to expect strict failure, retained `partial_success`, `verified: false`, errors, warnings, and verification note. | Depends on fixture shape. Created counts, temp maps, or instance GUIDs mean yes; errors alone mean no. | No automatic retry assertions for partial mutation. | Run `rg -n "gh_edit|partial_success|edit_summary|apply_gh_edit_contract" mcp_server/tests -g "*.py"` during Phase 3 and update success assertions case by case. |
| Managed bridge source | `src/Rook/Handlers/GrasshopperHandler.cs::HandleGrasshopperEdit` / `ApplyEdit` lines 6781-6855 and edit summary construction lines 7206-7242 | The managed bridge applies the batch phases, accumulates `errors`, then returns `snapshotResult` with `edit_summary.errors`. If the snapshot succeeds, the outer `ApiResponse.Success` can remain true even when embedded edit errors exist. | Python wrappers must normalize this bridge shape before agents interpret the result. Managed can continue returning the compatibility shape during Phase 1. | Python must enforce the final contract. Aligning the managed bridge with strict semantics is optional only if every public caller still goes through Python; otherwise the bridge should also return strict failure for partial edit errors. | Yes when counts or GUID maps are non-empty and `edit_summary.errors` is non-empty. | No automatic retry through Python. Direct managed consumers need the same caution if exposed. | Add managed or integration coverage only if direct bridge consumers remain supported outside the Python contract wrapper. |

## Migration Checklist

- Flip the helper default or caller strict flag for `gh_edit` partial mutations.
- Ensure `_record_gh_to_session` records `success: false` plus `partial_success: true`.
- Update batched GH intent fallback to treat `partial_success` as non-retryable without inspection.
- Apply contract to `gh_replay_recipe` or document why replay is separate.
- Update tests currently asserting success for partial edit errors.
