# LM9B-P Evaluator-Only Continuation Result

## Scientific Question

Under the corrected evaluator contract and authority boundary, is the exact
mechanically accepted recipe from the sealed visibility intervention
semantically faithful, semantically unfaithful, or evaluation-inconclusive?

The predeclared deterministic prediction was:

```text
semantically_faithful + nonempty unresolved_intent
-> probe_candidate_blocked
```

This was a separately identified derivative observation over the exact sealed
recipe bytes. It was not a Planner rerun, a repair, or a replacement for the
historical result.

## Verified Identities

The public official verifier accepted the derivative at its identity-bound
canonical destination:

```text
reviewed implementation merge:
  ccd963e5504d6c340f87f48eaacfba0d61e74f47
source archive:
  C:/Users/bring/rook-lm9b-p-attempts/2026-07-22-visibility-intervention
source manifest raw SHA-256:
  sha256:ac7716b7d5a61e2e6359bc0e01e145d7d17e0871ff03d1d5329710541d274c90
source checkpoint aggregate identity:
  sha256:c57c88c61715588a2071d97d73e65f41447754868d18dc8517708734d92a081e
derivative archive:
  C:/Users/bring/rook-lm9b-p-attempts/2026-07-22-evaluator-only-continuation/derivatives/visibility-evaluator-01
derivative archive identity:
  sha256:48bcdcb36fb3ccee49fe358335f577ffabcb83b0f74e9f84d96951d6b3950b94
derivative subject fingerprint:
  sha256:b236cf74e89d1bf48831f0a7b6bbeacaf72482f594ee97e5cb2f6732103f0d9d
preflight fingerprint:
  sha256:df760c2f12b6d6b3d2f41bea5f658efb3d3034c8ae8bd041a19e299c26b92ab0
instrument fingerprint:
  sha256:02698f86a21bb45289c3d7cb9795bebe4c92508159678785227aa1565d13b532
attempt fingerprint:
  sha256:138c46c4450009e6f5b244f4ead0f2402912ba3833273d06944bd32999df9951
```

The verified source binding preserves the historical observation at commit
`15df78ee665cf8ff433a4af20e364365b779143b`. Its classification remains
`probe_inconclusive`, Checkpoint 2 remains `not_evaluated`, and the derivative
explicitly records `replaces_historical_result=false`.

## Exact Recipe Binding

The derivative consumed the exact archived `rook.planner_graph_recipe:v1`
bytes without rewriting, repair, translation, or reserialization. Mechanical
verification still applied the frozen semantic normalization profile
internally; that verification value did not replace the archived recipe bytes:

```text
mechanical status: mechanically_accepted
recipe raw SHA-256:
  sha256:5c5dba9def1ffc3002240f3154f02f36e60134019659d5e6a9d546b0317965af
historical and ratified recipe fingerprint:
  sha256:eb70994fa9ead99fbe75f6f25e81327045258389d46e91474cc5b581befc068a
recipe value fingerprint:
  sha256:e5c3de882dd2baf141ffe7c9d61eb311789e4993b889b7b87c977ab9470dd8f2
```

The independently recomputed mechanical gate was
`mechanically_accepted` with no diagnostics and bound the same raw, historical,
ratified, and value identities.

## Readiness Observation

One content-free readiness canary ran for the single `planner_evaluator` role
on `gpt-5.4`. It returned an attributable assistant message and the required
`ack({"ok":true})` tool call. Existing readiness verification accepted the
record with no failures:

```text
provider profile: litellm.completion.tool_calling.no_parallel:v1
readiness completed: 2026-07-22T23:52:44Z
readiness verified: 2026-07-22T23:52:45Z
maximum age: 600 seconds
readiness record fingerprint:
  sha256:1932479b163217d9624d479467a606f22c4c3b5c2a47d743a936cebd7047821a
route manifest fingerprint:
  sha256:fef425b91528cb2bd03d6839c34f75ba63e0f22f992854e2e8851a1e5633d074
decision: ready
```

Readiness established route viability at that time. It did not judge recipe
meaning or confer execution authority.

## Evaluator Observation

The evaluator terminated quiescently with one valid semantic recommendation:

```text
model: gpt-5.4
termination: valid_recommendation
recommendation: semantically_faithful
```

## Attempt Accounting

The sealed attempt capture and usage records bind:

```text
evaluator dispatches: 1
elapsed: 6,631 ms
prompt tokens: 7,718
completion tokens: 400
total tokens: 8,118
cost: $0.025295
finish reason: tool_calls
response model: gpt-5.4-2026-03-05
```

The complete bounded evaluator evidence was:

1. **`brief_fidelity`.** The recipe preserves a new 10-by-10 box array whose
   heights are lowest at the array center and do not decrease with increasing
   radial distance. The goal and maintain clauses match the governed box kind,
   X/Y counts, and radial-height relationship.
2. **`material_authority`.** The recipe does not invent the absent box footprint,
   grid spacing, or minimum/maximum height values. It preserves each as
   `unresolved_intent` and cites the applicable planning-policy constraints.
3. **`provenance_fidelity`.** The recipe cites the task envelope for box kind,
   counts, operation scope, and radial relationship; the environment snapshot
   for document-unit context; and planning policy as authorization context for
   unresolved values. It does not claim an unsupported value is known.
4. **`unresolved_intent_honesty`.** The five unresolved items correspond to
   genuinely missing semantic authority. The brief and task envelope contain
   no footprint, spacing, or height values, while policy supplies constraints
   rather than selected values.
5. **`implementation_leakage`.** The recipe contains no tools, executable
   payload, topology realization, or expected implementation answer. It
   remains a semantic planning artifact.

## Deterministic Classification

The five explicit unresolved semantic keys are:

```text
box_footprint_x
box_footprint_y
grid_spacing
minimum_height
maximum_height
```

The shared deterministic classifier combined the parser-derived
`semantically_faithful` recommendation with the verified recipe and its
nonempty blocker projection. It produced:

```text
probe_candidate_blocked
```

The falsifiable prediction therefore succeeded. The evaluator judged meaning;
deterministic authority prevented advancement because material information was
still unauthorized. The evaluator did not override explicit recipe state.

## Boundary Facts

The sealed derivative records:

```text
Planner continuation entry: absent
compiler entry: absent
Checkpoint 2: not_evaluated
execution permitted: false
historical result replaced: false
```

There was no Planner call, compiler handoff, compiler-role construction,
compiler provider call, Rhino or Grasshopper execution, or mutation authority
in this continuation.

## Supported Conclusion

For this radial scenario, a frontier Planner authored a mechanically
admissible and semantically faithful recipe after the complete accepted
language was made model-visible. The Planner preserved the requested meaning
and did not invent five material values that its authority inputs did not
supply. The evaluator recognized that restraint as faithful, and deterministic
authority correctly kept the recipe blocked.

This supports the visibility hypothesis: the earlier failures substantially
implicated contract-carrier visibility rather than demonstrating that the
model lacked the intelligence needed to author the accepted recipe language.
It does not establish that visibility was the only cause of earlier failures
or that the result generalizes beyond this controlled observation.

## Reconciliation Warning

Each preserved unresolved item contains the later-superseded
`permitted_assumption_outcomes` field under `resolution_authority`, with
`policy_auto` as its recorded value. The blocked classification depends on the
explicitly nonempty `unresolved_intent` collection, not on that field.

The field is historical recipe evidence, not new resolution authority. Any
successor must reconcile it against the latest information-authority decisions
before consuming the recipe and must not treat it, or a policy range, as an
authorized value or default.

## Limitations and Non-Claims

This result does not establish:

- an unchanged model-authored **ready** recipe crossing into LM9B-C;
- governed resolution of any unresolved item;
- repeatability of Planner authorship or evaluator judgment;
- local-model capability;
- authoritative LM9A validation;
- generality beyond the radial 10-by-10 box-array scenario;
- general Planner, evaluator, or compiler reliability;
- compiler behavior in this derivative observation;
- product integration, execution authorization, or live runtime behavior;
- that contract visibility was the sole cause of every earlier failure.

The separately preserved hand-authored R01 compiler evidence remains a distinct
observation. This derivative makes no new compiler claim and does not revise
the permanently inconclusive historical visibility result.
