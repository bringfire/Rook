# Qwen3.8 Varied Product Cohort V1 Execution

Date: 2026-08-18

Classification: **qualified cohort execution with mixed operational outcomes and
independently retained shadow judgments**

## Scope

This cohort moved beyond the repeated point-row, grid, and helix specimens. It
ran Qwen3.8 once on each of three distinct Grasshopper tasks:

1. A brownfield annular-band extension with explicit preservation duties.
2. An underspecified radial canopy-rib system requiring architectural judgment.
3. A multi-stage twisted lofted mass requiring dependent native geometry.

The Actor received no semantic evaluator feedback. Each row used a fresh
Grasshopper document and fresh Prime goal. Model or semantic failure could not
stop a later row; only environment, process, custody, or evaluator-infrastructure
failure could do so.

The campaign used the frozen V6 runtime configuration:

    model: ollama_chat/qwen3.8:27b
    thinking: low
    Prime commit: 27b5be22cf0e0e81e324a59ebabbb41edfee6ec0
    Prime skill: 30CA98809CCE8F4BE5B1CC291DEB8820511B6B13A0D546CBA93848DBC9074B07
    checkpoint: 2D574EABF45EC1EB9B9BBCFCBD68F1CBE311E466CD922DF312F88A51925D63E7
    rook_full adapter: 06F1CB4AA58FD8C4C6F61F96CE7B8A5F4FEF7B6B126D00C0550B2CB3AA0BBF74
    provider-token ceiling: 2,000,000 per row
    gateway-call ceiling: 150 per row
    wall-clock ceiling: 1,800 seconds per row

The two pre-contact custody repairs were committed at:

    0de69a0cc2cb370ea501cd5205942fc5f4560b1b

They require verified Prime goal context before cohort continuation and prevent
missing protected snapshot fields from comparing equal. No product Rook or Prime
runtime code changed.

Frozen artifact hashes:

    cohort protocol:
    3250486ED3012AC7B3EAD290984F835B3ECB0AE694D1743DD19158B0F1F00E09

    shadow adjudication:
    7E187C739654CFBAACADCC2D5040C7E71C6CA9D726FBB78BEF38DDD61224F400

    campaign runner:
    A71666972479799AC25212ADAF65DDA275AAA95F3E93C3FBC277E032C824A357

## Execution

    & C:/UDEV/Rook/mcp_server/.venv/Scripts/python.exe
      scripts/qwen38_self_termination_campaign_runner.py run
      --protocol docs/superpowers/experiments/2026-08-18-qwen38-varied-product-cohort-v1.json
      --evidence-root C:/UDEV/RookEvidence/2026-08-18-qwen38-varied-product-cohort-v1
      --document-serial 268435457
      --process-id 52292

The runner completed all three rows sequentially without retry, evaluator
feedback, fallback, or configuration change. Its top-level status `complete`
means the prescribed cohort transaction completed; it does not mean every row
passed its operational gates.

| Row | Formal semantic | Goal | Budget/custody | Shadow disposition | Seconds | Tokens | Calls |
|---|---|---|---|---|---:|---:|---:|
| VP1 | `unproven` | `complete` | pass / pass | `credible_success` | 242.469 | 501,605 | 23 |
| VP2 | `incomplete` | `complete` | fail / fail | `credible_success` | 940.265 | 2,073,688 | 69 |
| VP3 | `incomplete` | `budget_limited` | fail / fail | `partial_success` | 759.656 | 2,057,140 | 76 |

The VP2 and VP3 monitors detected and terminated each process after the first
provider report above the two-million-token ceiling. This permits a one-message
overshoot. Exact goal context, effective low thinking, EOF, evaluator
infrastructure, and zero lingering owned processes were established in all
three rows.

## Independent Shadow Judgment

No aggregate score is assigned. Each disposition applies only to its retained
row evidence and is separate from formal process qualification.

### VP1: Credible Success

Observed:

- The receipt-fenced snapshot at source sequence 14 contains nine components,
  seven wires, zero errors, and zero warnings.
- The original Radius slider remains `8`; a new Width slider is `5`; the added
  `R+W` output is `13`; and distinct original and outer Circle components each
  return one Circle.
- The closed preservation evaluator passed with no violations. The original
  circle, Radius control, arithmetic reference components, values, and exact
  protected flows remained intact.
- Qwen used one committed edit. It later inspected both circle outputs and
  errors, then completed its Prime goal within budget.

Inferred:

- The original radius acts as the inner boundary and Width expands the outer
  boundary, yielding a useful adjustable annular band. The prompt did not
  require Radius to represent a symmetric centerline.
- The later output and error inspections were relevant verification, although
  they caused the frozen "snapshot must be the final gateway event" metric to
  report `later_gateway_call`.

Unresolved:

- Width was not behaviorally perturbed in this row. Its causal role is supported
  by the retained wiring and solved `8 + 5 = 13` result, not a separate probe.

### VP2: Credible Semantic Success, Operationally Incomplete

Observed:

- The receipt-fenced snapshot at sequence 66 contains 45 components, 62 wires,
  zero errors, and zero warnings.
- It contains seven meaningful controls: Span, Hub, Rise, Ribs, Curve, Rot, and
  Smp. Twelve rotated interpolated curves form radial ribs; inner and outer
  circles and one revolution Brep provide additional inspectable canopy
  geometry.
- Qwen changed Span/Ribs from `6/12` to `10/24` and restored them. It changed
  Curve/Rot from `0.8/0` to `-1/45` and restored them. Each successful change
  produced a solve receipt and was followed by observation.
- A retained panel states the center, z-up convention, controls, rib equation,
  shell construction, and radians assumptions.
- Qwen called `goal.complete()`. The monitor subsequently observed cumulative
  usage above the ceiling and terminated before a valid terminal Prime marker
  could be retained.

Inferred:

- The native graph is a coherent conceptual canopy framework, not incidental
  geometry. Its controls govern span, opening, rise, rib count, profile shape,
  and rotation in ways consistent with the underspecified request.
- The disclosed assumptions are proportionate; no unstated aesthetic choice is
  treated as user fact.

Unresolved:

- The row lacks a formally admitted terminal Prime lifecycle and independent
  post-process final observation because of the token breach.
- The fenced snapshot proves native curve/Brep outputs and data flow, but does
  not provide a rendered visual judgment of architectural quality.

### VP3: Partial Success

Observed:

- The receipt-fenced snapshot at sequence 74 contains 25 components, 43 wires,
  zero errors, and zero warnings.
- Base Size, Total Height, Profile Count, and Total Twist controls participate
  in a native pipeline: Ellipse -> Rotate Axis -> Move -> Loft.
- The final graph returns 14 transformed NurbsCurve profiles and one Loft Brep.
  Retained values show 14 levels from z=`0` in approximately `115.385`
  increments and twist angles progressing toward `360` degrees.
- Qwen exercised the requested principal controls: twist `180 -> 360`, profile
  count `8 -> 14`, base size `200 -> 300`, and height `600 -> 1500`.
- After the fenced snapshot, Qwen baked outputs to Rhino and planned another
  visual check instead of completing. The token monitor stopped the row.

Inferred:

- The retained topology and solved outputs support a credible adjustable
  twisted lofted mass. Native Ellipse profiles are appropriate closed profiles
  for the Loft input.
- The requested geometry was materially achieved before the budget breach.

Unresolved:

- Qwen did not call `goal.complete()`, and the last gateway event was a bake
  rather than the admitted fenced snapshot.
- No post-bake visual inspection or terminal process evidence was retained.
  These stopping failures prevent a full credible-success disposition even
  though the Grasshopper artifact itself is strong.

## Efficiency Evidence

| Row | Input tokens | Output tokens | Discovery calls | Total calls | Mutations / commits |
|---|---:|---:|---:|---:|---:|
| VP1 | 484,254 | 17,351 | 16 | 23 | 1 / 1 |
| VP2 | 2,012,994 | 60,694 | 36 | 69 | 7 / 6 |
| VP3 | 2,009,582 | 47,558 | 51 | 76 | 5 / 5 |

Input/context accounted for approximately 96.5%, 97.1%, and 97.7% of reported
tokens respectively. Discovery accounted for 16/23, 36/69, and 51/76 gateway
calls. VP2 and VP3 therefore expose a concrete efficiency problem: repeated
context and component/tool discovery dominate cost even when Qwen ultimately
constructs credible geometry.

This cohort does not justify a supervisor, terminalization protocol, or runtime
semantic acceptance vocabulary. It does justify measuring and simplifying the
existing observation/discovery path before increasing task complexity.

## Evidence Custody

Pre-contact verification retained:

    177 passed, 11 existing warnings
    exit code: 0
    stderr: empty

Independent post-run verification reproduced:

    campaign: 123/123 files, zero mismatches
    VP1:       38/38 files, zero mismatches
    VP2:       33/33 files, zero mismatches
    VP3:       33/33 files, zero mismatches

Manifest SHA-256 values:

    campaign:
    3E62BE054F33AA87D8F1A6745BA7B0769419220A954A5667160CB89EB1EB4990

    VP1:
    22C78D69103950B647C89E1A43B97079F1FABC8C7EBB3C42C3284CD8AE2E7BE9

    VP2:
    4B03E3CEFA12019764E9F452C0F35BE85A919518281CD81570801FAB3677C124

    VP3:
    0BC0CB20AFF0B0CF563A1D11C6112DB5E27FBA1F236C26CDB690EF944471C397

The evidence root is:

    C:/UDEV/RookEvidence/2026-08-18-qwen38-varied-product-cohort-v1

The sealed evidence archive was not modified during independent adjudication.
No campaign-owned process remains.

## Bounded Conclusion

Qwen3.8 demonstrated materially useful Grasshopper competence across
brownfield preservation, open architectural judgment, and multi-stage native
geometry. The result is not three operational passes: only VP1 stayed within
all row gates. VP2 achieved and formally completed a credible artifact but
crossed the provider-token ceiling; VP3 achieved a credible artifact but failed
to stop before the same ceiling.

The next pressure is narrow and empirical: reduce repeated discovery and
context replay while preserving the simple model-led loop and existing receipt-
fenced observation discipline. No broader architecture is justified by this
cohort alone.
