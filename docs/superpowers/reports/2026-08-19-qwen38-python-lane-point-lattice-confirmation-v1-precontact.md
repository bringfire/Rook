# Qwen3.8 Python-Lane Point-Lattice Confirmation V1: Pre-Contact Review

**Date:** 2026-08-19
**Status:** `frozen_precontact_review_required`
**Parent:** `d43fad3f62c2e00c374d13434a3302c4377a7138`

No Rhino, Grasshopper, MCP, Prime, Ollama, Qwen, deployment, target
preparation, or screening contact occurred while preparing this package.

## Objective

Confirm the unchanged Qwen3.8 Python authoring lane on one compact task from a
different computational and geometry family.

The exact prompt is:

> Create an adjustable rectangular lattice of points in the XY plane. Expose
> integer Columns, integer Rows, X Spacing, and Y Spacing controls. Produce
> exactly Columns × Rows points, starting at the origin, with adjacent columns
> separated by X Spacing and adjacent rows separated by Y Spacing; all Z
> coordinates must remain zero. Exercise Columns and Y Spacing independently,
> observe each effect, restore defaults, obtain a final receipt-fenced
> observation, and stop when satisfied. Do not create curves, surfaces, bake,
> or add downstream geometry.

This is a single varied confirmation, not a native comparison, rerun, or claim
of general Python-lane superiority.

## Frozen Boundary

The confirmation preserves the prior Python row's exact:

- Python fixture;
- Qwen3.8 model and low thinking level;
- Prime revision, bootstrap, and sealed kernel;
- Rook build and `rook_full` adapter;
- goal and provider budgets;
- fresh empty target preparation;
- silent post-run evaluation;
- receipt-fenced checkpoint and completion discipline; and
- evidence and process custody.

The only operational changes are the task, single-row execution order, and
task-specific shadow-adjudication questions. There is one execution, no retry,
no tuning, no native row, no C#, and no skill edit.

The existing authoring-lane runner profile was minimally generalized to admit
one closed confirmation shape:

```text
classification = single_python_lane_varied_confirmation
executionOrder = [P2]
row fixture = exact prior P fixture
```

It rejects a changed fixture hash, an added native row, or a nonzero retry
count. This is experiment-runner admission only; no product runtime or model
tool surface changed.

## Shadow Adjudication

The evaluator remains silent until the Actor terminates and the evidence is
sealed. Independent judgment asks whether retained evidence supports:

1. exactly `Rows × Columns` points;
2. an origin-anchored rectangular XY lattice with the requested spacings and
   every Z coordinate equal to zero;
3. integer behavior through retained source plus solved count, without claiming
   unobserved slider step granularity;
4. independent Columns and Y Spacing exercise and exact restoration;
5. Python-lane compliance without curves, surfaces, baking, or downstream
   scope expansion; and
6. a final receipt-fenced observation before `goal.complete()`, with no later
   gateway call.

No diagnostic criterion was added. Existing point count and coordinates are the
intended semantic evidence.

## Efficiency Observations

The retained report will also record:

- searches for listed capability names;
- first committed mutation sequence;
- cumulative provider tokens through the uniquely originating Actor turn;
- total discovery, mutation, call, token, and wall-clock cost;
- implementation-mode stability;
- equivalent repeated mutation without a named material reason; and
- semantic and evidence quality before assigning efficiency credit.

The inherited thresholds remain 12 gateway calls and 200,000 cumulative
provider tokens through the assistant turn that originated the first committed
source event. Ambiguous attribution remains `unproven`.

## Test-First Verification

The new tests were observed RED before implementation:

```text
3 failed: confirmation protocol and adjudication absent
```

After the minimal runner and frozen artifacts were added:

```text
4 targeted tests passed
208 focused campaign/acceptance tests passed
11 existing dependency warnings
```

Python compilation and JSON parsing passed. `git diff --check` passed. The
proposed durable evidence root is absent.

## Frozen Hashes

```text
Python fixture
E70095B557A2C55FDA33FCA7BC7830A4D2FD54F98E7B66A861AC87DB9C4B4AD6

Source paired protocol
D75537826C764A254928279420AEA045FB8AFBD9F60BDBE692A0B8A506CDEFB6

Point-lattice protocol
8247806CC2C95887898BD127A906438D8431BE198445E729D6908775FD739666

Shadow adjudication
353B1B0C27F8A1B766FACC6B219ADFC7D90CD07B7D1EFF3016CEEE251DA23EE3

Runner
8B0DB56B830C19C0D462C64094AA44C93F710B1B9D69D66DF951AAF827788819
```

## Proposed Post-Approval Command

Run only after explicit independent approval:

```powershell
$python = "C:/UDEV/Rook/mcp_server/.venv/Scripts/python.exe"
$runner = "C:/UDEV/Rook/.worktrees/coordinating-intelligence-evidence-ledger/scripts/qwen38_self_termination_campaign_runner.py"
$protocol = "C:/UDEV/Rook/.worktrees/coordinating-intelligence-evidence-ledger/docs/superpowers/experiments/2026-08-19-qwen38-python-lane-point-lattice-confirmation-v1.json"
$evidence = "C:/UDEV/RookEvidence/2026-08-19-qwen38-python-lane-point-lattice-confirmation-v1"

& $python $runner run `
  --protocol $protocol `
  --evidence-root $evidence `
  --document-serial 268435457
```

The runner must refuse before contact if protocol, fixture, runtime, target,
budget, tool-surface, or preflight custody fails. After execution, evidence must
be sealed and independently verified before interpretation.

## Gate

Stop here for normal independent pre-contact review. Do not execute the command,
prepare a target, or contact any live system from this package.
