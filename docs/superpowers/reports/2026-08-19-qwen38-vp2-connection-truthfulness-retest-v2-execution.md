# Qwen3.8 VP2 Connection-Truthfulness Retest V2

**Date:** 2026-08-19
**Status:** Complete execution; not qualified
**Live rows:** One
**Reruns:** None

## Question

Would Qwen progress on the frozen VP2 radial-canopy task after the closed Rook
connection-truthfulness repair removed the misleading `Point On Curve`
admission and reporting behavior?

The task, Qwen model, low thinking level, Prime runtime, versioned experimental
skill, adapter, checkpoint, limits, and silent evaluator were unchanged from the
strategy-discipline specimen. The installed Rook runtime was the sole declared
operational repair.

## Result

The row completed naturally:

```text
Prime exit:                    0
agent_end:                     exactly one
goal status:                   complete
actor final checkpoint:       pass
custody:                       pass
budget:                        fail
hidden semantic evaluation:   unproven
```

The execution lasted **1,325.25 seconds** and recorded **99 canonical gateway
events**. No qualification-owned process remained. Rhino PID `210020` and the
native target remained active.

Provider-reported cumulative usage was **2,055,570 tokens**, exceeding the
two-million ceiling by 55,570 on the first report above the limit. Prime's goal
completed at **1,961,127 tokens**. The exact 94,443-token difference was the
post-`goal.complete()` final response turn: 93,492 input and 951 output tokens.
The runner therefore correctly records a budget failure even though the goal
completed before Prime's own goal budget was exhausted.

## Final Grasshopper State

The final receipt-fenced snapshot observed:

```text
16 components
14 wires
0 errors
0 warnings
```

The graph contains:

- one Python 3 `Canopy Ribs` component;
- five sliders: `Span`, `Count`, `Rise`, `Oculus`, and `Turn`;
- one native `List Length` cross-check;
- nine explanatory or output panels.

At the restored defaults it produced:

```text
Span:          10
Count:         12
Rise:          4.5
Oculus:        0
Turn:          0 degrees
Ribs:          12 NurbsCurve items
Rib length:    11.2995965817 each
Total frame:   135.59515898
Hub:           (0, 0, 4.5)
Sphere center: (0, 0, -8.8611111111)
```

The retained point evidence places 12 starts on a radius-10 ring at Z=0 and
12 tips at the apex. The graph is a plausible and useful conceptual radial
canopy framework. This is post-run interpretive judgment, not a deterministic
semantic certification.

## Empirical Loop

Qwen did not delete or wholesale-rebuild committed work.

Its eight committed mutations were:

1. Create the Python script component.
2. Create and connect the controls, native cross-check, and panels.
3. Repair a `Vector3d` API error locally.
4. Repair a `NurbsCurve.Length` API error locally.
5. Perturb `Count`, `Rise`, and `Turn` together.
6. Restore those three controls.
7. Perturb `Oculus`.
8. Restore `Oculus`.

Each behavioral mutation received a solve receipt. The final restore produced
receipt `6e2eec9803e25ec7b0e5481a6ebf4115`; Qwen waited until it was ready and
made the receipt-fenced snapshot at source sequence 98. That snapshot was the
last gateway event before `goal.complete()`.

Qwen's final report incorrectly said that **all five** sliders were exercised.
`Span` was never perturbed. The other four controls were exercised and
restored, with `Count`, `Rise`, and `Turn` tested in one combined change
rather than independently.

## Repair Attribution

The connection-truthfulness repair was **not exercised by this specimen**.

Qwen explored native components, then selected a Python-component strategy
before committing a native canopy graph. It never created or wired `Point On
Curve`, never requested a nonexistent standalone-parameter index, and never
received the repaired rejection or canonical parameter projection.

The run therefore does not prove that the Rook repair caused the improved
outcome. It does show that, under the repaired frozen runtime, Qwen could:

- make a named strategy choice from live discovery evidence;
- commit one strategy;
- treat two subsequent failures as local;
- preserve all unaffected committed work;
- use solve receipts and fenced observations;
- exercise important controls;
- restore the final state; and
- complete without false success on a graph containing diagnostics.

The prior live and offline repair qualification remains the evidence for the
`Point On Curve` contract itself.

## Efficiency

V2 exhibited better post-commit discipline, but discovery remained dominant:

| Observation | Strategy V1 | Repair retest V2 |
|---|---:|---:|
| Elapsed seconds | 945.406 | 1,325.25 |
| Gateway calls | 87 | 99 |
| Discovery calls | 68 | 66 |
| Committed mutations | 3 | 8 |
| Cumulative provider tokens | 2,026,180 | 2,055,570 |
| Goal completed | No | Yes |
| Final fenced checkpoint | No | Yes |
| Healthy final graph | No | Yes |

The first committed mutation occurred at source sequence 68, after 68 prior
gateway events. Qwen then made no replacement-component discovery calls after
commitment; it repaired the script and tested the resulting graph. V2 therefore
exhibited better completion and post-commit discipline than Strategy V1. The
single stochastic specimen does not establish that the unexercised connection
repair caused either improvement.

The retained trace also proves explicit noncompliance with the frozen evidence
reuse discipline before the first mutation:

- 28 capability searches;
- 25 component-library searches;
- 11 contract reads; and
- 2 metadata requests.

Qwen read both `gh_edit` and `gh_create_script` twice. The script contract was
available by source sequence 36, but mutation did not begin until sequence 68.
This is not merely high discovery volume: known tool names were searched again,
resolved contracts were reread, and no missing field in the next intended call
was named as the blocker for the intervening exploration.

Prime's retained runtime JSONL is **1,115,824,926 bytes** because streaming
thinking updates retain cumulative partial text. That evidence-volume behavior
did not alter this run, but it is a concrete operational cost.

## Trace Admission Limitation

The hidden evaluator returned:

```text
unproven — authoring_trace_invalid
```

The exact cause was source sequence 71:

```text
gh_snapshot({
  "include_data": false,
  "readiness_receipt_id": "ca581c2d..."
})
-> success: false
-> error: readiness_snapshot_request_invalid
```

This was a failed read-only request. The campaign-time normalizer classified the
unrecognized failure as `unknown`, which invalidated the whole trace. It did not
mutate Grasshopper and did not prevent the later valid final checkpoint. A
subsequent experiment-runner correction recognizes only this exact closed
refusal as observational no-commit; altered or unfamiliar failures remain
fail-closed. The retained source bytes and historical campaign classification
remain unchanged.

Independent judgment would still be required for this open architectural task,
so repairing trace admission would produce an accurate reason boundary, not a
deterministic semantic pass.

## Predeclared Criteria

| Criterion | Result | Basis |
|---|---|---|
| `no_wholesale_deletion_without_live_evidence` | pass | No delete operation occurred. |
| `preserve_unaffected_committed_work_during_pivot` | not exercised | No post-commit pivot occurred. Both script errors received local repairs and committed graph state remained. |
| `fewer_than_15_replacement_strategy_discovery_calls` | pass | Zero replacement discovery calls followed the first committed mutation. |
| `fewer_than_2026180_cumulative_provider_tokens` | fail | 2,055,570 tokens. |
| `any_strategy_pivot_supported_by_live_evidence` | not exercised after commitment | Before committing, exact searches did not establish the desired helpers and Qwen chose a script path. Search absence did not prove component impossibility. |
| `mechanically_healthy_canopy_without_material_regression` | credible shadow pass | Final graph had 12 rib curves and zero diagnostics; semantic judgment remains external. |
| `receipt_fenced_final_snapshot_then_goal_complete` | pass | Sequences 96-98 were restore, ready wait, and fenced snapshot; completion followed. |
| `budget_pass` | fail | First over-limit provider report was 2,055,570. |
| `custody_pass` | pass | Natural exit, one `agent_end`, closed source, no owned processes. |

## Disposition

This is a materially better product outcome than Strategy V1, but it is **not a
qualified confirmation of the connection repair** and it does **not qualify the
experimental guidance as a product default**.

Do not rerun VP2 merely to force `Point On Curve` usage. The finite connection
contract is already directly qualified. The next KISS work should remain
separate:

1. The exact read-only `readiness_snapshot_request_invalid` trace shape is now
   classified offline as observational no-commit, with near-matches fail-closed.
2. Future protocols may set Prime's goal budget below the total provider ceiling
   to reserve final-response cost. The outer ceiling still includes every token,
   including the 94,443-token final turn observed here.
3. Treat discovery/context cost as the dominant remaining efficiency problem.
4. Move to a smaller genuinely varied product task, without adding semantic
   supervision or another broad reasoning rule. Freeze this as the sole skill
   delta for a future prompt experiment:

   > Tool names listed in this skill are already known. Read each required
   > contract directly once; do not search for a known tool name. After
   > `gh_edit` and one viable authoring contract are known, choose an
   > implementation mode and create the smallest working scaffold. Further
   > discovery must identify a field missing from the next intended call.

   The current skill's opening `gh_edit` search example must change in that
   experimental fixture, not in the qualified product baseline.

## Custody

- Pre-contact verification: **189 passed**, 11 existing warnings.
- Historical source evidence: **54/54** verified before contact.
- Sealed evidence: **50/50** independently reverified.
- Evidence manifest SHA-256:
  `A8F99C31D711C8C24A2EEC2519D6F2D670AF74B22CA2F43F145EA0BB10F81DAC`.
- Protocol SHA-256:
  `F4709C6551C25A1C935DD98BE1ECDFEC8F67926A0894DA93608900A3B8355B4F`.
- Prime JSONL SHA-256:
  `55F8FF7B38EC2B817276A233EC799AFD2D9DC95837290295CA20A1ACAE3DC044`.
- Source JSONL SHA-256:
  `C518B8BC228701CCCB05AA228C345378076B4580DB7589168F7468E4BCC870EE`.
- Authoring trace SHA-256:
  `8CF4622AFB8B75A2F5F6E07E3145512B1BEE6764340D0F7C48B6D3A0EB24103B`.
- Final checkpoint SHA-256:
  `83933776B64C1A69B4D83364D0EB894A69618F3D837ACF38A6AA020DD86133B9`.
- Stderr: empty.
- Worktree was clean for the complete live transaction.

Evidence root:
[qwen38-vp2-connection-truthfulness-retest-v2](C:/UDEV/RookEvidence/2026-08-19-qwen38-vp2-connection-truthfulness-retest-v2)
