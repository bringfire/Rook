# Qwen3.8 Native/Python Authoring-Lane Screen V1: Pre-Contact Review

Date: 2026-08-19

Status: `frozen_precontact_review_required`

Repository parent: `626fa30fad3bee3b0446acac1a7a9bcbb0cc23b1`

No Rhino, Grasshopper, MCP, Prime, Ollama, Qwen, deployment, target
preparation, or screening contact occurred while preparing this package.

## Objective

This paired product screen asks whether routing Qwen3.8 into an explicit
Grasshopper authoring lane before discovery reduces orientation churn while
preserving the model-led empirical loop.

The model receives the same task on two fresh empty targets:

- `N`: native Grasshopper components only.
- `P`: one Python script component plus ordinary controls.

The fixtures are model-facing guidance over the unchanged full Rook tool
surface. They do not mechanically hide capabilities. Route compliance and any
implementation-mode switch are measured from the retained gateway trace.

The canonical product skill is unchanged at SHA-256
`30CA98809CCE8F4BE5B1CC291DEB8820511B6B13A0D546CBA93848DBC9074B07`.
Neither experimental fixture is approved for product promotion by this pair.

## Evidence reviewed offline

Preparation re-read the retained VP1, VP2, and VP4 execution reports and the
VP4 tool catalog. The capability audit binds the current Python server source
and the retained 382-entry public catalog before accepting any authoring-lane
contract.

The audit confirms these exact public names:

- Canonical script creation: `gh_create_script` with explicit
  `language="python"` or `language="csharp"`.
- Back-compat creation aliases: `gh_create_python_script` and
  `gh_create_csharp_script`.
- Preparatory pin editing: `gh_set_script_pins`.
- Terminal script source update: `gh_update_script`.
- Shared mutation and evidence path: `gh_edit`, `gh_errors`,
  `gh_wait_for_solve_readiness`, and receipt-fenced `gh_snapshot`.

The native lane additionally starts from `gh_library` and
`gh_batch_component_info`. The Python lane may use native discovery only for an
ordinary control or a named blocker required by the next mutation. C# is not
part of this experiment.

## Offline implementation

The runner now counts verified Python and C# script-creation aliases as
mutation attempts. It reports both:

- `firstMutation`: the first attempted mutation route and its commit status.
- `firstCommittedMutation`: the first authentic committed mutation.

This prevents a refused script-creation attempt from earning the experiment's
first-commit threshold while preserving existing native `gh_edit` accounting.
Historical evidence is unchanged.

The frozen protocol preserves the VP4 model, low thinking, Prime
revision/bootstrap, Rook build, adapter, goal/provider budgets, reserve, target
preparation, silent evaluator, evidence capture, checkpoint discipline, and
completion discipline. It changes only the task, row order, canonical skill
reference, row skill fixture, and task-specific shadow adjudication.

The exact shared task is:

> Create an adjustable circular array of vertical line segments. Expose
> Radius, Count, Height, and Start Angle. Produce exactly Count evenly spaced
> vertical lines around the circle. Exercise Count and Height, observe the
> effects, restore defaults, obtain a final receipt-fenced observation, and
> stop when satisfied.

There is one execution per row, no retry, no mid-campaign tuning, and no
evaluator feedback before termination. The result is interpreted as a product
screen, not pure causal proof.

## Frozen artifacts

```text
Canonical skill
30CA98809CCE8F4BE5B1CC291DEB8820511B6B13A0D546CBA93848DBC9074B07

Native lane skill
AB053FA8FE7D0526E8C076DE4488C34FAD127A7CEC63BF3059378F1EB9EDBE53

Python lane skill
E70095B557A2C55FDA33FCA7BC7830A4D2FD54F98E7B66A861AC87DB9C4B4AD6

Capability audit
DB701751044A0B3EDDB08D5FF1609D7A74B068AE696AE637765D0A9D86DFF7AF

Shadow adjudication
328774E470C612F6FA127DDF174C65D9438986FC71D868DE28DD3F427176CC25

Protocol
B7CFC42F1145B444E1AF721C64A9AB28AAE5882184DB4257E3467CA1A96B4215

Runner before the pre-contact documentation commit
07DF207B7178FB78839BA3954800290E3C09ED6AEE0F62EC2D9F45E6364F955D
```

## Predeclared observations

Each row is reported independently against these frozen criteria:

- credible result or honest failure;
- budget and custody pass;
- Count and Height exercised and restored;
- final receipt-fenced snapshot before `goal.complete()`;
- no later gateway call;
- no search for listed capabilities;
- no implementation-mode switch;
- first commit within 12 gateway calls and 200,000 cumulative tokens;
- no equivalent repeated mutation without a named material reason; and
- no efficiency credit if semantic or evidence quality regresses.

The post-run report compares first-mutation cost, discovery, route stability,
semantic quality, evidence custody, stopping, total tokens, calls, and time.

## Verification

The focused offline suite passed:

```text
205 passed, 11 existing dependency warnings
```

It covers the protocol, exact fixture deltas, capability custody, per-row skill
staging, script-alias mutation telemetry, refused-attempt handling, unchanged
native mutation telemetry, runner behavior, and the existing behavioral
acceptance surface.

Python compilation passed. The actual runner admitted the protocol and shadow
adjudication with execution order `N,P`. Every frozen hash above matched,
`git diff --check` passed, and the proposed durable evidence root was absent.
The committed scope and clean worktree are verified after the pre-contact
commit and reported with the review handoff.

## Proposed post-approval command

Run only after explicit independent approval and only while the resumed goal
is active:

```powershell
$python = "C:/UDEV/Rook/mcp_server/.venv/Scripts/python.exe"
$runner = "C:/UDEV/Rook/.worktrees/coordinating-intelligence-evidence-ledger/scripts/qwen38_self_termination_campaign_runner.py"
$protocol = "C:/UDEV/Rook/.worktrees/coordinating-intelligence-evidence-ledger/docs/superpowers/experiments/2026-08-19-qwen38-native-python-authoring-lane-screen-v1.json"
$evidence = "C:/UDEV/RookEvidence/2026-08-19-qwen38-native-python-authoring-lane-screen-v1"

& $python $runner run `
  --protocol $protocol `
  --evidence-root $evidence `
  --document-serial 268435457
```

The runner must refuse if the evidence root already exists or any frozen
custody, budget, target, tool-surface, or preflight gate fails. After approval,
it runs `N` once and `P` once, seals and verifies evidence, and performs no
retry or tuning.

## Gate

The package must stop here for independent review. No live command above is
authorized by this document. The active Prime goal must remain active until
authorized execution, verified evidence, a committed honest result report,
final validation, and a clean worktree are all complete.
