# Work Queue

**Last triaged:** 2026-05-12 (**Slice 3 video poster sidecar producer is ready to publish.** Branch `codex/video-poster-sidecar-producer` at current PR head `4bd4f85` has final no-findings review against the approved Slice 3 spec/plan. Fresh verification in the slice worktree covered the focused Slice 3 suite (`88/88`), full managed `net48` suite (`2012/2012`), primary managed plugin build `dotnet build src\Rook\Rook.csproj -f net7.0 --no-restore`, `git diff --check origin/main...HEAD`, and clean worktree status. No live Rhino or real-ffmpeg smoke was run for this slice; that matches the service-boundary scope. Current ffmpeg resolution is external only: Slice 3 production uses a replaceable `ffmpeg.exe` resolved from `PATH`; explicit configured-path support exists in the lower-level resolver/test seam but is not wired to production configuration yet. This machine's dev binary is the WinGet `Gyan.FFmpeg` full build on `PATH`, which is acceptable for local development but has `--enable-gpl` and must not be treated as a shippable Rook dependency. **Current Now remains:** Slice 4 frame-exact `start_frame` / `end_frame` sidecar producer. **Do not skip ahead** to existing-video backfill, provider-payload poster ingestion, installer ffmpeg packaging, or GH NLE token work until Slice 4 gates are satisfied and the ffmpeg packaging/license decision has its own reviewed slice.)

**Last triaged:** 2026-05-12 (**RookVision video thumbnail Slice 3 complete.** Newly completed generated-video artifacts now attempt synchronous best-effort `poster` sidecar creation after the MP4 artifact is durably saved and before the video job appends `Complete`. `ArtifactStore.Create(generated_video/video)` remains the durable success boundary: missing ffmpeg, extraction failure, invalid output, sidecar publish failure, post-artifact local I/O failure, cleanup failure, unexpected poster finalizer failure, and poster-stage cancellation cannot convert the saved video into `Error` or `Cancelled`. Poster publication uses `VideoSidecarPublisher`; duplicate posters remain idempotent no-ops; Gallery consumes `poster` when present without eager MP4 preload; and generated-video frame picker eligibility remains limited to future `start_frame` / `end_frame` roles. **Promoted to Now:** Slice 4 frame-exact `start_frame` / `end_frame` sidecar producer. **Do not skip ahead** to existing-video backfill, provider-payload poster ingestion, installer ffmpeg packaging, or GH NLE token work until Slice 4 gates are satisfied.)

**Last triaged:** 2026-05-11 (**RookVision video thumbnail/frame-sidecar roadmap advanced to Slice 3.** The full thumbnail/chaining path is tracked in `docs/rook_docs/video-thumbnail-roadmap.md`. Current state: Slice 0, the sidecar contract substrate, is complete and pushed to `main`; it pins `poster` as display-only, keeps generated-video picker eligibility role-level, excludes `video`/`poster` from frame input paths, and keeps unknown provider payload fields inert. Slice 1, the local MP4 extraction tooling design + small executable spike, is complete and pushed to `main`: the spike used an existing local generated-video MP4, extracted one poster candidate through replaceable external `ffmpeg.exe`, wrote no `ArtifactStore` data, avoided `VideoJobManager` integration, avoided provider calls, avoided installer changes, recorded sanitized findings in `docs/rook_docs/video-extraction-spike-findings.md`, and selected ffmpeg as the production extraction-path candidate. Slice 2, sidecar publication semantics, is complete and pushed to `main`: `ArtifactStore.AppendBlob(...)` owns atomic append-style blob publication; `VideoSidecarPublisher` owns generated-video sidecar policy; duplicate sidecar roles are deterministic idempotent skips at the video layer; failed manifest replacement leaves the original manifest valid and does not reference missing blobs; and the path supports both new-video sidecars and future existing-video backfill without wiring extraction, Gallery, picker, providers, installer packaging, or `VideoJobManager`. **Current Now:** Slice 3 poster thumbnail producer. Produce display-only `poster` sidecars from the local MP4 for completed generated-video artifacts by using the existing ffmpeg extraction candidate and publishing only through `VideoSidecarPublisher`. Sidecar extraction/publication failure must remain non-fatal to video job success, `poster` must remain picker-ineligible, and completed videos should show Gallery thumbnails without eager MP4 preload. **Next after Slice 3:** frame-exact `start_frame` / `end_frame` sidecar producer, then existing-video reconcile/backfill. **Corrected principle remains binding:** local MP4 extraction is the default producer for `poster`, `start_frame`, and `end_frame`; provider payload audit is optional/parallel and only useful for provider/model-specific display-poster optimization. **Do not skip ahead** to frame-exact sidecars, backfill/reconcile, installer dependency changes, provider-wide fal mappings, or GH NLE token work until the roadmap gates are satisfied.)

**Last triaged:** 2026-05-10 (**#114 lifecycle-wrapper coverage closed.** Added managed source-level coverage in `RookPluginLifecycleSourceTests` for the plugin lifecycle wrapper gap without adding native shutdown routes, harness fault injection, or Rhino lifecycle test seams. The tests pin `OnLoad` scheduling through `TryInitializeRuntime`, startup video reconcile as a non-fatal caught/logged block, independent `OnShutdown` try/catch wrappers for native bridge cleanup, chat shutdown, and video subsystem disposal, and `base.OnShutdown()` remaining after/outside teardown wrappers. The source reader strips comments before matching so a commented-out lifecycle call is not a false positive. Mutation check commented out `ChatServiceManager.Instance.Shutdown()` and the focused class failed on the intended missing-wrapper assertion; restored code then passed. **Promoted to Now:** post-#114 product/polish lane selection. **Tentative Next:** RookVision video thumbnails Tier 1, because it is the smallest visible polish candidate if selection does not choose #37 or video MCP parity.)

**Last triaged:** 2026-05-10 (**Rook v1.5.3 Vision-panel hotfix shipped, then #113 test-hardening closeout selected.** `v1.5.3` was released from `main` with the RookVision dedicated native-panel host fix (`fb7fe45`) plus version bump (`eef989b`) and GitHub asset `Rook-Setup-1.5.3.exe`. The local clean-env follow-up showed the old user-level WebView diagnostics flags were not masking the issue here; the shipped fix removes the Vision-only chat-tab host/reload path rather than relying on the risky repaint workaround. After release, the queue was re-read: open items were #113, #114, #53, #37, #34, #29. #29/#34 remain blocked, #53 remains parked because its trigger has not fired, and #37 is real product work but larger native block-link expansion. **Closed current Now item:** #113 with source-level regression tests in `NativeVisionDispatchSourceTests` pinning `DispatchVisionOpWithPathId`, artifact routes using `artifact_id`, and video routes using `job_id`; mutation check changed one video route to `artifact_id` and the new test failed on the intended route. **Promoted to Now:** #114 lifecycle-wrapper coverage follow-up. **Promoted to Next:** post-#114 product/polish lane selection, with #37, RookVision video thumbnails, and PR-V4 video MCP parity as the leading candidates.)

**Last triaged:** 2026-05-08 (**PR #142 Rhino runtime harness shipped and merged.** The first #114 slice is now complete: `scripts\run_rhino_runtime_harness.py` starts one owned Rhino process, waits only for `%TEMP%\rook\instance-{PID}-native.json` matching `Popen.pid`, verifies `/ping` on that exact owned port, scopes selected live pytest smoke with `ROOK_RHINO_PORT` + `ROOK_RHINO_PROCESS_ID`, captures run artifacts under `.scratch\rhino-runtime-harness`, snapshots owned discovery before shutdown, saves dirty smoke documents into the run artifact folder before external close, and closes only the owned Rhino process. Live verification covered `--smoke ping-only` and `--smoke pytest-select`; `pytest-select` passed all 13 selected live tests with `cleanup.status: graceful_exit` and no stale native discovery. **Promoted to Now:** #113 native unit-test gap, because the harness foundation is shipped and the remaining known test-debt item is small and explicit. **Promoted to Next:** #114 lifecycle-wrapper follow-up, limited to deciding the next evidence-backed slice for `RookPlugin.OnLoad` / `OnShutdown` try/catch coverage now that process-owned live smoke exists.)

**Last triaged:** 2026-05-08 (**Promoted #114 Rhino runtime harness to Now.** After reviewing the existing live-Rhino test foundation, we are not starting from zero: `mcp_server/tests/*_live.py`, `pytest -m requires_rhino`, `scripts/validate_rhino_operational_suite.py`, `scripts/validate_gh_runtime.py`, `scripts/validate_gh_operational_suite.py`, and the installer/register scripts already cover parts of the runtime-validation story. The missing piece is a process-owning harness that starts Rhino, waits for RookNative discovery/health, runs selected live smoke/tests, captures `%TEMP%\rook` logs, shuts Rhino down, verifies cleanup, and can later grow failure-injection seams for `RookPlugin.OnLoad` / `OnShutdown` wrapper coverage. **Promoted to Now:** #114 as the concrete next build item. **Promoted to Next:** #113 native unit-test gap or explicit follow-up after the harness slice is scoped, because #113 is smaller but less leverage than the harness foundation now selected.)

**Last triaged:** 2026-05-08 (**Rook v1.5.2 released and installer workflow reconciled with the current net7 companion architecture.** Since the previous triage, Kling v3 Standard I2V shipped on `main`, live Rhino/fal smoke passed after lifecycle-root and artifact-fetch robustness fixes, Seedance 1080p support/pricing was corrected and smoke-tested, the Inno installer was hardened to package `src\Rook\bin\Release\net7.0` companion artifacts (`Rook.rhp`, `Rook.deps.json`, `Rook.runtimeconfig.json`, DLLs, `runtimes\*`), `RookNative.rhp` is now a required installer source, Rhino registry `FileName` writes match the known-good `...\PlugIn\FileName` script path, and release docs/skills were updated to the Windows/PowerShell net7 workflow. Release guard coverage now pins installer net7 packaging, no stale net48/bash release guidance, native-required packaging, registry subkeys, and runtimeconfig `runtimeOptions.tfm == net7.0`. `v1.5.2` was built with the pinned native MSVC/MFC toolset, the managed net7 companion, and Inno Setup, then published to GitHub with `Rook-Setup-1.5.2.exe`. **Promoted to Now:** #113 / #114 test-debt triage, because Kling and the release are shipped and those were the queued low-risk cleanup decisions. **Promoted to Next:** choose the next product or polish lane after #113/#114 are closed or explicitly deferred. **Still open after this release:** #113, #114, #53, #37, #34, #29, and stale #26.)

**Last triaged:** 2026-05-06 (**PR-19 Seedance source-frame transport shipped as PR #141.** PR #141 removes the Seedance data-URI source-frame path and always uploads start/end source frames through the provider-private fal CDN initiate + PUT flow before queue submit. Scope stayed managed-provider-private: no native route, MCP tool, public bridge op, ledger schema change, durable source media, or public fal source URL exposure. Source uploads use the confirmed fal REST contract (`rest.fal.ai/storage/upload/initiate?storage_type=fal-cdn-v3` -> raw PUT to fal media upload URL -> submit returned `file_url`) with `X-Fal-Object-Lifecycle` fixed at one hour; queue submit sends `X-Fal-Object-Lifecycle-Preference`, `X-Fal-Store-IO: 0`, and `X-Fal-No-Retry: 1`. Seedance durable handles remain request-id-only, upload failures happen before queue submit, transient upload HTTP failures and non-caller timeout cancellation retry safely, caller cancellation propagates, and queue submit retry remains limited to the PR-18 connection-establishment case. Privacy tests now pin raw JSONL, artifact metadata, provider/public errors, and handler payloads against `fal.media`, `rest.fal.ai`, `api.fal.ai`, `image_url`, `end_image_url`, and `data:image` leakage. **Verification before merge:** focused PR-19 suite passed `219/219`, full managed suite passed `1877/1877`, managed `net7.0` Release build/deploy passed with deployed `Rook.rhp` hash matching the worktree build, boundary scan found no source-upload exposure in `src/RookNative`, `mcp_server`, or `src/Rook\InternalBridge`, live non-generation fal upload smoke passed against `v3b.fal.media`, and user live Seedance smoke passed after local deploy. **Promoted to Next:** Kling 3.0 I2V remains the likely next curated video model now that fal source transport is hardened; keep its heavier schema (`multi_prompt`, negative prompt, cfg/reference/element/audio branches) behind a fresh spec/review pass.)

**Last triaged:** 2026-05-06 (**PR-18 Seedance 2.0 Image-to-Video shipped as PR #140.** Scope held to one curated fal video model, `bytedance/seedance-2.0/image-to-video`, with provider identity `fal`, managed Vision video lifecycle only, artifact-id source frames only, no dynamic discovery, no native/MCP/public HTTP expansion, and request-id-only durable fal handles for Seedance. The implementation hardened fal video as model-aware lifecycle work rather than a catalog-row-only addition: Seedance submit uses the full `/image-to-video` queue endpoint, while status/result/cancel reconstruction uses the parent `bytedance/seedance-2.0` lifecycle endpoint; active and restart lifecycle paths use explicit model identity and do not infer Seedance from handle shape. Durable records keep only provider + model + provider_job_id; reconstructed fal URLs remain transport-only and are stripped before persistence. Seedance failure paths strip provider detail so fal bodies that echo `image_url`, `end_image_url`, or data URIs cannot enter the JSONL ledger. Legacy URL-bearing fal jobs keep provider-name cancel cleanup through the intentional legacy path, preserving deprecated-model cleanup behavior. **Live Rhino smoke:** after deployment to `C:\Users\aryan\AppData\Roaming\McNeel\Rhinoceros\8.0\Plug-ins\RookNative`, a viewport/source-image + prompt round-tripped through fal Seedance 2.0 to a local Gallery artifact (`video.mp4`, ~2.7 MB) and the latest ledger/artifact manifest scan found no `data:image`, source-field, fal queue URL, `status_url`, `response_url`, or `cancel_url` leakage. **Smoke fixes captured in branch:** initial polling/result/cancel HTTP 405s were traced to using the submit endpoint as the lifecycle base; fixed by splitting Seedance submit vs lifecycle endpoints. A completed request then hit fal HTTP 422 because optional generated audio tripped provider content policy; PR-18 now submits `generate_audio=false`. A later submit transport failure on low-quality hotel Wi-Fi was reproduced outside Rhino as a .NET `HttpClient` socket connection-establishment timeout to `queue.fal.run:443`; PR-18 adds one narrowly scoped Seedance submit retry only for socket-level connection-establishment failures and deliberately does not retry generic transport failures that may have reached fal and could duplicate provider jobs. **Verification:** fal provider suite passed `61/61`, Seedance-filtered suite passed `35/35`, submit retry regression pair passed `2/2`, `git diff --check` passed, managed Release `net7.0` build passed with existing warnings, deployed `Rook.rhp` hash `B2BCCAA99396`, deployed `RookNative.rhp` unchanged at `CACC6804BEDC`. **Promoted to Next / PR-19:** robust Seedance source-frame transport. fal's current Seedance API lists start/end images as JPEG/PNG/WebP with max 30 MB, so Rook's current 1 MB raw-data-URI cap is the real workflow blocker for normal viewport captures; do not default to lossy downscale as the primary fix. PR-19 should verify the current official fal limits again, then replace the cap/workflow with a robust private source upload or equivalent transport that accepts provider-sized source images while preserving PR-18 privacy boundaries: no durable source media, no durable provider URLs/envelopes, no public/native/MCP exposure, fake HTTP/fake provider tests first. Also carry forward the slow-network lesson: PR-18's one safe connect retry is only a tactical mitigation; PR-19 should define a fuller submit robustness policy for large source uploads on slow/unstable networks without risking duplicate provider jobs. **Deferred after PR-19:** Kling 3.0 I2V remains the likely next curated model once fal source-video transport is hardened; its heavier schema (`multi_prompt`, negative prompt, cfg/reference/element/audio branches) stays explicitly deferred.)

**Last triaged:** 2026-05-05 (**PR-17 fal GPT Image 2 edit source-image workflow shipped via PR #139 and is treated as merged for the post-merge queue state.** PR #139 adds curated fal.ai `openai/gpt-image-2/edit` as a source-image-only async image job model for Generate and Studio. Scope held to one primary `input_image_path`, prompt required, no prompt-only GPT Image 2, no references/masks/provider options, bounded in-memory data URI transport, `image_size: "auto"`, `quality: "high"`, `output_format: "png"`, fal queue lifecycle via provider/model-aware lookup, request-id-only durable provider handle persistence, sanitized durable/bridge errors, and no native/MCP/public HTTP expansion. **Live Rhino smoke:** Generate tab viewport capture + prompt round-tripped successfully through fal GPT Image 2 edit; a post-smoke 405 on result fetch was traced to fal serving results from the bare request endpoint rather than `/response`; the provider/test/plan docs were corrected; Rhino restart verified the generated artifact still persists in Gallery. **Verification before merge:** full managed suite passed `1791/1791`, focused PR-17 suite passed `299/299`, `git diff --check origin/main...HEAD` passed, boundary scan found no native/MCP/internal bridge exposure, PR #139 was `MERGEABLE`/`CLEAN`, and GitHub reported no checks configured. **Promoted to Now:** PR-18 curated video model expansion for high-value video workflows, starting with the analogous provider-specific design for Kling 3.0 / Seedance 2.0-class models rather than continuing one-image-model-at-a-time catalog filling. **Still parked:** PR-14a mode UX remains conditional because live Generate/Studio use is clear enough for the current source-image workflows.)

**Last triaged:** 2026-05-04 (**PR-15 and PR-16 shipped; local `main` is synced to `origin/main` at `0eb5ad7 PR-16: Flux 2 Pro source-image workflow`.** PR #137 landed durable image job lifecycle state for managed bridge image jobs: append-only JSONL ledger, startup reconciliation to `Interrupted`, durable list/status/result/cancel-after-restart behavior, sanitized durable errors, fake-ledger isolation in tests, and no native/MCP/public HTTP exposure. PR #138 landed the first serious source-image Replicate workflow: curated `black-forest-labs/flux-2-pro`, async source-image-only Generate and Studio routing, one primary `input_image_path`, bounded data URI transport, MIME/size validation, zero-reference UI gating, schema fix for `resolution: "match_input_image"`, and leakage tests proving prompt/source/data URI/provider internals do not cross ledger/bridge/artifact boundaries. **Live Rhino smoke:** Flux 2 Pro round-tripped successfully with Replicate, generated artifact persisted after Rhino restart, and Schnell prompt-only async still works. **Verification before merge:** focused managed suites passed, diff checks passed, boundary scans found no native/MCP/internal registrar exposure, PR #138 was squash-merged, remote branch deleted, worktree removed, and PR-16 planning docs were removed before merge. **Promoted to Now:** PR-17 curated model expansion / catalog strategy decision for source-image-first workflows across Replicate and fal.ai. **Deferred:** PR-14a mode UX remains parked because the current Generate/Studio behavior was clear enough during live use.)

**Last triaged:** 2026-05-03 (**PR-14 Replicate Settings + visible image model shipped as PR #136 and local `main` is synced to the squash merge.** `origin/main` is at `1359fc8 [codex] Add Replicate Settings and visible image model`, the squash merge of the PR-14 design spec, implementation plan, and managed Vision implementation. Shipped scope: Replicate appears as a Settings credential owner with `replicate.api_token`; `black-forest-labs/flux-schnell` appears in the managed image catalog with backend-owned `submission_mode`; Generate routes prompt-only async T2I submissions through bridge-only image job ops; Studio remains source-image/edit oriented with T2I-only models incompatible/disabled; Replicate output is copied into local artifacts through the authenticated output request selector; and native/MCP/public HTTP/`NativeGhBridgeRegistrar` boundaries remain closed. **Verification:** PR #136 passed senior review with no requested changes, was squash-merged, remote branch deleted, local worktree removed, and local `main` matches `origin/main`. Local verification before merge included focused managed suites, full managed suite `1687/1687`, diff check, and boundary/leakage scans. **Promoted to Now:** PR-15 image job durability and restart recovery. **Conditional Next:** PR-14a mode UX is parked behind the trigger: promote if PR-14 manual smoke/live use shows Generate-only prompt T2I causes visible confusion or Studio mismatch friction.)

**Last triaged:** 2026-05-01 (**PR-11/PR-12 stacked Replicate async-image design + hidden image job substrate shipped as PR #134 and local `main` is synced to the squash merge.** `origin/main` is at `26ab841 Add hidden image job substrate`, the squash merge of the PR-11 design spec, PR-12 implementation plan, and hidden managed image-job substrate. Shipped scope: first-class managed-bridge-only image job ops; root-owned `ImageJobManager`; image job submit/status/cancel/result/list response types; fake-provider sync/async tests; authenticated materialization request-factory selection while keeping byte reads, size/MIME policy, and error classification in the materializer-owned path; local artifact availability checks before result fetch; cancellation races covering queued, polling, materializing, provider-cancel failure, and provider-already-terminal cases; and guard coverage proving no image job ops were exposed through native, MCP, or `NativeGhBridgeRegistrar`. **Guardrails preserved:** existing `generate` behavior remains unchanged; Replicate is still absent from default Settings, picker metadata, native routes, MCP, and credential-owner metadata; production Replicate authenticated fetch selection remains for the hidden Replicate provider slice. **Verification:** PR #134 passed review with no open findings, was squash-merged, remote branch deleted, execution worktree removed, stale local planning branch deleted, and local `main` matches `origin/main`. Managed suite passed 1611/1611 on the PR branch; `git diff --check origin/main...HEAD` was clean; hidden boundary scans found no image job ops in native/MCP/native-bridge surfaces. **Promoted to Now:** PR-13 hidden Replicate image provider registration tests. **Promoted to Next:** PR-14 Replicate Settings + visible image model.)

**Last triaged:** 2026-04-30 (**RookVision disappearing/blanking investigation reopened; diagnostics-only evidence pass, no behavioral fix.** Trigger: recurring report that RookVision panel content sometimes disappears/blanks, while Rook Chat appears unaffected despite both using `RookWebSurface`. User explicitly rejected another repaint workaround and asked for root-cause evidence first. **Skill/process:** followed `superpowers:systematic-debugging`; no fixes before evidence. **Repo context inspected:** this doc; PR #132 code/docs/history; `RookWebSurface.cs`; `VisionTab.cs`; `VisionWebSurface.cs`; Vision `app.js` + `styles.css`; Chat equivalents (`ChatTab`, `AgentChatTab`, `ClaudeCodeTab`, shared setup); PR #103/#132 history. **Key lifecycle difference found:** Chat/KG/Claude share the substrate but do not run the Vision host-activation recovery path; Vision alone participates in `RookChatPanel.PanelShown(...) -> VisionTab.RecoverAfterHostActivation(...) -> RookWebSurface.ReloadAfterHostActivation(...)`, which issues a WebView reload on `Show` / `ShowOnDeactivate`. This is the current leading hypothesis area, not proven root cause. **Important constraint preserved:** `ROOK_ENABLE_WEBVIEW_REPAINT_WORKAROUND` stayed unset; no unconditional `CoreWebView2Controller.IsVisible` toggles were reintroduced. **What was tried live:** user closed/reopened Rhino, opened/closed RookVision, switched focus/unfocus, switched between RookVision/Rook Chat/KG, and hammered panel visibility/focus transitions. The bug did NOT reproduce in this session. **Initial pre-patch diagnostics:** `%TEMP%\rook\webview-focus.log` showed normal Chat and Vision WebView setup, bridge up, `document-loaded`, and Vision-only host-activation reloads (`request-repaint detail=Show`, `request-repaint-skip workaround-disabled`, `pre-reload:Show`, `post-reload:Show`) with no `process-failed`, setup/configure failure, or bridge failure. This narrowed suspicion toward the Vision-only activation reload path, but did not prove it. **Evidence-only instrumentation added:** `RookWebSurface` diagnostics now log WebView2 navigation start/completion + HTTP status, explicit `host-activation-reload`, missing embedded resources, virtual resource failures, JS errors, unhandled promise rejections, resource load errors, and document/layout snapshots (`viewport`, body/document sizes, `appRect`, active element). Added source-level regression pin `WebViewFocusDiagnostics_CapturesNavigationResourcesJsAndLayout`. Built and deployed companion `net7.0` Release; deployed `Rook.rhp` SHA-256 matched build output; `scripts\register-rooknative-suite.ps1` reverified native + companion registration. Persisted `ROOK_ENABLE_WEBVIEW_FOCUS_DIAGNOSTICS=1` at User scope; `ROOK_ENABLE_WEBVIEW_REPAINT_WORKAROUND` remained unset at User and Machine scope. **Verification:** focused `RookWebSurfaceTests` passed 30/30; `dotnet build src\Rook\Rook.csproj -f net7.0 -c Release` succeeded; full managed suite `dotnet test src\Rook.Tests\Rook.Tests.csproj` passed 1560/1560. **Fresh post-patch Rhino log while user could not reproduce:** Vision navigations completed successfully (`success=True;status=200`), latest Vision heartbeat showed `ready=True`, `bridge=True`, `controller.isVisible=True`, `webViewVisible=true`, and nonzero layout (`appRect=0,0,1614x1104`). Counts in the active session: `process-failed=0`, `configure-failed=0`, `setup-init-failed=0`, `setup-failed=0`, failed navigation count 0, `js-js-error=0`, `js-js-unhandledrejection=0`, `resource-virtual-error=0`, `resource-resolver-failed=0`, `js-probe-parse-failed=0`. **Observed noise:** Vision emits repeated `js-resource-error` for empty `<img src="">` placeholders resolving as `https://app.rook.invalid/index.html`; Chat emits one missing `/favicon.ico`. These do not currently correlate with blanking and should not be treated as root cause without a failing timestamp. **Current deduction:** focus/unfocus and close/reopen alone are not sufficient to reproduce the bug in the current deployed build. The failure is not presently evidenced as a renderer crash, navigation failure, JS exception, bridge outage, or CSS/layout collapse. The main unresolved question remains whether the Vision-only `PanelShown` reload occasionally races Eto tab visibility/controller state, causing a blank presentation state that a later reload/reopen masks. **Next capture instruction:** if the panel blanks again, stop interacting immediately and record the exact wall-clock time before closing/reopening/switching tabs; then inspect `%TEMP%\rook\webview-focus.log` around that timestamp. The desired classification is: blank WebView with live DOM/layout, navigation/resource failure, JS runtime failure, CSS/layout collapse, bridge hang, or WebView2 process/render failure. **Do not** convert this entry into a behavior change until a failing log timestamp exists.)

**Last triaged:** 2026-04-30 (**PR-10 Replicate substrate shipped as PR #133 and local `main` is synced to the squash merge.** `origin/main` is at `da06d56 PR-10 Replicate substrate`, the squash merge of the PR-10 design/spec, implementation plan, and managed-only Replicate substrate. PR #133 proves the second-aggregator contract without exposing Replicate in Settings, picker metadata, native routes, or default model registrations. Shipped scope: `ReplicateApiClient` with Bearer auth and API-host restriction to `api.replicate.com`; official-model-only `ReplicatePredictionEndpoint` with owner/name segment validation; create/get/cancel prediction calls; lifecycle mapping for `starting` -> Pending, `processing` -> Running, `succeeded` -> complete with updated handle, and distinct failed/canceled outcomes; error and retry classification including non-retryable `data_removed: true` retention expiry as `DependencyUnavailable`; approximate pricing helpers over provider timing metadata; output-cardinality preservation in provider metadata while omitting logs by default; authenticated output-fetch proof seam for exact `replicate.delivery` or suffix `.replicate.delivery` hosts; and credential-boundary tests proving Replicate remains absent from `VisionProviderRegistrations.CreateCredentialMetadata()`. **Guardrail preserved:** `GenerationSecretKeys.ReplicateApiToken` is a known secret key for substrate tests, but Replicate is not a credential-owner provider in the default metadata catalog; default `set_provider_secret` behavior rejects `provider_name: "replicate"` because metadata does not expose it. **Verification:** managed suite passed at 1559/1559 on the PR branch, `git diff main...HEAD --check` was clean, boundary scans found no Replicate exposure in UI/native/internal bridge/default registrations, PR #133 was marked ready, reviewed with no blocking findings, squash-merged, remote branch deleted, local worktree removed, and local duplicate pre-squash commits were replaced by the merge state. **Local post-PR-10 build/deploy closeout:** after Rhino was closed, `dotnet test src\Rook.Tests\Rook.Tests.csproj` passed 1559/1559; native Release built and deployed with MSVC `14.44.35207`; companion `net7.0` Release built and deployed; `scripts\register-rooknative-suite.ps1` verified native + companion registration and rewrote `RookChatService.json`; deployed `RookNative.rhp`, `Rook.rhp`, `Rook.deps.json`, and `Rook.runtimeconfig.json` hashes match the current Release outputs. **PR-9 non-spend Rhino UI smoke observed:** Settings renders Gemini + fal.ai credential cards; Gemini test shows passed; fal.ai test correctly reports that the credential cannot be proven without generation work; fal.ai models appear in the dropdown surfaces; and `fal-ai/wan/v2.7/text-to-video` is available for video generation. No live provider generation/spend calls were run. **Carried-forward pre-PR-10 context:** PR #130 shipped fal video provider support; PR #131 shipped provider Settings/picker UX with provider-aware canonical bridge ops; PR #132 gated the risky WebView repaint workaround behind `ROOK_ENABLE_WEBVIEW_REPAINT_WORKAROUND=1` and left diagnostics behind `ROOK_ENABLE_WEBVIEW_FOCUS_DIAGNOSTICS=1`. Future agents must not reintroduce unconditional `CoreWebView2Controller.IsVisible` toggles in Chat/Vision/KG/Claude panels. **Promoted to Now:** post-PR-10 next-slice decision checkpoint. **Promoted to Next:** Replicate async image/materialization design decision unless user direction pivots to a different small provider-framework slice.)

**Last triaged:** 2026-04-26 (**PR-V3 manual smoke + smoke-unblockers — VisionTab UI for video shipped on `fix/rook-vision-v3-ui` (PR #115).** 968/968 tests passing including 55 new V3 tests; net7.0 `Rook.rhp` deployed and live-verified. **Manual-smoke run surfaced two real bugs that got fixed in-line and bundled into the same PR:** (1) **WebView focus-blackout** — the long-running substrate bug where Vision/Chat/KG panels go solid-dark when the host loses focus or the user switches Eto tabs, deferred 3+ times across 2026-04-23/24/26 triages. Root cause: `RookWebSurface` never wired focus events; the WebView2 swap chain landed in a state that kept rendering the `DefaultBackgroundColor` instead of re-presenting. Fix: `_webView.GotFocus`/`Shown` handlers call into a new `TryForceWebViewRepaint` that pulls `CoreWebView2Controller` via reflection, toggles `IsVisible` false→true, and calls `NotifyParentWindowPositionChanged` — both are documented WebView2 workarounds. **Memory entry created** at `project_webview_focus_blackout.md` so future Vision/Chat/KG/Claude-panel work doesn't re-derive this; indexed in `MEMORY.md`. (2) **Frame-slot mode-gating** — the `hidden` HTML attribute was being overridden by CSS specificity (`.video-frame-slot { display: flex }` beats UA `[hidden] { display: none }`), so T2V mode left the start/end frame slots visually present and clickable. User picked a start frame in T2V and hit a provider rejection. Fix: switched to a `.disabled` class with `opacity: 0.4 + pointer-events: none + user-select: none`, `:after` pseudo-element appends "— not used in this mode" to the slot label, all picker buttons explicitly disabled, defensive guard `isSlotAllowedNow(slot)` rejects programmatic picker opens for disallowed slots. The codebase already had a `.hidden { display: none !important }` class at `styles.css:1203` — V3 used the wrong pattern; lesson captured. **PR shape:** V3 main commit + 2 smoke-unblocker commits on top, will squash on merge. **Surfaced follow-up:** **video thumbnail extraction** for Gallery — currently relies on poster blobs that aren't being written. Tiered scope (Tier 1 ~30 min: copy start_frame bytes as poster on I2V/Interp at "Saving" stage; Tier 2 ~1–2 h: JS-side first-frame canvas extraction for T2V; Tier 3 ~half day: Windows Media Foundation server-side extraction). See `Next` deferred candidates for full breakdown. Recommended: Tier 1 as fast-follow with V3 or V4. **Promoted to Now:** PR #115 ready to merge. **Promoted to Next:** PR-V4 unchanged (MCP tools for video). **Substrate-observation hold window** continues passively through 2026-04-28; V3 close-out did not consume hold capacity (architectural-feature lane).)

**Last triaged:** 2026-04-25 (**Video track V1+V2 closeout — full transport stack now on `main`.** Six RookVision PRs shipped 2026-04-25 closing V1 polish (#106 per-image Show in Folder; #107 vendored Archivo + JetBrains Mono `.woff2`) and the entire video transport layer (V1a/V1b/V1c/V2 → #108/#109/#110/#111). PR #111 (`555e196`) lands the five `/vision/video/*` native HTTP routes + `RookSubsystemRoot` composition + `VideoOpHandler` + `VisionWebSurface` two-handler injection + `RookPlugin` reconcile/dispose lifecycle, all going through one shared `VideoJobManager` per `RookSubsystemRoot.Video` lazy. ABI v14 unchanged; V1c JSONL byte-identity fixtures still green; 913/913 managed tests passing. **Rhino-runtime smoke verified** all 5 routes via curl against the deployed companion — including the C++ `body["job_id"]` injection traversal that Codex flagged as the native unit-test gap. **Deploy gotcha caught + captured to memory:** `Rook.rhp` MUST be the `net7.0` build, not `net48` — `RookWebSurface`'s entire WebView2 init is gated under `#if NET7_0_OR_GREATER`; the net48 build silently falls back to `MinimalFallbackHtml` with NO diagnostic log, breaking both Vision AND Knowledge Graph panels. Captured in `reference_deploy_paths.md`. **Three follow-up GitHub issues filed:** (1) Codex hardening — remove unsafe net48 fallback paths in `install.ps1` + `scripts/register-companion.ps1`; (2) native helper test gap for `DispatchVisionOpWithPathId` (closed indirectly by runtime smoke); (3) plugin lifecycle integration test for `RookPlugin.OnLoad`/`OnShutdown` try/catch wrappers (static review only today). **Promoted to Now:** PR-V3 — VisionTab UI for video, consuming the bridge ops V2 already wired into `VisionWebSurface.OpRoutes`. Same Pattern A bridge image side uses; same `VideoJobManager` instance native HTTP routes hit. PR-V4 (MCP tools `rhino_render_video` etc. + tool-catalog wiring) queued as Next. **Substrate-observation hold window** (through 2026-04-28) continues passively in parallel as before — V3 is architectural-feature lane, doesn't consume hold capacity.)

**Last triaged:** 2026-04-25 (**Promoted to Now: per-image `Show in Folder` affordance in Gallery modal.** Surfaced as item (2) of yesterday's RookVision fix-it triage; promoted today per user direction to maintain RookVision UI momentum. **No prerequisites unlock needed** — infrastructure is fully shipped via PR #99 + #102: (a) `VisionHandler.BuildOpenFolderStartInfo` static helper at [`VisionHandler.cs:1409`](src/Rook/Handlers/VisionHandler.cs#L1409) — same `UseShellExecute = true` shell-out pattern that backs the global `Open artifacts folder` button (the per-file variant uses `explorer.exe /select,<path>` to highlight a specific blob, but the security profile and helper shape are identical); (b) op-allowlist plumbing pattern proven across 14 ops at [`VisionWebSurface.cs:146`](src/Rook/UI/Vision/VisionWebSurface.cs#L146) — new op (e.g., `reveal_artifact`) adds one entry to the route map + one rejection case in each of the three dispatcher guards (`DispatchUi`, `DispatchOffUi`, `DispatchAsync`); (c) path canonicalization + traversal rejection inherited from `ArtifactStore.GetBlobAbsolutePath(id, role)`; (d) `RequireArtifactId` Guid-from-args helper at [`VisionHandler.cs:1426`](src/Rook/Handlers/VisionHandler.cs#L1426). **PR shape:** new `RevealArtifact` handler method (off-UI, threadpool-routed like its global sibling) + op-allowlist entry + dispatcher rejection cases + one button between Approve and Delete in `index.html` modal + one JS handler calling `bridgeCall("reveal_artifact", { artifact_id })` + tests for path resolution / `/select,` invocation / op-allowlist regression (14 ops × dispatcher → 15 ops × dispatcher). Estimated similar size to PR #102's installer fix bundle: ~150-300 LOC across ~6 files. **Lane separation preserved:** substrate-observation accumulation continues passively through 2026-04-28; this is architectural-feature lane, does not consume hold capacity. Same justification pattern as image-track closeout (per 2026-04-23 entry) and Vision fix-it session (per 2026-04-24 entry).)

**Last triaged:** 2026-04-24 (**RookVision fix-it session ready for PR — UI polish + viewport/artifact hygiene.** Local branch `feat/vision-generate-layout-polish` now contains a focused Vision cleanup pass after the image-track closeout. **Shipped in this session:** (a) **Vision UI styling pass** — CSS-only brutalist/drafting refinement, CSP-safe font fallback after removing Google Font `@import`, cyan contrast split (`--accent-cyan` vs `--accent-cyan-bright`), mobile/docked-WebView nav breakpoints, reduced ornament density, grain overlay disabled because it modified evaluated image pixels, dark grey source-preview letterbox background, Approve tooltip/ARIA explanation. (b) **Viewport framing correctness** — viewport selector now keys live views by `RuntimeSerialNumber`, sends `view_id` for open viewports and `view_name` for named views, and stops sending stale UI width/height; `ViewportHandler` resolves exact `RhinoView` and captures the selected view's current `ActiveViewport.Size` when dimensions are not explicit. Fixes the full-framing/aspect issue without resizing the UI container. (c) **Settings artifact breakdown** — Settings now reports `Generated images`, `Viewport captures`, `Prompt enhancements`, `Depth maps`, and `Total artifacts` instead of one opaque "Artifacts stored" count. (d) **Gallery/Explorer affordance cleanup** — Gallery gets an `Open artifacts folder` button that opens `%APPDATA%\Rook\artifacts`; default WebView2 image context menu is blocked so misleading Edge options like `Share` no longer surface. (e) **Artifact-spam fix** — preview source capture now uses new UI-thread `preview_viewport` op returning a temporary `/viewport-preview/{file}` URL from `%TEMP%\rook\viewports`; normal viewport browsing no longer creates durable `captured_viewport` artifacts. Existing `capture_viewport` remains as the explicit durable artifact path. **Store cleanup performed manually:** purged 114 unapproved artifact directories (`captured_viewport=97`, `depth_map=2`, `enhanced_prompt=14`, `generated_image=1`), preserving 8 approved generated images. **Validation:** Vision web/handler slice green at 190 tests; `dotnet build src/Rook/Rook.csproj -f net7.0` succeeded with existing warnings; Rhino plugin copy verified at `%APPDATA%\McNeel\Rhinoceros\8.0\Plug-ins\RookNative\Rook.rhp` timestamp `2026-04-24 22:32:16`, size `2295296`. **Surfaced follow-ups:** (1) add a first-class UI command to purge unapproved artifacts with dry-run count + confirmation; (2) add per-image `Show in Folder` / reveal-selected-file affordance in Gallery modal; (3) decide whether viewport source provenance should be promoted only at Generate time as a parent artifact, or left temp-only; (4) add temp-preview retention cleanup for `%TEMP%\rook\viewports`; (5) vendor Archivo + JetBrains Mono `.woff2` and uncomment the local `@font-face` block; (6) configurable Gallery/artifact root remains a broader architecture item, not solved by the current global-folder button. **Still outstanding from earlier queue:** focus-flash on panel blur; configurable Gallery folder; native-build artifact strategy / CI ABI check; broader multi-viewport capture behavior beyond exact selected-view sizing.)

**Last triaged:** 2026-04-24 (**Vision framing + WebView lifecycle polish shipped as fast-follow #102** [`84ce646`], extending yesterday's image-track closeout. Merged 2026-04-24T06:59:55Z, 16 files / +658/-61, Codex-authored with user review + local Rhino smoke test. **Three distinct areas:** (a) **Vision framing polish** — `aspect_ratio` dropdown expands from 5 to 15 Gemini-supported options + "Auto" default (internally resolves to null → omit from API call, letting Gemini match source aspect); new `NormalizeAspectRatio(raw)` helper (`"auto"/"current"` → null pass-through; unknown values rejected against `AllowedAspectRatios` list of 15); resolution dropdown gains `"512"` option gated to nano-banana-2 only via new model-aware `ValidateResolution(resolution, model)` overload + `SupportedResolutionsForModel(model)` resolver; `AllowedResolutions` now includes `"512"` while new `CommonResolutions` stays `{1K, 2K, 4K}` for the baseline UI; `get_settings_overview` now returns `allowed_aspect_ratios` + `default_model_supported_resolutions` + `supported_resolutions_by_model` so the UI can gate per-model dynamically; model catalog entries gain `supported_resolutions` arrays; `BuildImageConfig(resolution, aspectRatio)` extracted so aspect handling has one source of truth. (b) **WebView lifecycle cleanup** — `RookWebSurface` now implements `IDisposable`; added `_disposed` flag + `IsDisposed` property + disposal guards at every async await boundary (`ConfigureVirtualHost`, `OnDocumentLoaded`, `OnWebMessageReceived`, each `AddScriptToExecuteOnDocumentCreatedAsync` step); init-event handler removal tracked via `_nativeControlWithInitHandler` / `_initEvent` / `_initHandler` and cleared on dispose; `CreateWebContent` throws `ObjectDisposedException` on reuse after dispose; `ExecuteScript` early-returns on disposed state. Chat family gains a `CloseWebSurface()` method on `ChatTab` (idempotent via `_tabClosed` guard) + `Dispose(bool)` override that routes to `OnTabClosed()`; `AgentChatTab` and `ClaudeCodeTab` override `OnTabClosed` to call `CloseWebSurface()` before their own teardown; `KnowledgeGraphPanel` disposes its surface on `PanelClosing`; `VisionTab` gets a `Dispose(bool)` override + sets `Content = null` before `_surface.Dispose()` so Eto releases the host. `ChatServiceManager` shutdown hardened for cleaner Rhino exit. Background `Rhino.exe` now exits immediately after close per smoke test. (c) **Installer target-selection fix** — `install.ps1::Step-BuildCompanion` was preferring `net48` over `net7.0` when both output dirs existed (`bin/Release/net48/Rook.rhp` AND `bin/Release/net7.0/Rook.rhp`); Rhino 8 requires `net7.0`, so clean-user installs were silently deploying the wrong companion. Fix swaps priority order in both the "already built" and "newly built" branches. **Addresses one outstanding yesterday-listed follow-up.** Local deploy verified: installed `Rook.rhp` hash matches `src/Rook/bin/Release/net7.0/Rook.rhp`. **Tests: 437 passed** (+36 from PR #101's 401 baseline: +119 VisionHandlerTests for aspect normalization + resolution model-gating + helper coverage; +85 VisionWebSurfaceTests for IDisposable contract + disposal guards; +22 RookWebSurfaceTests for substrate disposal pinning). **Consumer-level Eto disposal tests deliberately unpinned** per Codex post-review note — headless Eto test harness unavailable, covered by live Rhino smoke instead. **Follow-up items status:** (a) focus-flash on panel blur — NOT addressed; the lifecycle work targets shutdown, not focus-transition rendering; still outstanding. (b) configurable Gallery folder — not touched. (c) multi-viewport capture — not touched. (d) native-build artifact strategy / CI ABI check — installer fix handles WRONG-TARGET selection but NOT ABI drift between companion + native; still outstanding.

Main is now at `84ce646`. Re-triage posture unchanged — substrate-observation hold window still active through 2026-04-28.)

**Last triaged:** 2026-04-23 (**RookVision image track CLOSED — huge milestone.** Three PRs merged today closing the 11-PR SA_Banana → RookVision image-track arc (#90-#101): **#99** VisionTab UI, Pattern A core closeout (`e5e183c`); **#100** Codex-diagnosed `RookPlugin` startup retry-storm fix (`73a900f`) — discovered during PR-7b integration smoke, orthogonal to Vision; **#101** v1 polish: "Active view (live)" sentinel, API key preview persistence, same-view no-reset (`4b51932`). **Live-verified end-to-end roundtrip:** `/ShowRookVision` opens the panel inside Rook Chat's tab strip → save API key (persists across restarts with `AIza…xxxx` preview placeholder proving persistence) → Generate captures live viewport with orbit preserved → Gemini call → artifact lands in `%APPDATA%\Rook\artifacts\{YYYY-MM-DD}\{uuid}\` → thumbnail renders via `/blob/{id}/{role}` virtual-host URL in Gallery → Approve/Delete works. **Architectural landmarks from the arc:** Pattern A (in-process JS↔C# bridge) substrate proven — `VisionWebSurface` extends `RookWebSurface` with explicit op-allowlist (14 ops × correct dispatcher, no default-to-offUi fallback), `connect-src 'none'` CSP override, `/blob/{artifact_id}/{role}` virtual resource resolver, 180s async-op timeout matching native `vision_dispatch` trampoline. **UI-only op containment rule load-bearing:** 6 new ops (`set_api_key`, `test_api_key`, `get_settings_overview`, `capture_viewport`, `list_views`, `open_image_picker`) are JS-bridge-only — native `HandleVisionDispatch` allowlist rejects them by default. **Short-name→full-Gemini-ID mapping** (`GeminiClient.Models.ResolveShortName`) decouples UI dropdown labels from Google's preview→GA renames; catalog is paid-only (free-tier deliberately absent — API keys can't invoke free-tier endpoints, would only generate 429s). **DPAPI-wrapped API key storage** (`VisionSecretStore`, CurrentUser scope, purpose-binding entropy) with plaintext-safe `ApiKeyPreview` for UI display — no decrypt on every settings read; `first4…last4` preview stores only the publicly-known "AIza" prefix + 4 trailing chars of entropy. **Day-bucketed artifact store** at `%APPDATA%\Rook\artifacts\{YYYY-MM-DD}\{uuid}\` with atomic creates via `.tmp` staging + `Directory.Move`, manifest `id` === directory name identity rule, neutral `{kind, files[], parent_ids, metadata, flags}` schema. 401/401 unit tests green; zero regressions. Full details in `project_rookvision_image_track.md` memory + `rook_docs/2026-04-22-rookvision-image-track-checkpoint.md`.

**Integration-smoke discovery captured (save for future ABI-bump rollouts):** during PR-7b live deploy, user's deployed `RookNative.rhp` (built 04:51 Apr 23) predated the ABI v14 bump from PR #95's `VisionDispatch` callback; companion expected v14 struct shape; registration failed; pre-#100 companion's periodic retry-timer + re-entry saturated Rhino's UI thread; Rhino froze at startup and had to be force-quit. Recovery: moved `Rook.rhp` aside to regain usable Rhino, rebuilt native from current source, redeployed matched pair. #100 now makes the fallthrough graceful regardless of ABI state (one-shot timer + `Interlocked.Exchange` in-flight guard); a CI matched-binary check for clean-user installs remains as a follow-up.

**Follow-ups captured (not scoped as PRs yet):** (a) focus-flash on panel blur — affects Chat tab too, likely wants substrate-level fix in `RookWebSurface`; (b) configurable Gallery folder — plumbing 80% present via existing `ArtifactStore(overrideRoot)` ctor; per-project (alongside `.3dm`) vs global-override architectural question worth settling before building; estimated ~400 LOC when scoped; (c) multi-viewport capture — `ViewportHandler.CaptureTier3` currently mutates `ActiveView`'s projection rather than capturing a specific named non-active pane in quad-viewport layouts; fix = resolve view by enumerating `doc.Views` before snapshot; (d) native-build artifact strategy for clean installs — current deploy pipeline makes ABI drift silent, should emit matched pairs or add CI check. **All video work remains explicitly deferred** per the locked 2026-04-22 checkpoint framing (v3 addendum questions for Codex drafted but send-status not confirmed — verify on resume).

**Re-triage posture unchanged:** substrate-observation hold window per 2026-04-21 remains active; `substrate_observations.jsonl` accumulating since PR #88 merge. Today's image-track closeout did not consume or displace hold capacity — different lane (architectural feature work vs. data-driven typed-route campaign selection). Now/Next remains deliberate hold until on/after 2026-04-28.)

**Last triaged:** 2026-04-22 (**Contextual capability understanding** captured as a **design-track architecture item**, not a start-now implementation. New design doc: `rook_docs/2026-04-22-contextual-capability-understanding-design.md`. Core frame: build around a semantically thick middle that understands context and capability adjacency, with UI surfaces treated as thin consumers. Key conclusion from the 2026-04-22 design pass: do **not** center the effort on route graphs, route lists, or any single host surface; center it on a durable substrate that can serve humans-in-the-loop now and agent-first workflows later. The first eventual implementation slice is an **adapter-based, replayable event-ingestion layer** that normalizes host conversation logs + native/GH session-recorder streams into canonical `NormalizedEvent`s, with explicit install detection, per-source consent, local-only raw storage by default, and full reprocessing/versioning support. **Queue decision:** parked as architecture/design until we are ready to choose the first implementation slice; this does **not** displace the current substrate-observation hold window or the 2026-04-28 hotspot-driven re-triage for typed-route campaign selection.)

**Last triaged:** 2026-04-21 (Substrate-observation persistence shipped — PR #88, merged `fb970a7`. 5 files, +505 / −8, single squashed commit after two Codex review rounds. **Unblocks data-driven selection of future typed-route campaigns** by persisting `SubstrateObservation` records to `knowledge/substrate_observations.jsonl` across sessions; prior behavior was session-scoped summary only. Schema: one JSONL line per routed tool call with `{timestamp, session_id, tool, operation, route_taken, success, verified, error}`; best-effort write, per-write flush, never raises. Four capture sites: `BaseAgent._record_observation()`, `chat_runner` (uses `conversation.id` as session_id), `server.py::_record_observation()` (MCP-direct path, single top-of-function `_is_handoff` gate covers both metrics + substrate). Substrate persistence fully decoupled from metrics path — survives `observation_recording=False`, missing metrics infra, and metrics-store exceptions. **This PR was a precursor** (not a campaign) — spawned from the **Theme A gate verdict: PARKED** as a deliberate non-project because `rhino_create` shows 1636 calls / 99.69% success / 0 corrections over 12 days with zero discoverability pain signal; splitting into 13 per-primitive MCP tools would be pure external repackaging. Per-primitive dispatch already exists internally via `intent_runtime.py:233` + `intent_planner.py:238`. Full gate results: tool-level metrics show no primitive struggle, `knowledge/gh/component_observations.json` has 0 `correction_detected: true` across 516 observations, no user asks for per-primitive tools. 16 new tests (20/20 substrate_analytics green, 45/45 intent_runtime green, 1382 passing in full non-live suite vs 1366 baseline = +16, zero regressions). Two Codex review rounds caught: (1) substrate writes gated on metrics availability in both agent seam + MCP seam — fixed by hoisting persistence above early returns / into sibling try block; (2) misleading "re-applied `_is_handoff` skip" comment claimed substrate-level guard that was actually dead code because upstream early return already exited function — fixed by hoisting handoff check to explicit top-of-function gate that covers both telemetry paths.

**Next Now: deliberate hold.** Per user direction 2026-04-21: let `substrate_observations.jsonl` accumulate for ~1 week, then pick the first substrate-promotion campaign PR from actual hotspot data rather than speculating. During the hold window, the Now item is intentionally empty; the substrate data is the gating input for the next campaign. Do not start a substrate-promotion campaign until the log has meaningful hotspot signal.)

**Previously triaged:** 2026-04-21 (**GH script-component-routing campaign DONE**. Status: closed cleanly; live validation confirms both-language behavioral closure.

**5 PRs shipped** (all merged 2026-04-21):
- **#83** (`e75cb24`) — Contract hygiene across 7 agent-facing surfaces
- **#84** (`105bfa8`) — Unified `gh_create_script(language=...)` tool with back-compat aliases
- **#85** (`515763a`) — `gh_execute_intent` handoff enforcement on script-component intents
- **#86** (`1ff213b`) — Modern RhinoCode C# Script resolver visibility + preference
- **#87** (`1c4a49a`) — Python 3 modern-over-IronPython 2 legacy resolver preference

**Live validation** (post-#87, MCP restart, fresh canvas): 7/7 scenarios pass.
- Generic Python intent → handoff (modern PY3_GUID, `language=python`)
- Generic C# intent → handoff (modern CS3_GUID, `language=csharp`) — was the primary validation gap after #85 that #86 closed
- "python 3 script" explicit intent → handoff with modern Python 3 (confirms #87 ranking flip)
- Legacy C# exact name → legacy created normally, no handoff (per memo §Tagging: no `creationTool`)
- IronPython 2 exact name → legacy created normally, no handoff
- Sphere (non-script) → normal creation, no handoff (narrow-match invariant)
- Ambiguous legacy wording (e.g. "legacy dotnet C# script") resolves to modern + handoff rather than silent misrouting — safe failure mode; intentionally not filed as a bug

**Reference material:**
- Campaign memory: `~/.claude/.../memory/project_gh_script_routing_campaign.md` (architectural decisions + trigger-gated deferrals)
- Design memo: `rook_docs/2026-04-21-gh-script-component-routing-design-pass.md`
- Scope-pass checklist addition landed in `feedback_pr_scoping_rhythm.md` memory (handler-acceptance-set change → audit four surfaces rule)

**Deferrals** (both explicitly in Parked table below with triggers): GhPython component-note + creation-identity story; end-to-end agent-behavior harness.

Pending re-triage for the next Now/Next items.)

**Previously triaged:** 2026-04-21 (GH script-component routing PR-1 shipped — contract hygiene across 7 agent-facing surfaces. Merged as `e75cb24` (PR #83). 7 files, +186 / −26, single commit, single-round Codex review with clean sign-off. **Zero handler-side changes** — `git diff --name-only | grep -E '\.(cs|cpp|h)$'` returned 0 files, scope containment gate held exactly. Closes the 2026-04-21 live agent-failure incident per `rook_docs/2026-04-21-gh-script-component-routing-design-pass.md` §PR-1. Changes: `gh_set_script` + `gh_execute_intent` MCP descriptions rewritten (Py3-only drift removed; `gh_execute_intent` stays current-state only — no PR-3 forward-anchoring); `personas/worker/role.md` + `personas/architect/role.md` + `personas/scripter/role.md` rewritten (scripter role fully rewritten with Language Decision / Forbidden Paths / C# branch; architect role preserved its existing correct "Do NOT use rhino_execute for GH" rule); `prompts/WORKER.md` display snapshot mirrored for consistency. **Test caught a hidden drift site during implementation** — `worker/role.md:83` wasn't in pre-PR grep inventory; the parameterized persona-prompt regression test surfaced it on first run, validating Codex's correction to add prompt-level testing. 5/5 new regression tests + 45/45 intent_runtime + 100 pre-existing = all green. Next: PR-2 (unified `gh_create_script(language=...)` with existing tools aliased; follows the `/curve/boolean` Phase 2 discriminator-param precedent). Campaign 1/3 complete.)

**Previously triaged:** 2026-04-21 (**Phase 3 closed with 1 shipped PR after PR-2 killed by implementation-time API audit.** PR-2 (`AlignWithSurface` on `/surface/sweep1`) was ready for scope-pass-to-plan-doc when a pre-code RhinoCommon reflection audit revealed: `SweepOneRail` class exposes only Freeform + Roadlike modes (SetToRoadlike{Front,Right,Top} + SetRoadlikeUpDirection); NO `SetStyleAlignWithSurface` method, NO IsAlignWithSurface property; `Brep.CreateFromSweep*` 11 static overloads take no Surface/BrepFace argument; `SweepFrame` enum has 2 values; **assembly-wide reflection scan for `AlignWithSurface`/`AlignWith`/`StyleAlign` returned 0 matches across all of RhinoCommon**. Conclusion: Rhino's `_Sweep1` command UI exposes AlignWithSurface but the underlying opennurbs implementation is command-internal — never got a public managed wrapper. The spike memo's premise (`invoke SetStyleAlignWithSurface(surface)`) was unimplementable via the managed-bridge-reuse substrate. Decision per 2026-04-21 re-triage: killed (not pivoted to native C++ / opennurbs — that would abandon the Phase 1-3 substrate rhythm for different-shape campaign; not pivoted to scripted `_-Sweep1` fallback — violates Decision Record Rule 3). Re-parked with strict trigger set (RhinoCommon adds wrapper / new native-opennurbs design pass opened / real user workflow justifies native route cost). **Methodology lesson captured in spike memo §Post-Hoc Finding:** API-surface-reflection audits should run at spike time, not implementation time — mid-campaign API surprises are exactly what spike rhythm prevents, and "deferred to implementation time" let this one through. Phase 3 effectively closed 2026-04-21 with PR-1 shipped (`0a72a0f`, PR #82), PR-2 and `rails_disconnected` both re-parked with explicit triggers. Spike memo `rook_docs/2026-04-21-typed-route-phase3-spike.md` updated with §Post-Hoc Finding + revised TL;DR + revised §PR Sequence + revised §Campaign Exit Criterion (all 3 deferrals now either shipped or explicitly trigger-gated). Promoted to Now: `gh_set_script` MCP description drift investigation — a structural analysis task, not a minimal fix. Not "change the description text"; the incident surfaced a broader question about how MCP tool descriptions interact with agent probing behavior. User explicit direction: look carefully at tooling / MCP language / routes to fix structurally, not superficially.)

**Previously triaged:** 2026-04-21 (Phase 3 PR-1 shipped — `curve_intersects_axis` detection on `/surface/revolve`. Merged as `0a72a0f` (PR #82). 3 files, +123 / −24, single commit, single-round Codex review with clean sign-off. **Closes the first Phase 1 `DEFERRED PLAN-CODE`**. Empirically validated that `RevSurface.Create` does NOT classify curve-crosses-axis as factory failure — the factory accepts the input and produces invalid-but-successful geometry with `success: true` (Probe 1 Case 1.2 on 2026-04-21). New check is the **only diagnostic signal** the caller receives, not a fallthrough replacement as originally framed. Detection rule: `Intersection.CurveLine(curve, axis, tol, tol)` with `tol = doc.ModelAbsoluteTolerance * 10`; event classification via `ParameterA` vs `curve.Domain.{Min,Max}` using `RhinoMath.ZeroTolerance` (parameter-space precision, not intersection tolerance — Codex pre-implementation correction prevented near-endpoint dead zones). Tolerance choice validated via 6-case characterization probe (`.scratch/pr1_tolerance_probe.py` uncommitted): 4 Probe-1 cases + 2 new (arc endpoint on axis, polyline crossing twice). All 6 pass with initial choice; no fallback triggered. Changes: managed detection in `CreateHandler.cs::CreateRevolveStrict` (new `using Rhino.Geometry.Intersect`, docstring update, detection block between axis construction and factory call), native pointer comment in `SurfaceHandler.cpp` updated in lockstep. Live tests: 3 new in `test_revolve_live.py` — interior-crossing rejection, polyline-first-hit-wins semantics, endpoint-on-axis regression floor; 16 pre-existing unchanged. **Scope containment held exactly per plan doc §non-goals:** no native schema change, no new route, no new MCP tool, no ABI bump, no `BRIDGE_ROUTES`/`HTTP_MAPPINGS`/CapabilityRouter changes, no legacy `CreateRevolve` change, no `tolerance` schema exposure, no polycurve-structure-aware detection. 6/6 probe + 19/19 live + 45/45 intent_runtime = 70/70 green. **Codex residual note:** arc-endpoint-on-axis lives in the probe only, not the committed live suite — coverage gap, not blocker (line endpoint-on-axis live test exercises the same parameter-space rule). Next: Phase 3 PR-2 (`AlignWithSurface` on `/surface/sweep1`, cross-boundary).)

**Previously triaged:** 2026-04-21 (#50 shipped — preamble cast-failure runtime warning. Merged as `939a101` (PR #81). 2 files, +200 / −3, single commit, single-round Codex review with clean sign-off. **Script-pin sprint cluster COMPLETE** — all 4 items shipped in a continuous chain over ~8 hours: #38 (PR #78, payload `type` forward) → #39 (PR #79, ScriptParams pre-validation hoist) → #41 (PR #80, coverage gaps + outputs-recipient live) → #50 (PR #81, cast-warning observability). **Zero-behavior-change observability add** — `_cast(0)` substitution behavior identical to pre-#50 (25 pre-existing preamble/runtime tests still green as the behavior-preservation proof). New shared helper `_gh_cast_warn` emits one `ghenv.Component.AddRuntimeMessage(Warning, ...)` per pin per solve on first cast failure, wrapped in try/except so emission can never break a solve. `_warned = False` flag added to all three extractors (`_gh_extract_one`, `_gh_extract_list`, `_gh_extract_tree`) — per-call granularity means a 1000-item list with 500 bad casts emits 1 warning, not 500. Tests: 4 string-inspection (item/list/tree warn hook present + non-numeric pin has no invocation via no-cast-arg) + 3 runtime-exec extending the existing `_MockComponent` + `_MockGhEnv` framework with `AddRuntimeMessage` capture and a `sys.modules["Grasshopper"]` stub (list first-failure-only with 3 bad values, item single-value warning, defensive import-failure survival). **Codex residual testing gap noted:** tree-path warning branch is string-inspection-only, not runtime-exec — same warning hook pattern as item/list, coverage-equivalent via symmetric code path. 46/46 contract + 45/45 intent_runtime = 91/91 green. **Post-cluster re-triage 2026-04-21 (two-stage):** (1) explorer `HTTP_MAPPINGS` cleanup briefly promoted then demoted after scope audit revealed it's display/debug metadata only, not runtime routing — trigger hasn't fired. (2) #53 (`MaterializeExternalSourceSetForDefinition` helper) briefly promoted as next architecture move, then demoted after parallel scope audit — same shape as HTTP_MAPPINGS, concrete trigger ("4th handler" / "divergence bug" / "touching BlocksHandler for adjacent work") also hasn't fired. Both re-parked with sharper triggers. **Final promotion: Typed-route Phase 3 design pass** as Now. Rationale: returns to route-correctness / boundary-discipline work that matches recent momentum (Phase 1+2 shipped 16 typed routes over March-April; last 4 PRs were defensive hygiene on existing surfaces — good rhythm-break moment for architectural design work). Typed-route Phase 3 chosen over WebUI Phase 1 / RookVision / Security rollout because: (a) broader leverage than helper extractions; (b) closer to recent momentum; (c) WebUI + RookVision want deliberate product/design framing first that we haven't done. Deliverables for this Now: Phase 3 spike memo following Phase 2 template (candidate list, empirical probe results, substrate decisions per candidate). After spike signs off, Phase 3 PR-1 becomes Next.)

**Previously triaged:** 2026-04-21 (#41 shipped — `gh_set_script_pins` coverage + outputs-recipient live case. Merged as `ede71ee` (PR #80). 2 files, +317 / −11, single commit, single-round Codex review with clean sign-off. **Test-only; no source changes.** Five mock-based contract tests added next to the existing merge test in `test_server_contract_hardening.py`: (1) invalid-access rejection via `output_updates` for coverage balance (live suite already covers input-side) with corrected "one /gh/component read + no /gh/script-params write" invariant per Codex scope-pass (not "no HTTP round-trip"); (2) `current_name` nick-collision pinning current first-match-wins-on-name-or-nick behavior — tightening to name-only or raising on collision reserved for a separate behavior-change PR; (3) multi-rename in one call — two `{current_name, name}` entries both round-trip; (4) no-op rename (`current_name == name`) — idempotent AND payload still carries `current_name` which C# uses as wire re-attachment lookup key (dropping it would silently break reconnection); (5) typo `current_name` graceful error with structured envelope. Plus one live test in `test_gh_script_params_live.py` closing Codex's PR #79 residual-risk: `_setup_wired_script_with_downstream_recipient()` helper wires script.Result→panel, invalid `outputs` payload asserts recipient-side signature survived — symmetric to the 11 input-side tests. Helper tweak: `_wire` signature extended with optional `source_param` + `target_param` made keyword-only (multi-output disambiguation). Verification: 39/39 contract + 45/45 intent_runtime + 12/12 live = 96/96 green (109.44s live). **Script-pin cluster 3/4 complete** (#38, #39, #41 shipped; #50 last). Next up: #50.)

**Previously triaged:** 2026-04-21 (#39 shipped — ScriptParams pre-validation hoist. Merged as `63de668` (PR #79). 2 files, +453 / −0 across 3 commits (initial C# + live tests, Codex-driven test-harness rewrite, empirical wire-format corrections). **11/11 live-Rhino tests + 45/45 intent_runtime = 56/56 green.** Closes #39's atomic-rejection invariant: invalid JSON shapes (non-array `inputs`/`outputs`) or non-string field values (`name`, `access`, `nick`, `current_name`, `description`) now reject BEFORE any destructive unregister loop. Full destructive-path surface covered: pin-level `name`/`nick`/`current_name`/`access`/`description` + root-level `guid`/`nick`/`inputs`/`outputs`. Three Codex review rounds (v1→v2→v3) progressively widened the scope from "3 TryApply branches" to "all user-driven validation before any mutation, including the benign post-rebuild root `nick`." **Three empirical-probe findings landed during live test authoring, all test-harness only:** (1) `gh_create_python_script` returns `component_guid`, not `guid`; (2) `gh_connect` is NOT a registered MCP tool — wiring uses raw POST /gh/connect (GrasshopperHandler.cs:5193); (3) **`/gh/connections` JSON is camelCase, not PascalCase** — ASP.NET's default `JsonNamingPolicy.CamelCase` silently lowercases C# anonymous-type property names in transit. Both the original test code and the Codex static review assumed PascalCase based on the C# source; without this correction every rejection test would have passed blindly on empty `Inputs` payloads — the exact false-positive mode Codex's Finding #1 warned about pre-implementation. Live-test authoring was load-bearing. **Error text stability preserved** for pre-existing `TryApplyScriptPinDefinition` branches (`Pin definition is missing 'name'`, `Pin definition has an empty 'name'`, `Invalid access 'X' for pin 'Y'`); new branches carry direction + idx context. `TryApplyScriptPinDefinition` inline checks left in place as defense-in-depth for future callers. **Non-goals explicit:** SDK-driven `CreateParameter returned null` paths still bail mid-flight (Option B — catch-and-rollback — deferred); root `description` type check skipped (already ValueKind-guarded; silent-ignore → explicit-reject is a deliberate contract change reserved for a future PR). **Codex residual-risk note:** live suite is input-focused; invalid `outputs` payloads are covered by the pre-validation structurally but lack a dedicated live case with a downstream recipient. Captured as an in-scope extension of #41 on promotion. Script-pin cluster #2 complete; next up #41.)

**Previously triaged:** 2026-04-21 (#38 shipped — `type` field forwarded to `/gh/script-params` payload. Merged as `e82ac1c` (PR #78). 2 files, +15 / −1. Scope held exactly to the spike — single tuple-element addition at `mcp_server/src/rook/server.py:601` plus 4-line forward-compat comment; 3 rich-pin-creation `assert payload == {...}` expectations updated (csharp + python + chirp); existing `test_gh_set_script_pins_merges_existing_component_params` extended with `typeName` fixture + `type` assertions per Codex scope-pass feedback (less churn than a separate round-trip test). C# side unchanged — `TryApplyScriptPinDefinition` reads only known properties via `TryGetProperty`, additive field is a safe no-op there. Contract hardening 34/34 + intent runtime 45/45 = 79/79 green locally; Codex verified both suites independently at review time. Single-round Codex review, no findings. Script-pin cluster opener complete.)

**Last corrected:** 2026-04-21 (Documentation drift fix pre-#38 scope pass. Three errors found against live GitHub issues + current code: (1) `#39` section title and description paraphrased from memory as *"pin-removal orphan wires silently dropped by `gh_set_script_pins`"* — the actual GitHub issue #39 is *"ScriptParams: invalid `access` value leaves component half-rebuilt"*. Replaced section wholesale + revised risk from M to Low-M (prior M estimate was sized against the fictional "drain remaining wire queues" scope, not the real Option-A pre-validation hoist). No GitHub issue exists for the orphan-wires concern — if that was a real observation, it needs filing separately. (2) `#38` `why_now` + `resume_entry_point` referenced bundling with `#40` — `#40` shipped as PR #46 on 2026-04-16 (see Recently shipped row below). Removed the stale bundle question and replaced the generic resume pointer with exact file:line surfaces. (3) Parked `#41` trigger said "After #38/#39/#40 ship" — removed `#40`. No code changes; drift fix only.)

**Previously triaged:** 2026-04-20 (`#9 rhino_block_reset_scale_batch` shipped + **6-handler envelope-shape hygiene bundled** — Parked "Batch handler envelope-shape hygiene" entry closed in the same PR, the sanctioned "bundle with next batch-tool PR" trigger fired exactly on time. Merged as `caa86d9` (PR #77). 13 files, +981 / −7 across 4 fixup commits pre-merge (all pre-PR-open scope-pass iterations + 3 Codex review rounds). **Feature is native-only** (direct-sdk, no ABI bump — scope-pass v1 was wrong about "companion-backed ABI pattern"; corrected v3 on the GUID-preservation contract); **bundled hygiene is managed** (6 `TryGetProperty`-on-non-object guards in BlocksHandler.cs). Schema `{ids: [uuid-str, ...]}` with per-item best-effort; errors match HandleBlockReplaceInstanceBatch (`invalid_id`/`not_found`/`not_instance`/`recreate_failed`). GUID-preservation pinned empirically (oldInstanceId == newInstanceId per successful entry, matches the batch-family contract documented at BlocksHandler.cs:3314). Request-order preserved. 3 Codex review rounds landed pre-merge: (1) `reset-scale destroyed rotation` — both single + batch paths were extracting only translation from oldXform, silently zeroing rotation; fixed via new `BuildResetScaleXform` column-length decomposition helper applied to both routes, backfilled the pre-existing single-instance bug in the same PR because splitting would diverge the routes while the fix was in flight. (2) `DeleteObject return value unchecked` — both routes ignored the bool return before calling CreateInstanceObject, so a failed delete + successful recreate would leave duplicate instances AND report success with the wrong oldInstanceId. Fixed with stronger-recreate-semantics pattern (fail before recreate). (3) `empty ids:[] not a true no-op` — batch opened UndoScope + called Redraw unconditionally despite the documented no-op contract. Fixed with lambda short-circuit matching UserTextHandler.cpp:710 rhythm. **Test pin strengthened** to measure NextUndoRecordSerialNumber delta pre/post (empirical finding: `UndoRecordCount` does NOT exist on RhinoDoc in RhinoCommon 8; `NextUndoRecordSerialNumber` is the correct accessor; each `rhino_execute` advances it by 1 so expected delta == 1, delta ≥ 2 would prove handler opened its own scope). 14/14 batch live + 12/12 envelope-hygiene live + 24/25 block-family regression + 45/45 intent_runtime = **95/96 green** (1 pre-existing skip). Explorer/registry HTTP_MAPPINGS backfilled for both single (pre-existing gap) + batch routes. Parked "Batch handler envelope-shape hygiene" entry removed — bundled sanctioned-as. `blocks` CATEGORIES tuple extended with `reset_block_instance_scale_batch`. Next: script-pin sprint opener (#38) OR continued batch-tool roadmap — judgment call for re-triage.)

**Previously triaged:** 2026-04-20 (Usertext delete surfaces shipped — `/usertext/object-delete` + `/usertext/document-delete`. Merged as `f650bdd` (PR #76). 10 files, +1171 / −34 (single squashed commit — scope-pass fixups and Codex review fixups landed pre-PR-open, no fixup commits). Closes the Phase 2 usertext family; PR-9 + PR-10's empty-string-rejection forward-references now point at shipped surfaces. **19/19 new live-Rhino delete tests + 35/35 usertext set/get regression + 105/105 unit (45 intent_runtime + 60 Phase 2 audit after 2-delete extension) = 159/159 green.** Schema: `{keys: [string, ...]}` per route (+ `id` for object) — mirrors BlocksHandler delete convention, unambiguous vs set routes' `userStrings: {k:v}` shape. Delete sentinel: empty string at both levels (attribute characterized by test_usertext_object_live.py:194; document by test_usertext_document_live.py:11) — contract-internal pre/post-diff derivation of `deletedKeys`, no SDK-return-value dependency. **Duplicate keys silently deduplicated, PRESERVING FIRST-SEEN caller order** via `std::vector + std::unordered_set` (Codex scope-pass rejected `std::set` alphabetization). Idempotent delete (absent key → not in deletedKeys, no error). Empty `keys: []` = idempotent no-op. Reserved-prefix denylist gates doc-delete writes; error message anchors `/block/user-strings with action=delete` (Codex scope-pass: action hint load-bearing, not just route name). Wholesale rejection with sneak-delete guard test. **Codex first-round review landed 3 pre-PR-open findings:** (1) survived-existing-key failure detection — requested key present pre AND post-mutation now surfaces `operation_failed` rather than silently omitting from `deletedKeys` (prevents "delete failed" being indistinguishable from "key was absent"); (2) purge helper replaced with proven `StringTable.GetKey(i)` pattern + asserts `rhino_execute` result; (3) stale "delete not shipped yet" wording cleared from both MCP tool descriptions and RouteSpec descriptions on set routes (PR-9/PR-10 set tools now point at `rhino_usertext_{object,document}_delete` directly). Second Codex review round: clean sign-off based on diff + unit checks. **Phase 2 usertext family COMPLETE** — all 6 verbs shipped (object/document × set/get/delete); sibling audit covers all 6. `user_text` CATEGORIES tuple 4→6. Next: `#9 rhino_block_reset_scale_batch` (previously-parked batch-tool feature; demotion reason resolved).)

**Previously triaged:** 2026-04-20 (Phase 2 surface/curve PR-4 shipped — `/curve/boolean` typed route; **campaign close**. Merged as `5e36bc2` (PR #75). 17 files, +1474 / −153 + 1 fixup commit (+155 / 0 for Codex-requested operation-injection pins on both dispatch surfaces). **23/23 live PR-4 tests + 57/57 Phase 2 surface/curve regression (EdgeSrf + BlendCurves + Patch + NetworkSrf) + 83/83 Phase 1 surface regression (Pipe + Loft + Sweep{1,2} + Revolve) + 75/75 unit (45 intent_runtime + 30 sibling audit + 3 transform pins) = 238/238 green**. Three intent keys (`curve_boolean_{union,difference,intersection}`) → one `/curve/boolean` endpoint via `operation` discriminator; mirrors Phase 1 brep-boolean pattern. **Native helper promotion bundled**: `EmitNormalized` + `DispatchToManagedCreate` moved from SurfaceHandler.cpp anon namespace to `Infrastructure/ManagedCreateDispatch.{h,cpp}` (third-caller rule — PR-4 joined HandleBlendCurves + every SurfaceHandler X); HandleBlendCurves local-mirror tail (~65 LOC) and SurfaceHandler anon block (~95 LOC) collapsed — net -60 LOC across source. New plural-curve helper `InsertCurvesAsPluralResponse` parallel to Brep helper (generalization deferred). **Four contract findings from empirical probe** deviated from plan-doc: (1) factory has NO `out int error` — binary failure signal is `Length==0`; dropped proposed `non_planar_input` code in favor of collapsed `operation_failed` (PR-3 precedent); (2) `tolerance<=0` NOT silently coerced; worker-thread rejects as `invalid_input` for diagnostic clarity; (3) difference overload `(curveA, IEnumerable<subtractors>, tol)` exists — deferred to follow-up, PR-4 ships exactly-2 pair form; (4) nested union (outer+inner→outer envelope) pinned as `test_factory_permissive_smoke`. Empty-diff pin (`inner - outer`→`operation_failed`) added per Codex alongside disjoint-intersection — closes both canonical empty-result branches. Sibling audit `test_capability_router_phase2_surface_curve_coverage.py` with tuple-derived exact-endpoint inventory (NOT prefix-based — would overcapture Phase 1 + pre-Phase-1 routes) covers all 7 PR-1..PR-4 intents × 4 surfaces + symmetric inventory. Codex flagged one gap on first review: operation-injection was unpinned on both dispatch surfaces (MCP executor at server.py:10471 + agent-direct at tool_dispatcher.py:408) — a copy/paste mistake like `_difference→"union"` would have passed everything. Fixup added 3 live pins (discriminating disjoint-circles geometry — union=2 objs, difference=1 obj, intersection=operation_failed) + 3 parametrized unit pins against TRANSFORM_FUNCTIONS. Second review round: no findings, signed off for squash-merge. **Phase 2 surface/curve extension campaign COMPLETE** — 4 PRs (#72/#73/#74/#75), 5 endpoints, 7 intent keys, zero ABI bumps. `creation` count 20→39, `curves` count 12→16. Next: Usertext Delete surfaces (closes last missing usertext verb).)

**Previously triaged:** 2026-04-20 (Phase 2 surface/curve PR-3 shipped — `/surface/network` typed route. Merged as `66193af` (PR #74). 11 files, +747/-4 + 1 fixup commit (+5/-2). **20/20 live-Rhino NetworkSrf tests + 17 PR-2 + 11 + 9 PR-1 regression + 45 intent_runtime = 102/102 green** in 184.8s. Empirical probe landed one plan-doc correction: explicit overload takes 4 continuity slots (`uContStart`, `uContEnd`, `vContStart`, `vContEnd`), not 2 — PR-3 applies single param to all 4 slots (asymmetric deferred). Factory's `out int error` empirically binary (0/1) — collapsed proposed `network_build_failed` route-specific code to `operation_failed` with raw code in message for diagnostic traceability. Factory silently coalesces continuity values ≥1 and <0 (only `cont=0` observably distinct) + silently accepts zero/negative tolerances — worker-thread rejection load-bearing. Continuity-distinctness pin runs on explicit form to simultaneously exercise overload 2 reachability + continuity honoring. 5 Codex review rounds caught: empirical 4-slot continuity finding, XOR edge branches (i/j/j+ for empty arrays), interior tolerance, explicit-form pin, and a late MCP tool description drift (single-curve listed as operation_failed when route actually rejects as invalid_input via min-count). Next: PR-4 (CurveBoolean).)

**Previously triaged:** 2026-04-20 (Phase 2 surface/curve PR-2 shipped — `/surface/patch` typed route. Merged as `015b260` (PR #73). 11 files, +871 / −3. **17/17 live-Rhino Patch tests + 20 PR-1 regression + 45 intent_runtime = 82/82 green** in 166s. Empirical pre-implementation probe landed three API-shape findings that the plan-doc had assumed: (1) overload 3 with null seed is NOT equivalent to overload 2 (produces 49% larger surfaces on identical inputs), forcing a route-level branch on `startingSurfaceId` presence; (2) factory silently coerces `uSpans=0`, `flexibility=-1`, `tolerance=0` into garbage geometry, making worker-thread rejection load-bearing; (3) `AddPlaneSurface` returns a 1-face Brep not a Surface, so seed resolver must auto-extract `UnderlyingSurface()`. Real `operation_failed` case found empirically (single-point-only input) — satisfies Common Plan amendment without needing `test_factory_permissive_smoke`. Three new shared managed resolvers shipped (`ResolveGeometryStrict`, `ResolveGeometriesStrict`, `ResolveSeedSurfaceStrict`) — first callers are this PR; re-usable across future routes. All 4 agent discovery surfaces wired. Completeness count 37→38. 4 Codex review rounds; empirical-probe-as-hard-gate + factory-silent-coercion-rejection refinements captured to `feedback_pr_scoping_rhythm.md`. Next: PR-3 (NetworkSrf).)

**Previously triaged:** 2026-04-20 (Phase 2 surface/curve PR-1 shipped — `/surface/edge` + `/curve/blend` typed routes. Merged as `3b8e778` (PR #72). 14 files, +1037 / −10. **20/20 live-Rhino tests** (11 EdgeSrf + 9 BlendCurves) + 45/45 intent_runtime regression green. First mixed-substrate route in `CurvesHandler.cpp` (managed-bridge reuse alongside 12 direct-sdk routes). First strict-attribute creator whose factory returns a Curve — serialization parity pinned. Common Plan amendment landed at `2026-04-17-typed-route-phase1-plan.md:257` for factory-permissive-smoke — empirical probing found both factories accept zero-length / coincident / disjoint / 3D non-planar inputs. All four agent discovery surfaces wired (MCP tools, `BRIDGE_ROUTES`, `context.TOOL_CATEGORIES`, `explorer/registry.py`). Bundled hygiene: `test_all_geometry_creation_ops` count drift fix (20→37; was stale from Phase 2 annotation campaign). 5 Codex review rounds caught 7 pre-merge issues. Next: PR-2 (Patch).)

**Previously triaged:** 2026-04-20 (Phase 2 surface/curve extension plan-doc signed off by Codex after 4 review rounds. Spike memo `rook_docs/2026-04-20-phase2-surface-curve-spike.md` captures live-Rhino fallback matrix (5 urgent / 2 useful; OffsetSrf dropped as already shipped; BlendSrf + FilletSrf deferred on edge-on-face / UV-point-on-face scaffolding gap). Plan doc `rook_docs/2026-04-20-phase2-surface-curve-plan.md` locks 5 endpoints / 7 intent keys across 4 PRs, managed-bridge reuse via `CreateGeometry` (zero ABI bumps), sibling audit file with tuple-derived endpoint set, and an explicit completeness-count schedule `creation: 20→36→37→38→39→39` (PR-1 bundles pre-existing 20→36 drift fix) + `curves: 12→13→13→13→16`. Branch `fix/phase2-surface-curve-spike` holds the scope-pass memo + plan; no code changes yet. Next: PR-1 scope pass in chat per `feedback_pr_scoping_rhythm.md`, then implementation.)

**Previously triaged:** 2026-04-19 (Phase 2 PR-10 shipped — `/usertext/document-set` + `/usertext/document-get` + reserved-prefix denylist + new route-local error code `reserved_namespace` + 4-surface CapabilityRouter Phase 2 audit + `/select` additive-shape reciprocity pin. Merged as `4a4fbd6` (PR #71). **209 tests passing** (19 PR-10 live + 16 PR-9 regression + 88 annotation regression + 34 Phase 1 audit + 52 Phase 2 audit). **Phase 2 typed-route campaign COMPLETE** — all 10 shipped PRs (0, 1–7, 9, 10); PR-8 hatch remains plan-deferred to Phase 3. Coverage delta 62% → ~70% per plan §4 target. **Empirical pre-flight worked as designed:** probed `RhinoDoc.Strings.SetString(k, "")` BEFORE writing the scope, confirmed doc-level empty-string deletes same as attribute-level (PR-9's mid-PR surprise is not a surprise here); PR-10 inherited the rejection posture with no contract corrections required. **Audit caught two real inventory gaps pre-merge:** (a) annotation intents PR-1..PR-7 shipped without `CATEGORIES["creation"]` membership — backfilled 8 intents in-scope; (b) `rhino_select` MCP tool description only advertised "ids or layer," never the PR #62 additive `name`/`bbox` shapes — expanded description + schema. **Implementation-time test-helper finding:** `RhinoCommon StringTable.SetString(k, None)` does NOT delete (returns current value, no mutation) — the actual managed delete sentinel is the empty string, identical to native. Purge helper fixed in lockstep. Single Codex review round — no code findings. Next: Phase 2 surface/curve extensions (Category 5 — 6 surface ops + 2 curve ops on the Phase 1 Pipe `managed-bridge (reuse)` substrate) per work-queue `Next`.)
**Owner:** bringfire (lead) + Claude (implementation)

The single source of truth for "what's in flight, what's next, what's parked, why."
Update this file as part of finishing or starting any work. Don't keep deferred
items in memory — they go here before any new work begins.

---

## Operating model

- **One active implementation branch at a time, total.** Don't open a second
  feature branch while another is in review.
- **One explicit `Now` item, one explicit `Next` item.** Everything else has a
  written status: `Blocked` or `Parked`.
- **Mid-stream items get triaged immediately** with one of three labels before
  any continuation: `do now` (interrupt and integrate), `queue next` (move
  current Next to Parked, promote new item), or `park` (capture in this doc and
  keep going).
- **Don't mix work types in one session.** Finish a substrate cleanup chapter
  before opening a feature design. Brainstorm + plan + execute one thing fully
  before opening another.

## Ordering rule

When picking what to promote into `Next`:

1. Unblockers (anything blocking other queued work)
2. Small mechanical cleanups that close active context
3. Roadmap features with high product value
4. Architectural changes
5. Release polish

## Item fields

Each item carries:

- `type`: `feature` / `substrate` / `architecture` / `release` / `hygiene`
- `stage`: `brainstorm` / `plan` / `execute` / `review-ready` / `merged`
- `value`: short-text rationale for impact
- `risk`: `Low` / `M` / `High`
- `why_now`: one-line rationale for being in `Now` (only for the `Now` item)
- `blocked_by`: only for items in `Blocked`
- `trigger_to_revisit`: only for items in `Parked` — what condition promotes it
- `date_parked`: ISO date item entered `Parked` (only for items in `Parked`)
- `source_doc`: where the design/context lives

---

## Now

**Active item: post-#114 product/polish lane selection.**

- **Type:** triage / roadmap selection
- **Stage:** queue selection / design-needed
- **Risk:** Low-M (selection only; chosen lane may vary)
- **Why_now:** #113 and #114 are both closed, so the explicit low-risk test-debt lane is done. The queue should return to one concrete product/polish item rather than continuing to mine parked helper extractions without a trigger.
- **Source_doc:** Open issue #37; deferred RookVision video thumbnail tiers; PR-V4 video MCP parity notes; current RookVision focus-hotfix release context.

**Expected scope:**
- Re-read the remaining non-blocked product/polish candidates after #114 is resolved.
- Leading candidates today: #37 block-link geometry coverage, RookVision video thumbnails Tier 1, PR-V4 video MCP parity, or a user-directed Vision follow-up if the affected customer reports on `v1.5.3`.
- Do not promote #53 unless one of its documented triggers fires.

**Acceptance direction:** Choose one concrete implementation/design item for `Now`, or explicitly keep product-lane selection open with the reason.

**Rhino runtime harness usage notes for future work:**

Use the harness as the fast "does this actually work in Rhino?" gate when a change touches live routing, discovery, native readiness, selected live tests, or process cleanup. It is not a replacement for unit tests; run focused unit tests first, then use the harness for owned-process proof.

Primary commands:

```powershell
python scripts\run_rhino_runtime_harness.py --smoke ping-only
python scripts\run_rhino_runtime_harness.py --smoke pytest-select
```

- `ping-only` is the non-mutating startup/readiness/cleanup diagnostic. It proves Rhino launches, RookNative writes the owned discovery file, `/ping` answers on the exact owned port, artifacts are captured, and external close works.
- `pytest-select` is the first real live smoke. It proves selected `requires_rhino` tests run against the owned PID/port and that dirty-document cleanup exits cleanly after the harness saves the owned document into the artifact folder.
- Harness mode uses `ROOK_RHINO_PORT=<port>` and `ROOK_RHINO_PROCESS_ID=<pid>`. For pytest, this pair is the safety contract; do not rely on `NATIVE_PORT` alone.
- Run artifacts live under `.scratch\rhino-runtime-harness\<run-id>\`; inspect `manifest.json`, owned discovery snapshots, copied `%TEMP%\rook` files, smoke stdout/stderr, cleanup status, and warnings when debugging.
- Good prompt to invoke it: "Use the Rhino runtime harness to validate this change against a real owned Rhino session. Start with `--smoke ping-only`, then run `--smoke pytest-select`. Inspect the manifests and tell me whether readiness, scoped pytest routing, artifact capture, and cleanup were green. If anything fails, debug from the harness artifacts instead of using an already-running Rhino."
- Feature-specific prompt: "I changed `<route/fixture/discovery path>`. Use the runtime harness to run the selected live smoke against an owned Rhino process. Confirm the smoke env only targets the owned PID/port, verify the selected live tests pass, and check that cleanup is `graceful_exit`."

---

**Parallel diagnostic lane: WebView panel focus/blackout observation.**

`RookWebSurface` is shared by Chat, Vision, Knowledge Graph, and Claude-like panels. PR #132 made the dangerous repaint toggle opt-in because it caused an intermittent focus feedback loop and Rhino freeze during Vision testing. Future agents should use these rules:

- Default runtime: `ROOK_ENABLE_WEBVIEW_REPAINT_WORKAROUND` unset; `RequestWebViewRepaint` is effectively a no-op.
- Diagnostics only: set `ROOK_ENABLE_WEBVIEW_FOCUS_DIAGNOSTICS=1` to enable the JS focus probe and `%TEMP%\rook\webview-focus.log`.
- Risky workaround testing only: set `ROOK_ENABLE_WEBVIEW_REPAINT_WORKAROUND=1` after explicitly accepting the flicker/freeze risk.
- Do not reintroduce unconditional `CoreWebView2Controller.IsVisible` toggles in Chat/Vision/KG panel code.

---

**Parallel passive lane: substrate-observation accumulation.** `knowledge/substrate_observations.jsonl` started capturing 2026-04-21 with PR #88's merge (`fb970a7`) and now contains the current one-line routed-tool observation corpus. It is not part of the WebView hotfix or multi-provider PRs. Re-triage only after reviewing actual hotspot signal.

---

## Next

**Next: RookVision video thumbnails Tier 1 (tentative fast polish candidate).**

- **Type:** product polish
- **Stage:** scope-pass / implementation-ready if selection confirms
- **Risk:** Low-M (managed Vision artifact path; no provider behavior change intended)
- **Source_doc:** Deferred candidate "RookVision video thumbnails" below; current managed Vision video artifact pipeline.

**Expected scope:**
- During the product-lane selection pass, confirm whether this beats #37 and video MCP parity on value/time.
- If selected, keep Tier 1 only: for I2V/interp jobs, copy the source start-frame artifact bytes as a `poster` blob on the generated video artifact during the manager's saving/materialization stage.
- Do not introduce JS extraction, Media Foundation extraction, new public/native routes, or provider-specific thumbnail work in Tier 1.

**Acceptance direction:** Generated video artifacts from source-frame workflows get a poster blob without changing Gallery JavaScript or provider submission/result semantics.

**Deferred candidates** (any can promote if analysis or user direction surfaces a reason to pivot):

- **PR-14a mode UX:** parked unless real use shows confusion around Generate prompt-only vs source-image workflows, Studio compatibility, or async job affordances.
- **Provider discovery substrate:** useful as background research for Replicate/fal.ai catalogs, but should not become an uncurated dynamic model list without a separate design.
- **Native/MCP/public HTTP parity for image job ops:** still deferred until an agent/Rhino workflow specifically needs non-bridge access.
- **PR-V4 — MCP tools for video:** `rhino_render_video`, `rhino_video_jobs`, `rhino_video_status`, `rhino_video_cancel` + tool-catalog wiring per the parity rule. Still valuable, but wait until an agent/Rhino workflow specifically needs non-bridge video access or the next product-lane triage selects parity work.
- **Bigger strategic moves** (each needs design-first framing before any PR):
  - WebUI substrate Phase 1 — nonce auth + virtual host + CDN asset migration; unblocks Knowledge Graph / 2D-to-3D / Scene Graph / NLE dashboard modules (design in `project_webui_substrate_architecture.md`). **Note:** RookVision module already ships on an equivalent Pattern A substrate (`connect-src 'none'` + virtual-host origin + embedded-resource serving) via PR #99, so parts of this work may collapse into generalizing what VisionWebSurface proved rather than green-field design.
  - HAWPv3 / image-to-CAD spike resume — foundation substrate (artifact store, DPAPI-wrapped secrets, Pattern A JS bridge, virtual `/blob/{id}/{role}` resolver) now fully shipped via the image track; HAWPv3 can consume these directly rather than waiting on primitives. Branch state uncertain — check `feature/image-to-cad-spike`.
  - Security audit rollout — CORS + nonce auth path; first audit completed, rollout deferred pre-public-release flip
  - **RookVision v1 follow-ups** (small, post-merge polish candidates; none scoped as PRs yet): panel focus blackout is **not closed by an unconditional repaint workaround**. PR #132 makes the risky repaint workaround opt-in and keeps diagnostics available; revisit only with fresh reproduction/log evidence. Configurable Gallery folder (plumbing 80% present via `ArtifactStore(overrideRoot)`; settle per-project-vs-global question first); multi-viewport capture in quad layouts; **CI ABI-matched-binary check** so clean-user installs never hit the same freeze PR-7b integration smoke hit — note that PR #102's `install.ps1` fix addresses WRONG-TARGET selection (net48 vs net7.0) but NOT ABI drift between companion + native, which is the hazard that actually froze Rhino; the ABI-check item is narrower now but still open.

  - **RookVision video thumbnails** (NEW 2026-04-26 from V3 manual smoke; tiered scope, pick the one that matches available time):
    - **Tier 1 (~30 min)**: For I2V/Interp jobs, copy the start-frame artifact's bytes as a `poster` blob on the new `generated_video` artifact at the manager's "Saving" stage. Free thumbnail, 0 new deps, fixes ~50% of cases (architects often start from a viewport capture or earlier render). Gallery already prefers `role: "poster"` when present so JS impact is zero.
    - **Tier 2 (~1–2 h)**: JS-side first-frame extraction for T2V via hidden `<video preload="metadata">` + `<canvas>` + dataURL on first Gallery open; new `attach_artifact_poster` bridge op writes the bytes back as a poster file. Closes the remaining 50%. Requires a new UI-only bridge op + an `ArtifactStore.AppendFile` method.
    - **Tier 3 (~half day)**: Server-side MP4 frame extraction via Windows Media Foundation (`MFCreateSourceReaderFromURL` + `ReadSample`). Production-grade, no JS dependency, generates posters at create-time, robust against future non-WebView consumers (MCP tools, GH NLE). Right answer if/when GH NLE materializes; premature otherwise.
    - **Recommendation:** ship Tier 1 as a fast-follow alongside V3 or with V4; defer Tier 2/3 to GH NLE planning.

---

## Blocked

| Item | Type | Blocked by | Source |
|------|------|-----------|--------|
| **#29 — companion `CreateBlock` managed basePoint writer** | substrate | RhinoCommon 8.0.23304 `InstanceDefinition.UserData` SDK gap (#34 gate). Code change is small (`RookBlockBasePointUserData.Attach`), but under the current SDK limitation it does not buy real metadata persistence on the managed path. | https://github.com/bringfire/Rook/issues/29 |
| **#34 — retire legacy basePoint user-string write + managed reflection bridge** | substrate | ≥1 release window since #33 shipped AND RhinoCommon exposes plugin `ON_UserData` on `InstanceDefinition.UserData` to managed callers. Both conditions must be true. | https://github.com/bringfire/Rook/issues/34 |
| Per-index `mappings` reachability for `set_block_materials` / `_object_colors` / `_object_user_strings` via `/intent` | substrate | XOR `RouteSpec` architecture (Parked) | `2026-04-14-intent-runtime-followups.md` |

---

## Parked

Items deferred with explicit triggers for revisit. None are forgotten — each
has a documented re-entry condition.

| Item | Type | Risk | Date parked | Trigger to revisit | Source |
|------|------|------|-------------|--------------------|--------|
| **Contextual capability understanding substrate** | architecture | M | 2026-04-22 | When the design doc has been debated enough to choose the first implementation slice OR when user direction explicitly promotes it over the current hotspot-driven typed-route re-triage. First expected slice: build the replayable event substrate (`NormalizedEvent` schema + source adapters for Codex/Claude + native/GH session streams + local install detection/consent/versioned reprocessing), not any UI layer. | `rook_docs/2026-04-22-contextual-capability-understanding-design.md` |
| **Hermes-derived learning glue: background review, content guard, session recall** | architecture | M | 2026-04-22 | When user direction explicitly promotes agent-learning infrastructure over the current hotspot-driven typed-route/substrate campaign rhythm, OR when an adjacent task makes one narrow slice cheap to bundle. Preferred first slices, in order: (1) generalized `content_guard.py` adapted from Hermes' `skills_guard.py`; (2) read-only `session_recall` over Rook's split native + GH session stores with an FTS sidecar and match-centered truncation; (3) a quiet review loop that produces notes/drafts but does **not** auto-save patterns. Do **not** host this in `agent/conductor.py` (fleet coordinator only), do **not** assume a single session store, and do **not** bypass the current `gh_reflect` -> human approval -> `gh_save_pattern` contract. | `rook_docs/2026-04-22-hermes-adoption-design.md` |
| **`GeometryHandler` ON_TextDot branch for `/geometry` cross-surface verification on dots** | hygiene | Low | 2026-04-19 | When a real workflow needs to read back a text dot's text/font/height via `/geometry` (currently `/annotation/dot` POST response is the only surface that exposes those fields). Add an `ON_TextDot` cast branch in `CaptureGeometryDetail` mirroring the existing `CRhinoAnnotation` branch at GeometryHandler.cpp:48-64; surface text/secondaryText/heightInPoints/fontFace as a new `TextDotDetail` snapshot variant. Will also unlock cross-surface invariant tests for the dot route. | Codex review of PR #69 |
| **Zero-projection edge case for `/annotation/dim-linear`** | hygiene | Low | 2026-04-19 | When a user reports a surprising zero-dim behavior OR when extending dim-linear tests. Request like `start=[0,0,0], end=[0,5,0], direction=[1,0,0]` produces `measuredValue=0`; `AddDimLinearObject` with a dim-line parallel to direction and passing through both extension points is SDK-sensitive — may succeed, may produce a degenerate dim. Not currently pinned in the test suite. | Codex review of PR #64 |
| **XOR `RouteSpec` architecture** | architecture | M | 2026-04-14 | Another XOR shape lands in the router OR per-index `mappings` becomes a real `/intent` use case. **Currently blocks 5 entire routes + partial coverage of 4 others.** | `2026-04-14-intent-runtime-followups.md` |
| **Layer read/query intent reachability** (`rename_layer`, `move_layer_objects`, `merge_layers`, `layer_dependencies`) | substrate | Low | 2026-04-14 | A real workflow needs `/intent` to query/read layers. | `2026-04-14-intent-runtime-followups.md` |
| **`bootstrap/test_matrix.py` test cases for batch tools** | substrate | Low | 2026-04-14 | After intent routing and explorer classification stabilize across all block tools. | `2026-04-14-intent-runtime-followups.md` |
| **Two-engine reality for `transform_block_instance`** | architecture | M | 2026-04-14 | Only if zero-scale or other parity drift between native single-target and companion batch becomes a real correctness issue. | `rook_docs/2026-04-14-transform-instance-batch-design.md` |
| **Public release follow-ups:** Python `_load_env` defense, build-all script, chat panel recovery UX | release | varies | 2026-04-14 | Bundle when actually preparing a release or when individual issues become painful enough to justify interruption. | `~/.claude/.../memory/project_public_release_followups.md` |
| **SubD / annotation / non-materializable support for non-origin basePoint** | feature | Low | 2026-04-15 | When a real workflow surfaces non-origin basePoint + SubD. | PR #25 review |
| **`rhino_block_replace_instance` discoverability** | hygiene | Low | 2026-04-14 | When curating tool descriptions for discoverability sweeps. | PR #23 fresh-Claude smoke report |
| **`rhino_objects` block-definition identity for instance references** | hygiene | Low | 2026-04-14 | When a workflow surfaces that actually needs block-def identity at the `rhino_objects` layer. | PR #23 fresh-Claude smoke report |
| **explorer registry `HTTP_MAPPINGS` cleanup** | substrate | Very Low | 2026-04-21 | **Any of:** (a) explorer agent starts consuming `ToolDefinition.http_endpoint` for something operational (currently display/debug only); (b) `explorer/registry.py` is being touched for another reason (bundle cleanly); (c) per-PR `HTTP_MAPPINGS` backfill churn becomes annoying enough to justify interruption. **Originally mis-scoped** (Parked then briefly promoted to Now then re-demoted after scope audit) — the dict is NOT runtime routing, only display metadata for explorer agent debug output. Wrong entries mislead logs + greps but don't affect correctness. See scope-audit summary in chat transcript 2026-04-21. | `rook_docs/2026-04-14-intent-runtime-followups.md` (§ "explorer registry metadata cleanup (HTTP_MAPPINGS staleness)") |
| **#53 — extract `MaterializeExternalSourceSetForDefinition` helper** | architecture | Low | 2026-04-21 | **Any of:** (a) a 4th handler in `BlocksHandler.cpp` wants the same world-to-local materialization pattern; (b) one of the three existing call sites (`HandleBlockCreate` :1304-1325, `HandleBlockAddObjects` :1866-1887, `HandleBlockReplaceGeometry` :2060-2081) drifts from the shared pattern in a way that causes a correctness bug; (c) `BlocksHandler.cpp` is being touched for adjacent work (bundle cleanly). Original trigger from issue #53 was (a) or (b); (c) added per 2026-04-21 re-triage. **Briefly promoted to Now after script-pin cluster close, re-demoted after scope audit** (same shape as HTTP_MAPPINGS — concrete trigger hasn't fired; promoting "because it's shovel-ready" is rationalization). C++ refactor in native code; acceptance via existing block live test suites (rebase, replace-object-geometry, reset-scale-batch, batch-envelope-hygiene). Reference shipped pattern: PR #52 (`MutateDefinitionLocalFrame` + `RewriteDefinitionGeometry` extraction, closed #26). | https://github.com/bringfire/Rook/issues/53 |
| **GhPython component note + creation-identity story** | knowledge | Low | 2026-04-21 | **Any of:** (a) a `comp_*.json` note is added for GhPython Script (`410755b1-224a-4c1e-a407-bf32fb45ea7e`) in `knowledge/gh/notes/` — today only a recipe reference (`recipe_41f76822.json`) mentions it, so it never enters the `UnifiedStore.resolve_components()` candidate pool; (b) an agent workflow surfaces that needs GhPython specifically and current routing routes around it. **Follow-up work when triggered:** revisit `GHIntentResolver._component_concept_key` Python branch (`mcp_server/src/rook/learning/dspy_modules.py:1245`) to include GhPython in the `python_script` concept bucket; re-read memo §Tagging table (GhPython is GH1-legacy → no `creationTool` → would NOT join the handoff map); decide whether GhPython is a legacy cue target in `_select_preferred_candidate_for_concept` or stays outside preference rules. **Why parked:** the three-consumer rule is not met (hardcoded constants + name-based matching sufficient for Python 3 + IronPython 2); adding GhPython without a real consumer is speculative. | `~/.claude/.../memory/project_gh_script_routing_campaign.md` (Remaining deferrals) |
| **End-to-end agent-behavior harness for the 2026-04-21 script-routing incident** | test | Low-M | 2026-04-21 | **Any of:** (a) another agent-behavior regression surfaces on GH tool routing / MCP description drift — harness would have caught it; (b) a second incident-class emerges (e.g., different tool family drifting between description and handler capability) that argues for systematic behavioral coverage. **Context:** Codex flagged this on PR #83 review as missing end-to-end coverage; ultimately left unbuilt because 5 PRs of static + live test coverage (~157 tests across contract / persona / retrieval / ranking) plus PR #85's handoff enforcement have been empirically sufficient. Building a harness that can reproduce the original agent misroute would require a reproducible prompt-runner against a real MCP client — non-trivial scope. Revisit only on a concrete trigger. | Codex review of PR #83; `~/.claude/.../memory/project_gh_script_routing_campaign.md` |
| **`style="AlignWithSurface"` support on `/surface/sweep1`** | feature | M+ | 2026-04-21 | **Any of:** (a) RhinoCommon adds a managed wrapper for the Align mode on `SweepOneRail` or `Brep.CreateFromSweep*` (re-run the assembly-wide reflection audit from spike memo §Post-Hoc Finding to confirm); (b) a new native/opennurbs design pass is explicitly opened (separate campaign — abandons managed-bridge-reuse substrate, big architectural shift, needs its own spike + plan); (c) a real user workflow surfaces compelling need sufficient to justify the native C++ / opennurbs path cost. **Context:** Phase 3 PR-2 killed pre-code 2026-04-21 by empirical RhinoCommon audit — zero matches for `AlignWithSurface` / `AlignWith` / `StyleAlign` across all of RhinoCommon. Rhino's `_Sweep1` command UI exposes AlignWithSurface but the opennurbs implementation is command-internal and never got a public managed wrapper. Not a scope wrinkle — hard feasibility stop for the managed-bridge substrate. See `rook_docs/2026-04-21-typed-route-phase3-spike.md` §Post-Hoc Finding for the full audit. Native `SurfaceHandler.cpp:404-421` still rejects the enum; keep that rejection until a trigger fires. | `rook_docs/2026-04-21-typed-route-phase3-spike.md` §Post-Hoc Finding |
| **Theme A — per-primitive MCP tool split (13 `/create` ops → individual `/geometry/*` tools)** | feature | M | 2026-04-21 | **Either:** (a) a user explicitly asks for per-primitive MCP tools; (b) a new primitive type lands in Rhino whose shape doesn't fit `rhino_create`'s discriminator (e.g., parameter space that can't live in a oneOf). **Context:** Parked as a **deliberate non-project** (not a follow-up) per 2026-04-21 gate verdict. `rhino_create` data: 1636 calls / 99.69% success / 0 corrections over 12 days. `rhino_execute_intent` only 9 calls total — agents bypass the intent router for Rhino primitives and go direct. `knowledge/` has 0 `correction_detected: true` across 516 component observations and 108 tracked tools. No user asks. Per-primitive dispatch already exists internally at `intent_runtime.py:233` + `intent_planner.py:238`; MCP surface is a typed discriminator schema at `tool_schemas.py:201`. Splitting would be pure external repackaging — changes catalog shape, not capability or reliability. Current agent guidance (`ONBOARDING_NEW_CLAUDE.md:173`, `prompt_builder.py:14`) pushes `rhino_execute_intent` first and frames structured tools as substrate reliability, not per-primitive specialization. 4-6 PR campaign not warranted without evidence of agent pain. Gate methodology + verdict captured in chat transcript 2026-04-21 (§ "Gate verdict: park Theme A as a deliberate non-project"). | `rook_docs/2026-04-21-typed-route-phase3-spike.md` §Theme A Gate |

---

## Recently shipped

(items move here after merge — keep recent few for context, prune as needed)

| PR | Item | Merged |
|----|------|--------|
| #114 | **RookPlugin lifecycle wrapper source-level regression.** Closed with `RookPluginLifecycleSourceTests`, a managed source-level test class that pins the smallest safe lifecycle-wrapper slice without adding Rhino shutdown fault injection. The tests verify `OnLoad` schedules `TryInitializeRuntime`, video reconcile failure is caught/logged as non-fatal, each `OnShutdown` external teardown call has an independent `try/catch`, and `base.OnShutdown()` remains after/outside those wrappers. The source reader strips comments before matching; a mutation check commented out `ChatServiceManager.Instance.Shutdown()` and the focused class failed on the intended missing-wrapper assertion. | 2026-05-10 |
| #113 | **Native Vision dispatch source-level regression.** Closed with `NativeVisionDispatchSourceTests`, a managed source-level test class that pins the C++ `DispatchVisionOpWithPathId` contract without adding native project-file churn. The tests verify the helper uses `body[path_id_field]`, artifact routes pass `artifact_id`, and video status/cancel/result routes pass `job_id`. A mutation check changed one video route to `artifact_id`; the focused test failed on that route, proving the regression catches the #113 failure mode. | 2026-05-10 |
| release | **Rook v1.5.3 Vision-panel hotfix release.** Released from `main` on 2026-05-10 as tag `v1.5.3` with GitHub asset `Rook-Setup-1.5.3.exe` (~14.3 MB). Ships the RookVision dedicated native Rhino panel host and removes the legacy Vision activation reload path that was unique to the old chat-tab-hosted Vision panel. Native Release build used MSVC/MFC 14.44, managed companion packaged `net7.0`, full managed suite passed 1909/1909, installer source-path/guard checks passed, and local Rhino plugin deployment was refreshed with the release artifacts. | 2026-05-10 |
| #142 | **Rhino runtime harness.** Squash-merged as `035d6f4` on 2026-05-08. Adds the first process-owned Rhino live-test/development harness: starts one Rhino process, waits only for `%TEMP%\rook\instance-{PID}-native.json` matching the owned `Popen.pid`, pings the exact discovered RookNative port, re-checks owned discovery before smoke, runs selected live smoke with `ROOK_RHINO_PORT` + `ROOK_RHINO_PROCESS_ID`, captures `%TEMP%\rook` artifacts and smoke output into `.scratch\rhino-runtime-harness`, and cleans up only the owned Rhino process. Pytest harness mode fails closed on malformed/partial env and preserves ambient `requires_rhino` skip behavior when no harness env is present. `ping-only` provides non-mutating readiness/cleanup diagnostics; `pytest-select` reuses `test_select_additive_live.py`; mutating smoke cleanup saves the owned document into the run artifact folder before `WM_CLOSE` so dirty-doc prompts do not force cleanup. Live verification: `ping-only` passed with `graceful_exit`; `pytest-select` passed all 13 selected live tests with scoped env containing only `ROOK_RHINO_PORT` and `ROOK_RHINO_PROCESS_ID`, `cleanup.status: graceful_exit`, `warnings: []`, and no stale native discovery. | 2026-05-08 |
| release | **Rook v1.5.2 full installer release.** Released from `main` on 2026-05-08 as tag `v1.5.2` with GitHub asset `Rook-Setup-1.5.2.exe` (~14.3 MB). Version metadata was bumped across `mcp_server/pyproject.toml`, `installer/RookSetup.iss`, `src/Rook/Rook.csproj`, `RookNative.rc`, `RookNativePlugin.cpp`, and `RookServer.cpp`. Release build used the pinned native MSVC/MFC 14.44 toolset, managed `net7.0` Release companion, source-path preflight, release installer guards, Issue #112 guards, and Inno Setup. GitHub release URL: `https://github.com/bringfire/Rook/releases/tag/v1.5.2`. | 2026-05-08 |
| direct | **Release installer companion packaging hardening.** Committed on `main` as `a15c4cc` before the 1.5.2 release. Reconciles the Inno installer with the source install/register path: packages `src\Rook\bin\Release\net7.0` companion artifacts including `Rook.deps.json`, `Rook.runtimeconfig.json`, DLLs, and `runtimes\*`; requires `RookNative.rhp` as the public Rhino surface; keeps native PDB optional; writes Rhino plugin `FileName` values under `...\PlugIn\FileName`; updates build-release skills/docs and `BUILDING.md` to PowerShell/net7 guidance; adds `scripts/tests/release-installer-guards.tests.ps1` covering net7 packaging, runtimeconfig TFM, stale net48/bash release guidance, registry shape, and native-required packaging. | 2026-05-08 |
| direct | **Kling v3 Standard I2V video support.** Merged to `main` as `a6bdfc4` after design/spec review, implementation, live smoke, and release deployment checks. Adds curated fal model `fal-ai/kling-video/v3/standard/image-to-video` for `I2V` and `Interp` only, requires prompt, uses provider-private fal source-frame upload, sends `start_image_url`, optional `end_image_url`, selected duration, and fixed `generate_audio: false`, keeps durable fal handles request-id-only with explicit model identity, and preserves no-provider-URL leakage posture. Follow-up smoke fixes split Kling submit vs lifecycle routing to the fal queue root, added transient video artifact fetch retry robustness, and verified live Kling round trips. | 2026-05-08 |
| direct | **Issue #112 net48 companion registration hardening.** Implemented directly on `main` on 2026-05-06. Removes automatic `net48` companion fallback/reuse/success paths from `install.ps1`; source installs now select/build only `src\Rook\bin\Release\net7.0\Rook.rhp`. Release-package `plugin\Rook.rhp` now requires adjacent `Rook.runtimeconfig.json` with `runtimeOptions.tfm == net7.0`. `scripts/register-companion.ps1` now rejects explicit `net48` path segments and framework-less package paths whose runtime metadata is missing, malformed, or not `net7.0`, before any registry write or success output. Added PS5-compatible guard tests in `scripts/tests/issue112-net48-companion-guards.tests.ps1`. Verification passed: guard tests, managed `net7.0` Release build, installer dry-run, forbidden fallback scan, and `git diff --check`. | 2026-05-06 |
| direct | **Knowledge Graph bootstrap hotfix.** Committed directly on `main` as `3b7487f` on 2026-05-06 after live Rhino verification. Fixes the KG panel failure where the frontend emitted "Could not connect to Rook service" after 15 seconds if `PanelShown` missed/lagged service bootstrap. Adds an internal `KnowledgeGraphBootstrapCoordinator`, starts bootstrap from both `PanelShown` and `RookWebSurface.OnWebViewReady`, caches/replays bootstrap scripts until the WebView can accept them, injects explicit service-unavailable failures, and makes `knowledge-graph.js` event/function-driven so late bootstrap can still load `/knowledge/graph`. Tests cover request-before-ready, request-after-ready, service-unavailable messaging, and retry after failed bootstrap; focused WebSurface/Chat/KG tests passed `38/38`; managed `net7.0` Release build/deploy passed; user confirmed the issue was fixed in Rhino. | 2026-05-06 |
| #141 | **PR-19 Seedance source-frame transport.** Merged as `a014f61` on 2026-05-06. Replaces Seedance 2.0 data-URI start/end frame transport with provider-private fal CDN upload (`rest.fal.ai/storage/upload/initiate?storage_type=fal-cdn-v3` + raw PUT + returned `file_url`) before queue submit. Keeps scope managed-provider-private: no native route, MCP tool, public bridge op, ledger schema change, durable source media, or durable/public fal source URL exposure. Upload failures happen before queue submit; source media lifetime is one hour; queue submit disables fal platform retries with `X-Fal-No-Retry: 1`; Seedance durable handles remain request-id-only. Privacy tests pin raw JSONL, artifact metadata, provider/public errors, and handler payloads against `fal.media`, `rest.fal.ai`, `api.fal.ai`, `image_url`, `end_image_url`, and `data:image` leakage. Focused PR-19 suite passed `219/219`, full managed suite passed `1877/1877`, managed `net7.0` Release build/deploy passed, boundary scan found no source-upload exposure, non-generation fal upload smoke passed, and user live Seedance smoke passed. | 2026-05-06 |
| #140 | **PR-18 Seedance 2.0 Image-to-Video.** Merged as `b1aa49f` on 2026-05-06. Adds one curated fal video model, `bytedance/seedance-2.0/image-to-video`, to managed Vision video lifecycle with source-frame-only I2V/Interp workflows, request-id-only durable fal handles, model-aware fal lifecycle reconstruction, sanitized Seedance failure paths, and no dynamic discovery/native/MCP/public HTTP expansion. Live Rhino smoke round-tripped viewport/source-image + prompt through fal Seedance to a local Gallery artifact and found no data URI, source-field, fal queue URL, `status_url`, `response_url`, or `cancel_url` leakage. Smoke fixes split submit vs lifecycle endpoints, force `generate_audio=false`, and add one narrow retry for socket-level connection-establishment timeout before a fal job exists. | 2026-05-06 |
| #139 | **PR-17 fal GPT Image 2 edit source-image workflow.** Merged by squash on 2026-05-05. Adds curated fal.ai `openai/gpt-image-2/edit` as a source-image-only async image job model for Generate and Studio. Uses exactly one primary `input_image_path`, requires a prompt, rejects prompt-only GPT Image 2, rejects `reference_image_paths`, masks, and provider-specific option surfaces, and sends fal `image_urls: [data-uri]`, `image_size: "auto"`, `quality: "high"`, `num_images: 1`, and `output_format: "png"` through the async image job path. Adds model-level image submission mode, model-aware fal queue lifecycle, provider/model restart lookup, request-id-only durable provider handles, bounded source payload validation, MIME sniffing, leakage sanitization, and UI routing/gating for Generate and Studio. Live Rhino smoke: GPT Image 2 edit round-tripped from viewport capture + prompt, the fal result route was corrected from `/response` to the bare request endpoint after live 405 evidence, artifact persisted in Gallery after Rhino restart, and no native/MCP/public HTTP exposure was added. | 2026-05-05 |
| #138 | **PR-16 Flux 2 Pro source-image workflow.** Merged by squash as `0eb5ad7` on 2026-05-04. Adds curated Replicate `black-forest-labs/flux-2-pro` as an async source-image-only model for Generate and Studio. Uses exactly one primary source image from existing RookVision source workflows, bounded in-memory data URI transport, MIME/size validation, `input_images: [data-uri]`, `aspect_ratio: "match_input_image"`, and provider request `resolution: "match_input_image"` after live Replicate 422 smoke exposed the schema nuance. UI routing now sends async source-image jobs through `image_generate_start`; Generate and Studio disable/clear/omit references for zero-reference models; Schnell remains prompt-only async. Leakage tests cover ledger, bridge, artifact metadata, provider errors, and fake HTTP request boundaries. Live Rhino smoke: Flux 2 Pro round-tripped successfully, artifact persisted after restart, and Schnell still works. Planning docs were removed before merge. | 2026-05-04 |
| #137 | **PR-15 image job durability and restart recovery.** Merged by squash as `3344045` on 2026-05-04. Adds operational-only durable image job records via an append-only JSONL ledger, startup reconciliation of previous non-terminal image jobs to `Interrupted`, durable-aware bridge list/status/result/cancel behavior, cancel-after-restart using persisted `provider_job_id` only, and sanitized durable/bridge-facing errors. Durable schema intentionally excludes prompt, source paths, request summaries, provider handles, provider URLs, data URIs, result tokens, and provider metadata. Tests cover ledger compaction/read errors, stale terminal races, append-failure semantics, fake-ledger isolation, restart reconciliation without provider calls, cancel-after-restart, artifact availability, leakage, and closed native/MCP/public HTTP boundaries. | 2026-05-04 |
| #136 | **PR-14 Replicate Settings and visible image model.** Merged by squash as `1359fc8` on 2026-05-03. Exposes Replicate as a managed Vision Settings credential owner, registers the conservative `black-forest-labs/flux-schnell` text-to-image model in default image composition, adds backend-owned `submission_mode`, and routes Generate prompt-only async T2I through bridge-only image job ops. Keeps sync `generate` source-image based; keeps Studio source/edit oriented with T2I-only models incompatible/disabled; keeps native/MCP/public HTTP/`NativeGhBridgeRegistrar` boundaries closed; keeps Replicate key testing non-spend/inconclusive. Verification before merge: focused managed suites, full managed suite 1687/1687, clean diff check, and boundary/leakage scans; senior review reported no requested changes. | 2026-05-03 |
| #135 | **PR-13 hidden Replicate image provider.** Merged by squash on 2026-05-01. Adds a hidden production `ReplicateImageProvider` slice for `black-forest-labs/flux-schnell`, with registration/capabilities/options/pricing/provider code under `src/Rook/Services/Vision/Image/Replicate`. Reuses the existing Replicate prediction substrate for submit/status/cancel/error mapping and authenticated output request construction, but keeps default composition hidden: no Settings card, picker model, native route, MCP tool, or credential-owner metadata. Fake-HTTP coverage proves exact create body, lifecycle mapping (`starting`/`processing`/`succeeded`/`failed`/`canceled`/`data_removed`), single safe `replicate.delivery` output extraction, authenticated materialization through the image-job selector, missing-token/unsafe-URL no-fetch failures, and default registry no-exposure. Verification on PR branch: 1669/1669 managed tests, focused Replicate/image-job/catalog suites, clean diff check, and hidden boundary scans. | 2026-05-01 |
| #134 | **PR-11/PR-12 stacked — Replicate async-image design + hidden image job substrate.** Merged `26ab841`, docs/spec + managed/test substrate. Adds root-owned `ImageJobManager`, managed-bridge-only image job ops, fake-provider sync/async coverage, authenticated materialization request-factory selection, cancellation/materialization race coverage, and result artifact availability checks. Keeps existing `generate` behavior unchanged and preserves the hidden boundary: no Replicate Settings card, picker model, native route, MCP tool, or credential-owner metadata. Verification on PR branch: 1611/1611 managed tests, clean diff check, and hidden boundary scans. | 2026-05-01 |
| #111 | **PR-V2 — video HTTP routes + `RookSubsystemRoot` composition.** Merged `555e196`, 15 files, +3485/-54. Lands the five `/vision/video/*` native HTTP routes (submit/status/cancel/result/estimate) per v3.1 D3 + the matching bridge ops for the Vision tab — both transports go through one shared `VideoJobManager` via the new neutral `RookSubsystemRoot`. ABI v14 unchanged; `JsonlVideoJobLedger` format untouched (V1c byte-identity fixtures still green). HTTP submit/estimate accept `artifact_id` media refs only — `kind:"path"` rejected at `VideoOpHandler.ParseMediaRef` with `field:"kind"` per scope v3 §4 (conservative subset of v3.1 D2.1). Per-error HTTP status mapping by `VideoErrorCode` (InvalidRequest=400, UnsupportedMedia=415, DependencyUnavailable=503, ExecutionFailed=500, Cancelled/Interrupted=200) via additive `ApiResponse.HttpStatus`. Composition root threads same `ArtifactStore` + `VisionSecretStore` through `VisionHandler` AND `VideoOpHandler` on both native trampoline and Vision tab; reflection pins on both sides guard against shared-store drift. `RookPlugin.OnLoad` calls `RookSubsystemRoot.Instance.ReconcileVideoJobsOnce()` (Interlocked-guarded; flag-resets-on-throw); `OnShutdown` disposes video subsystem with each teardown step in its own try/catch. **Scope-pass arc:** v1→v2→v3 in chat (Codex caught path resolver gap, HTTP status plumbing, shared-store violation, dispose-before-build race, schema mismatch on `value`/`artifact_id`); 7 PR-time review rounds across the 8 implementation steps. **Net new tests: ~95** across factory, lifecycle, handler unit + integration, registrar trampoline, web surface, shared-singleton pins. **Rhino-runtime smoke:** all 5 routes verified via curl against the deployed companion — including the C++ `body["job_id"]` injection traversal Codex flagged as the native unit-test gap. **Deploy gotcha caught + captured to memory:** `Rook.rhp` MUST be the `net7.0` build, not `net48` — `RookWebSurface`'s WebView2 init is gated under `#if NET7_0_OR_GREATER`; net48 silently breaks both Vision and KG panels with no diagnostic log. Three follow-up issues filed (net48 fallback hardening, native helper test gap, plugin lifecycle integration test). | 2026-04-25 |
| #110 | **PR-V1c — video registry + codec + per-model pricing.** Merged `d254412`. Refactor closing V1: `IVideoProviderRegistry` resolves model id → `ResolvedVideoModel` with provider/capability/pricing/codec; `DefaultVideoProviderRegistry` enforces build-time invariants (no duplicate model ids, cap.Id matches dictionary key); per-model `IPricingModel` (single concrete `PerSecondPricingModel`) bound at the resolved-model edge for single-pass audit invariant. Provider-specific `ProviderOptions` + `VeoOptions` typed record + `IProviderOptionsCodec` round-trip the persisted `{"person_generation":"..."}` blob. `VeoProvider.SubmitAsync` casts `request.Options as VeoOptions` and returns typed `Fail(InvalidRequest)` on mismatch. Cancel-correctness invariants 6+7 close the BG-task-just-finished race in both directions. Bit-identity fixtures preserve V1b's JSONL byte-identical to V1a/V1b output. | 2026-04-25 |
| #109 | **PR-V1b — video runtime: provider, manager, ledger.** Merged `6f90cd0`. Real `VeoClient` (lifted/adapted from SA_Banana), `VideoJobManager` (in-memory state, wired to companion lifecycle/shutdown CTS), `JsonlVideoJobLedger` (append-only JSONL under `%APPDATA%\Rook\video\job-ledger.jsonl`). Terminal `ArtifactStore` writing on success; `ReconcileInterruptedJobs()` snapshot for non-terminal records on plugin reload per v3.1 D4. Tested with fake HTTP/provider paths and a real-API integration smoke gated behind an env-var. | 2026-04-25 |
| #108 | **PR-V1a — video domain contracts.** Merged `89d551e`. Domain DTOs (artifact_id-based, no base64 transport per D2.1), state/error envelopes, `VideoCapabilities` lift from SA_Banana, cost estimator interface + Veo-pricing impl. Tests for cost-estimator math (incl. fail-closed on unknown models), capability-matrix validation, error-code retryable matrix, media-ref kind/role validation. No HTTP, no real Veo calls, no UI, no `VideoJobManager` yet. | 2026-04-25 |
| #107 | **Vendored Archivo + JetBrains Mono variable fonts** for Vision UI. Merged `00dfcac`. Vendored `.woff2` + uncomment local `@font-face` block. Resolves item (5) of the 2026-04-24 fix-it triage follow-ups. | 2026-04-25 |
| #106 | **Per-image `Show in Folder` action in RookVision Gallery modal.** Merged `ee6d00e`. New `reveal_artifact_file` op (off-UI dispatcher), `RevealArtifactFile` handler in `VisionHandler.cs` using `explorer.exe /select,<canonical-path>` to highlight the specific blob. Path resolution through `ArtifactStore.GetBlobAbsolutePath(id, role)` so traversal rejection is inherited. Resolves item (2) of the 2026-04-24 fix-it triage follow-ups. | 2026-04-25 |
| #102 | **Vision framing polish + WebView lifecycle cleanup + installer target-selection fix** (Codex fast-follow on yesterday's image-track arc). Merged `84ce646`, 16 files, +658/-61. **Three distinct areas bundled:** (a) **Vision framing** — aspect_ratio dropdown expands to 15 Gemini-supported options + `"Auto"` default (internally resolves to null → omit from API call, letting Gemini match source aspect); new `NormalizeAspectRatio(raw)` (`"auto"`/`"current"` → null pass-through; unknown rejected against `AllowedAspectRatios` whitelist: `1:1, 1:4, 4:1, 1:8, 8:1, 2:3, 3:2, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9`); resolution dropdown gains `"512"` gated to nano-banana-2 via model-aware `ValidateResolution(resolution, model)` overload + `SupportedResolutionsForModel(model)`; `GetSettingsOverview` returns `allowed_aspect_ratios` + `default_model_supported_resolutions` + `supported_resolutions_by_model` for per-model UI gating; model catalog gains `supported_resolutions` arrays; `BuildImageConfig(resolution, aspectRatio)` extracted as single source of truth for the API payload. (b) **WebView lifecycle** — `RookWebSurface` now implements `IDisposable`; `_disposed` flag + `IsDisposed` property; disposal guards at every async await boundary (`ConfigureVirtualHost`, `OnDocumentLoaded`, `OnWebMessageReceived`, each `AddScriptToExecuteOnDocumentCreatedAsync` step); init-event handler removal tracked via `_nativeControlWithInitHandler` / `_initEvent` / `_initHandler` and cleared on dispose; `CreateWebContent` throws `ObjectDisposedException` on reuse. `ChatTab` gets `CloseWebSurface()` + `_tabClosed` idempotency guard + `Dispose(bool)` override routing to `OnTabClosed`; `AgentChatTab` / `ClaudeCodeTab` override `OnTabClosed` to call `CloseWebSurface` before subclass teardown; `KnowledgeGraphPanel` disposes surface on `PanelClosing`; `VisionTab.Dispose(bool)` sets `Content = null` before `_surface.Dispose()` so Eto releases the host. `ChatServiceManager` shutdown hardened — background `Rhino.exe` now exits immediately after close (pre-PR stayed alive holding resources). (c) **`install.ps1::Step-BuildCompanion` target-selection fix** — previously preferred `net48` over `net7.0` when both output dirs existed; Rhino 8 requires `net7.0`, so clean-user installs silently deployed the wrong companion and `ShowRookVision` fell back to the minimal HTML. Fix swaps priority in both the already-built and newly-built branches. Local deploy hash check verifies installed `Rook.rhp` matches `src/Rook/bin/Release/net7.0/Rook.rhp`. **Tests: 437 passed** (+36 net new on the 401 baseline): +119 `VisionHandlerTests` for aspect normalization + resolution model-gating + helper coverage; +85 `VisionWebSurfaceTests` for `IDisposable` contract + disposal-guard pinning; +22 `RookWebSurfaceTests` substrate disposal contract. **Consumer-level Eto disposal tests deliberately unpinned** per Codex post-review — headless Eto test harness unavailable, covered by live Rhino smoke instead. Two Codex review rounds: round 1 caught initial WebView lifecycle gap across non-Vision consumers (fixed pre-publish); round 2 no blocking issues, only the Eto testability limitation noted. **Addresses one outstanding yesterday-listed follow-up** (installer target-selection); focus-flash / configurable Gallery folder / multi-viewport capture / CI ABI check remain open. | 2026-04-24 |
| #101 | **RookVision v1 polish — three live-test findings from PR-7b.** (a) **"Active view (live)" sentinel** in Generate/Studio viewport dropdown — first option is empty-value sentinel, selected by default; `captureViewport()` omits `view_name` arg when selected; `ViewportHandler.CaptureTier3` skips the `TrySetStandardView` block entirely, preserving the live camera (orbit/pan/zoom intact). Picking a specific named view still forces the projection reset (useful for canonical angles). (b) **API key preview persistence** — new `VisionSettings.ApiKeyPreview` plaintext-safe field stored alongside the DPAPI ciphertext; `VisionSecretStore.BuildPreview(apiKey)` computes `first4…last4` (publicly-known "AIza" Gemini prefix + 4 trailing chars ≈ 4 chars entropy leak; middle 20+ stays DPAPI-protected); `GetSettingsOverview` returns the preview without decrypting (cheap read on every Settings open); `loadSettingsOverview` sets input placeholder to the preview and includes it in the status-indicator text so Settings visibly proves the key persists across restarts. Legacy `settings.json` (pre-preview field) returns null gracefully, UI falls back to "API key configured" text, next save rebuilds the preview field — no migration step. (c) **Same-view no-reset** in `CaptureTier3` — if requested `view_name` matches current `view.ActiveViewport.Name` (case-insensitive), skip both `TrySetStandardView` and `TryRestoreNamedView`. `SetProjection(DefinedViewportProjection.Perspective, null, true)` was unconditionally snapping the camera to Rhino's hard-coded default orientation even when already in that projection, throwing away user's orbit. Collapses "pick Perspective while in Perspective" into "Active view (live)" behavior. User-reported symptom: *"it jumps to some other view that I don't even have saved and doesn't exist."* Agent-path impact noted: agents previously calling `/viewport?view=Perspective` while already in Perspective received a reset canonical shot; now they receive the current camera. Acceptable semantic improvement (explicit reset is better served by a dedicated token or prior Rhino command). **Tests:** +10 covering viewport sentinel behavior + API key preview persistence (stored on set, persisted in settings.json, short-key asterisk fallback, cleared on Clear, rebuild on rotation, legacy-file compat). 401/401 green (+10 from 391 baseline). Live-verified end-to-end post-merge. Three commits on branch, rebased onto main+#100 before merge for clean integration. | 2026-04-23 |
| #100 | **`RookPlugin` startup retry-storm fix — Codex-diagnosed concurrency hazard.** Orthogonal to Vision; lives entirely in `RookPlugin.cs`. Discovered during PR-7b integration smoke when deployed `RookNative.rhp` predated ABI v14 (from PR #95's `VisionDispatch` callback addition); `NativeGhBridgeRegistrar.TryRegister` failed repeatedly; the pre-fix periodic 250ms timer + UI-thread re-entry saturated Rhino's UI thread and froze the app at startup (required force-quit). **Fix trifecta:** (a) timer rebuilt as one-shot — constructed with `Timeout.Infinite, Timeout.Infinite` initial args; rearmed via `Timer.Change(interval, Timeout.Infinite)` only at the end of a completed `TryInitializeRuntime`; (b) new `ScheduleNextStartupRetry()` method gated by new `_startupRetriesActive` bool — `StopStartupRetries` flips the gate first, so any in-flight callback that reaches the scheduling path becomes a no-op rather than racing to rearm a disposed timer; (c) `_startupInitInProgress` (via `Interlocked.Exchange`) in-flight guard — prevents `TryInitializeRuntime` from overlapping with itself when both the Idle hook and the timer post to the UI thread. **Retries still exhaust at `StartupRetryLimit`, but now strictly serial + stoppable** — cumulative work no longer blocks Rhino's own startup. Verified live: matched-ABI native → single "GH bridge registered — companion startup complete" line in `%TEMP%\rook\companion-startup.log`, no storm; deliberately mismatched native → retries exhaust cleanly, Rhino stays responsive, companion falls through to "bridge not available" gracefully. `dotnet build` clean both net48 + net7.0 targets; no test changes (the fix is a lifecycle shape change, not a contract change; the user-facing effect — freeze vs. graceful degradation — is only verifiable in a live Rhino session, which PR-7b integration smoke provided). Credit: diagnosis + patch by Codex during PR-7b integration smoke. | 2026-04-23 |
| #99 | **VisionTab UI — Pattern A WebUI surface (RookVision image-track closeout, PR-7b).** Final slice of the 11-PR SA_Banana → RookVision image-track arc (#90-#99, extended via #100+#101), rehousing SA_Banana's image-only WebUI as a Pattern A Rook tab. **4 views (Generate / Studio / Gallery / Settings)**; video deliberately absent at markup + state + handler + CSS + copy levels (grep-sweep for `veo|video|motion` returns only deliberate-absence comments). JS↔C# bridge is the sole transport — no `fetch`, no XHR, CSP override enforces `connect-src 'none'`. **Architectural highlights:** `VisionWebSurface` extends `RookWebSurface` with explicit op-allowlist (14 ops × correct route dispatch, no default-to-offUi fallback — unknown ops return structured failure, not dispatcher mis-routing); 180s async-op timeout matching native `vision_dispatch` trampoline, via pure-static `DispatchWithTimeoutAsync` helper (cancellation-to-timeout rewrite isolated for unit-testability); `/blob/{artifact_id}/{role}` virtual resource resolver delegating path validation to `ArtifactStore.GetBlobAbsolutePath` (canonicalization + directory-escape rejection inherited). **6 new UI-only ops** (`set_api_key`, `test_api_key`, `get_settings_overview`, `capture_viewport`, `list_views`, `open_image_picker`) — native `HandleVisionDispatch` allowlist rejects them by default, enforcing JS-bridge-only containment (agents cannot drive a file-picker dialog or plant an API key via the HTTP surface). **Model picker** exposes paid-only `nano-banana-2` / `nano-banana-pro` via short-name→full-Gemini-ID mapping in `GeminiClient.Models.ResolveShortName` (passes unknown values through so power users and agents can target new Google models ahead of Rook catching up); free-tier `gemini-2.5-flash-image` constant deleted — API keys can't invoke free-tier endpoints, listing it would only generate 429s. **`enhance_prompt` surfaces enhanced text in artifact metadata** rather than blob only — CSP `connect-src 'none'` blocks cross-origin reads from the virtual host, so the UI reads the text directly from the bridge response envelope without a secondary fetch. `VisionTab` is a bare Eto `Panel` (NOT `ChatTab` — no streaming lifecycle, no Send/Stop/Clear controls) hosted inside the chat panel's tab strip via `RookChatPanel.AddPanelTab` + `TabCleanupRegistry` (both shipped in PR-7a/#98). Exposed via `/ShowRookVision` command (picker-path intentionally unwired per v1 decision; command-based entry reviewed + accepted by Codex). **401 tests** cover CSP pin, op-routing table (14 ops × correct dispatcher enum), `PeekOp` JSON parsing, blob-URI parsing (valid shape + invalid segments + traversal attempts + uppercase role rejection), content-type guesser, `ApiResponse` round-trip, virtual-resource happy/404 paths, async-op timeout behavior (happy + observed cancellation + swallowed cancellation rewrite + non-timeout failure + non-cancellation exception propagation), model catalog shape + paid-only regression, short-name→full-ID mapping for known + unknown values. Three Codex review rounds; findings landed pre-merge covered cancellation wiring, Studio `capture_depth` integration, external-link removal (Google AI Studio link converted to plain text — top-level navigation would break the WebView), and overstated timeout coverage (behavioral tests added, not just constant pin). Three commits squashed on merge. **Closes the parity rule for the image track.** | 2026-04-23 |
| #89 | **First exotic-capability promotion: `POST /block/distribute-along-curve`.** Native direct-SDK route promoted from the user-authored script `DistributeBlocksAlongCurve.py`. Three distribution methods (`fill` / `fixedSpacing` / `fixedCount`), seeded randomness for deterministic replay, follow-curve/world orientation with `keepUpright` guard covering both XY-projected and full-3D frame branches, atomic mid-loop rollback with `rollback_failed` classification when cleanup is incomplete, plural-creator cardinality, `span_exceeds_curve` rejection when `fixedCount` requested span exceeds curve length (prevents silent under-delivery). Sibling to `HandleBlockArrayInstances` in `BlocksHandler.cpp`; no new handler file (deferred until second placement route lands). Env-gated `/block/_debug/fail-next-instance` seam (`ROOK_ENABLE_DEBUG_ROUTES=1`) for rollback testing. Attribute composition deliberately out of scope — chain via `rhino_block_set_instance_properties`. 6 files: handler header + implementation, route registration in `RookServer.cpp`, MCP tool in `server.py`, CapabilityRouter entry folded into existing `CATEGORIES["blocks"]` tuple, live test file. Live coverage: 31/31 passing — 12 contract tests (all mode-validation rejections + `span_exceeds_curve` × 3 placements + bogus-UUID + empty-pool), 18 integration tests (seed-determinism across all modes + `keepUpright=true/false` bbox differentiation on 45° inclined curve + vertical-tangent warning + zero-length curve + rollback via debug seam + missing-name warning + placement variants), 1 DSPy operation-recognition smoke test (≥80% hit rate on 10 natural-language prompts). Three rounds of Codex review caught: (1) GH sparse index misidentified as Rhino intent surface (it's deprecated + GH-only); (2) `RouteSpec.description` not actually on the DSPy fast path (operation-key quality is); (3) `keepUpright` accepted-but-ignored under `orientation="world"` — rejected with `invalid_input`, same footgun doctrine as the removed `preview`; (4) original `keepUpright` implementation ignored the flag entirely in the transform branch — fixed with parametrized positive-path bbox test; (5) `ON_Curve::GetLength()` wrong signature (needs out-param per repo convention); (6) `rollback_failed` advertised but never detected — `RollbackOutcome {attempted, deleted}` now surfaces partial rollback; (7) `fixedCount` silently truncating when requested span exceeds curve length — `span_exceeds_curve` check on UI thread. **First exemplar under the exotic-capability promotion doctrine**; future script-originated capability work reuses the same pipeline (Stage 0-6). Doctrine: `rook_docs/2026-04-22-exotic-capability-promotion-plan.md`; scope: `rook_docs/2026-04-22-exotic-capability-pr1-scope.md`. Source script preserved as frozen reference in `rook_docs/script-reference/promoted/DistributeBlocksAlongCurve.py`. Shipped in parallel to the substrate-observation hold window — different lane, did not consume hold capacity. | 2026-04-22 |
| #88 | Persist substrate observations to `knowledge/substrate_observations.jsonl`. **Spawned from the Theme A gate verdict** (see `## Last triaged` note at top) — gate ran 2026-04-21, found zero primitive pain signal, parked Theme A as a deliberate non-project, AND exposed that `substrate_analytics` produced only session-scoped summaries with no on-disk corpus. This PR closes the persistence gap before any future typed-route campaign selection needs historical hotspot data. **Four capture sites, four decoupling invariants:** `BaseAgent._record_observation()` (direct-bridge agents; persistence hoisted above the metrics `observation_recording` + infra-availability early returns so substrate writes survive both off-states), `chat_runner` at the existing `_obs` append site (uses `conversation.id` as `session_id`), `server.py::_record_observation()` (MCP-direct path; substrate now lives in a sibling try/except so metrics-store exceptions cannot silently drop substrate telemetry). **Single top-of-function `_is_handoff` gate** in `server.py` covers both metrics + substrate paths — two Codex rounds needed here: round 1 had substrate guarded only via a dead `not result.get("_is_handoff")` check inside the substrate block that was never reached because the upstream metrics-try early-returned from the entire function first (Finding: "comment claimed a guard that was dead code; test passed vacuously"); round 2 hoisted the handoff check to explicit top-of-function gate + renamed `test_server_substrate_skips_handoff_results` → `test_server_handoff_gate_skips_both_paths` and strengthened it to assert BOTH metrics.record() and persist spy are untouched, proving one gate covers both paths. **Schema:** `{timestamp, session_id, tool, operation, route_taken, success, verified, error}` per line. Best-effort write (per-write flush, no fsync, never raises — logs `warning` on I/O failure). **`_compact_error` fallback posture** (Codex's implementation note): top-level `result["error"]` → nested `data["error"]` → stringified `data` → empty on success; truncates to 120 chars. Minimal `load_substrate_observations_jsonl()` reader added for test ergonomics (10 lines, skips malformed JSON lines, returns empty on missing file). **Tests (16 new):** schema / append / round-trip / session_id fallback to task_id / unwritable-path swallow / missing-file loader / malformed-line skip / all 4 `_compact_error` branches + truncation / metrics-disabled substrate survival pin (Finding #1 regression) / metrics-raising substrate survival pin (Finding #2 regression) / handoff single-gate skip pin (Finding #3 regression). 20/20 substrate_analytics + 45/45 intent_runtime + 1382/47/41 full non-live suite (same pre-existing baseline as main — zero regressions introduced). **Two-round Codex review;** methodology captured: decoupling from adjacent telemetry paths is the invariant to pin, not just the new additive path. **Next action on this workstream:** intentional 1-week hold for `substrate_observations.jsonl` to accumulate real hotspot data; no substrate-promotion campaign until log has signal. Revisit on/after 2026-04-28. | 2026-04-21 |
| #87 | Python 3 modern-over-IronPython 2 legacy resolver preference. **Follow-up to #86 (C# analogue)** — closes the Python half of the 2026-04-21 script-routing campaign's behavioral goal. Pre-PR, IronPython 2 Script (legacy) ranked ABOVE Python 3 Script (modern) on every generic Python intent via UnifiedStore retrieval because the legacy note's `trigger_intents` claimed bare `"python"`. Didn't break live because DSPy's prompt selection masked the retrieval bug — fragile, not structural. Three-layer fix symmetric-in-shape to #86 but scoped separately per Codex (PR-86 explicitly pinned Python unchanged; flipping that guard deserved its own scope pass, not a symmetric ship). **L1 narrowing (`comp_a1655c21.json`):** removed bare `"python"` + `"iron"` from trigger_intents; added `"ironpython"`, `"ipy"`, `"python2"` (no-space, indexable single token); added `"legacy"` + `"gh1"` to keywords. **Spike-time correction:** initial draft used multi-word triggers (`"python 2"`, `"legacy python"`) which `add_note` splits on whitespace → re-indexed bare `"python"` into IronPython 2's bucket, defeating the narrowing. Swapped to compact single-word forms. **L2 `python_script` concept bucket** added alongside existing `csharp_script` (same `"script" in tokens` gate). GhPython OUT OF SCOPE (no component note exists; only a recipe reference). **L3 Python preference rule:** default modern, legacy cues = substring (`ironpython`, `python2`, `ipy`, `legacy`, `gh1`) + word-bounded (` python 2 `, ` py2 `, ` iron python `). Codex finding added `python2` (no-space) + ` iron python ` (spaced) to cover realistic legacy spellings. **Test surface:** DELETED `test_concept_key_python_variants_unchanged` (PR-86 guardrail, intentional flip) + orphaned `_GHPYTHON`/`_IRONPYTHON` fixtures. NEW `test_dspy_resolver_python_ranking.py` (15 unit tests) + `test_unified_store_python_retrieval.py` (9 integration tests). Updated PR-86 C# test file docstring to point at the Python follow-up. 157/157 green (118 campaign + 9 C# unit after deletion + 15 Python unit + 6 C# integration + 9 Python integration). Python-only changes; scope containment held. **Campaign behavioral goal now fully realized for both languages.** | 2026-04-21 |
| #86 | Modern RhinoCode C# Script resolver visibility + preference. **Closes the parked follow-up from the GH script-routing campaign** — live validation after PR #85 exposed `"create a C# script"` resolving to legacy `ComponentLegacyCsScript` instead of modern RhinoCode C# Script, so PR-3's handoff (narrow-match by design) didn't fire for C# intents. Three-layer fix centered on retrieval + deterministic ranking, not DSPy prompt tuning or post-selection rewrite. **Scope-pass correction landed pre-implementation:** original plan pointed at `sparse_index.json`; Codex caught that the load-bearing retrieval path is `UnifiedStore.resolve_components()` reading `knowledge/gh/notes/`, not sparse index. Replanned against the correct surface. **L1 knowledge-notes:** new `comp_21019ccf.json` for modern RhinoCode C# Script (`b6ba1144-...`) mirroring Python 3 note shape with indexable keywords (`csharp`, `csharp script`, `rhinocode`, etc. — not `c#` which the tokenizer strips); narrowed legacy note (`comp_e953e26a.json`) — removed non-indexable `c#` keyword, added `legacy` / `gh1` first-class cues. **L2 `_component_concept_key`:** Python 3 + IronPython 2 + VB explicitly guarded OUT of `csharp_script` bucket. **L3 `_select_preferred_candidate_for_concept`:** pre-sort branch for `csharp_script` — default modern, legacy cues = substring (`legacy`, `dotnet`, `gh1`) + word-bounded ` net ` (padded to avoid `internet`/`network` false positives). **Sparse-index updates deliberately deferred** — optional backfill only. Codex sign-off on note-id convention: use any unused `comp_<8hex>`, project does NOT derive from GUID (per `knowledge_note.py`). Empirical spike pinned baseline (modern absent; `"csharp"` empty) + post-L1 state (modern rank 2, legacy rank 4 for generic intent; `"rhinocode c#"` resolves uniquely to modern; `"legacy C# script"` routes legacy first). **Tests:** 10 resolver unit + 6 UnifiedStore integration + 2 positive live handoff tests (C# + Python symmetric). 134/134 green. Python-only changes. | 2026-04-21 |
| #85 | `gh_execute_intent` handoff enforcement on script-component intents (GH script-routing design-pass PR-3). **Closes the behavioral loop** opened by PR-1 (description) and PR-2 (unified tool). Post-resolution boundary in the `gh_execute_intent` case arm refuses to create modern RhinoCode script components via `/gh/create-component`; returns structured `handoff_required` response pointing at `gh_create_script(language=...)`. Python-only, zero C++/C# changes. **Control-flow via `_GhScriptHandoffRequired` custom exception** caught at the existing try/except boundary — keeps the 500-line batch-execution block unindented and avoids tail-variable-undefined fallout. **Hardcoded Python constant** (`_GH_SCRIPT_COMPONENT_CREATION_MAP`) keyed on the two fixed RhinoCode GUIDs; no knowledge-store tagging. Memo proposed tagging, but the hardcoded constant is the entire truth for the two fixed GUIDs with no second consumer today; deferred per scope-pass. **Codex pre-review findings landed pre-merge:** (1) `_mcp_tool_executor` was wrapping `Error: ...` text as success, misrepresenting handoffs — fixed by parsing `Error: <json>` payloads into `{"success": False, "data": <dict>}`. (2) `_record_observation` ran unconditionally at call_tool's tail, recording handoffs as failed intents — fixed by `_is_handoff` sentinel (underscore-prefix convention matching `_metrics_extra`/`_injection_meta`); skipped in observation recording; popped before response serialization. (3) `call_tool` failure-text formatter now JSON-serializes dict `data` payloads as `Error: {<json>}`. **Tests:** 9 new contract + 1 live non-handoff regression floor. Positive live handoff coverage deferred for C# because of the resolver-ranking gap (now closed by #86) — live Python handoff added in #86's live test file. 118/118 green. | 2026-04-21 |
| #84 | Unified `gh_create_script(language=...)` MCP tool with back-compat aliases (GH script-routing design-pass PR-2). Collapses sibling-tool decision axis onto a discriminator param per the `/curve/boolean` Phase 2 precedent. `language` required at the API boundary via `inputSchema` (`enum: ["python", "csharp"]` + `"required": ["language", "code"]`) AND redundantly in the helper — schema-bypassing callers still get a structured `invalid_input` naming both choices. Aliases `gh_create_python_script` / `gh_create_csharp_script` preserved: response payload bytewise-identical, exception-prefix preserved via `tool_name` kwarg threading, session-history action names preserved for audit continuity. **Scope correction pre-implementation:** original plan included `scriptLanguage`/`scriptRuntime` response annotation on `gh_snapshot` — Codex caught as scope creep (no PR-2-internal consumer; `gh_component` is an internal HTTP route not a user-facing MCP tool); deferred to PR-3 if needed (ultimately unnecessary — handoff used hardcoded constants). **Codex review-round fixes:** (1) `gh_create_script.inputSchema.required` was over-constrained with `pins_in`/`pins_out` (helper only requires at-least-one-non-empty); relaxed to `["language", "code"]` only, negative assertions added to prevent drift. (2) `gh_execute_intent` description still pointed at the pre-PR-2 aliases; updated to name `gh_create_script(language=...)` as primary with aliases marked back-compat. (3) Alias error-prefix preservation added via `tool_name` kwarg + 3 new failure-path tests. **Tests:** 9 new contract + 3 live end-to-end + PR-1 persona-test extended. 109/109 green. Python-only, zero C++/C#. | 2026-04-21 |
| #83 | Contract hygiene for script-component routing (GH script-routing design-pass PR-1). **Zero handler-side changes** (`grep -E '\.(cs\|cpp\|h)$'` of diff returned 0 files). Closes the 2026-04-21 live agent-failure incident where an agent pivoted to `rhino_execute` reflection when trying to read a C# Script component's source — because `gh_set_script`'s MCP description declared Py3-only when the handler actually duck-types 4 component types. **7 agent-facing surfaces rewritten across 4 categories:** (a) MCP descriptions — `gh_set_script` removes 3 categorical Py3-only claims + adds `rhino_execute`-is-wrong-substrate framing (Codex correction: substrate-correctness, not impossibility — `_-RunPythonScript` CAN reach GH but result is unstructured/unsupported/bypasses capability detection); `gh_execute_intent` removes false *"This is the ONLY tool for creating GH components"* claim, **current-state guidance only** (no PR-3 forward-anchoring per Codex correction). (b) Runtime personas (`prompt_builder.py:63-67` builds from `personas/<name>/role.md`, NOT from `prompts/WORKER.md`) — `personas/worker/role.md` + `personas/architect/role.md` + `personas/scripter/role.md` all rewritten; scripter fully restructured with §Language Decision (4-rule disambiguation) + §Forbidden Paths + C# patterns branch added alongside existing Python; architect preserved existing correct `"Do NOT use rhino_execute for GH"` rule at :90. (c) Display snapshot — `prompts/WORKER.md` mirrored for consistency. (d) Tests — 2 MCP-description regressions in `test_server_contract_hardening.py` + 1 parameterized persona-prompt regression in `test_chat_prompt_builder.py` (worker × architect × scripter) **caught a hidden third drift site at `worker/role.md:83` on first run** that pre-PR grep had missed, validating Codex's correction to add prompt-level testing. 5/5 new regressions + 45/45 intent_runtime + 100 pre-existing = all green. Single-round Codex review, no findings. **Codex residual note:** no end-to-end agent-behavior harness reproducing the original incident yet; static-test anchors + eventual PR-3 handoff enforcement are the floor. Campaign 1/3 complete; PR-2 (unified `gh_create_script`) next. | 2026-04-21 |
| #82 | Detect curve-crosses-axis in `/surface/revolve` (Phase 3 PR-1). **Closes the first Phase 1 `DEFERRED PLAN-CODE`** (`curve_intersects_axis`). Empirically validated that `RevSurface.Create` does NOT classify curve-crosses-axis as factory failure — the factory accepts the input and produces invalid-but-successful geometry with `success: true` (Probe 1 Case 1.2 on 2026-04-21). New check is the only diagnostic signal the caller receives, not a fallthrough replacement. Detection: `Intersection.CurveLine(curve, axis, tol, tol)` with `tol = doc.ModelAbsoluteTolerance * 10`; event classification via `ParameterA` vs `curve.Domain.{Min,Max}` using `RhinoMath.ZeroTolerance` — parameter-space precision, NOT intersection tolerance (Codex pre-implementation correction prevented near-endpoint dead zones). Tolerance choice validated via 6-case characterization probe before PR open; 4 Probe-1 cases + 2 new (arc endpoint on axis, polyline crossing twice) — all pass, no fallback needed. Managed-only (`CreateHandler.cs::CreateRevolveStrict`); native pointer comment at `SurfaceHandler.cpp:557` updated in lockstep. Live tests: 3 new in `test_revolve_live.py` — interior-crossing rejection (Probe 1.2 promoted to regression), polyline-first-hit-wins semantics (pins loop exits on first interior event), endpoint-on-axis floor (guards against tolerance-too-loose). Scope containment held exactly per plan doc §non-goals: no native schema change, no new route, no new MCP tool, no ABI bump, no `BRIDGE_ROUTES`/`HTTP_MAPPINGS`/CapabilityRouter changes, no legacy `CreateRevolve` change, no `tolerance` schema exposure, no polycurve-structure-aware detection. 6/6 probe + 19/19 live + 45/45 intent_runtime = 70/70 green. Single-round Codex review, no findings. Phase 3 1/2 complete. | 2026-04-21 |
| #81 | Emit runtime warning when preamble cast falls back to `cast(0)` (closes #50). **Closes the 4-item script-pin sprint cluster** (#38 → #39 → #41 → #50, all shipped 2026-04-21). Zero-behavior-change observability add — `_cast(0)` substitution identical to pre-#50; only the side-effect of a Warning-level `AddRuntimeMessage` is new. New shared preamble helper `_gh_cast_warn(_i, _cast, _v)` wrapped in try/except so emission can never break a solve (import failure, missing GH enum, etc. all swallowed silently). Each of the three extractors (`_gh_extract_one`/`_list`/`_tree`) gets a per-call `_warned = False` flag — first cast failure emits + sets, subsequent failures in the same call silently substitute. A 1000-item list with 500 bad casts → 1 warning, not 500. Tests: 4 string-inspection (item/list/tree warn-hook present + non-numeric pin has no invocation via no-cast-arg) + 3 runtime-exec (extends existing `_MockComponent` + `_MockGhEnv` framework with `AddRuntimeMessage` capture + `sys.modules["Grasshopper"]` stub): list first-failure-only with 3 bad values, item single-value warning, defensive import-failure survival. Positive tests use `float` per Codex scope-pass (`bool(...)` too permissive to exercise cast-failure path reliably). Codex residual testing gap: tree-path warning branch is string-inspection-only, not runtime-exec — same hook pattern as item/list, coverage-equivalent via symmetric code path. 25 pre-existing preamble/runtime tests unchanged as the behavior-preservation proof. 46/46 contract + 45/45 intent_runtime = 91/91 green. Single-round Codex review, no findings. | 2026-04-21 |
| #80 | `gh_set_script_pins` coverage + outputs-recipient live case (closes #41). **Test-only; no source changes.** Five Python-layer mock contract tests in [test_server_contract_hardening.py](mcp_server/tests/test_server_contract_hardening.py): (1) invalid-access rejection via `output_updates` with corrected "one /gh/component read + no /gh/script-params write" invariant per Codex scope-pass (not "no HTTP round-trip"); (2) `current_name` nick-collision pinning first-match-wins-on-name-or-nick — tightening to name-only or collision-error reserved for separate behavior-change PR; (3) multi-rename — two `{current_name, name}` entries round-trip; (4) no-op rename (`current_name == name`) — payload still carries `current_name` (C# uses it as wire re-attachment lookup key at GrasshopperHandler.cs:1262-1265; dropping would silently break reconnection); (5) typo `current_name` graceful error. Plus one live test closing Codex's PR #79 residual-risk: `_setup_wired_script_with_downstream_recipient()` wires script.Result→panel, invalid `outputs` payload asserts recipient-side signature survived symmetric to 11 input-side tests. Helper tweak: `_wire` signature extended with optional `source_param` + `target_param` keyword-only (multi-output disambiguation). Single-round Codex review, no findings. 39/39 contract + 45/45 intent_runtime + 12/12 live = 96/96 green. **Script-pin cluster 3/4 complete** — only #50 remains. | 2026-04-21 |
| #79 | Pre-validate `/gh/script-params` before component mutation (closes #39). Hoists all user-input-driven JSON validation ahead of the unregister→reregister loops in [`ScriptParams`](src/Rook/Handlers/GrasshopperHandler.cs#L1164). Previously, invalid JSON shapes (non-array `inputs`/`outputs`) or non-string field values (`name`, `access`, `nick`, `current_name`, `description`) threw mid-rebuild AFTER the component was stripped of inputs+outputs, leaving it wedged with no rollback. Not reachable via Rook clients (Python `_normalize_gh_script_pin_access` rejects upfront); reachable via any direct-HTTP caller. Fix: new `ValidateScriptPinDef` helper + root-level shape guards pre-unregister. Preserves exact pre-existing error text for `TryApplyScriptPinDefinition`'s three original branches; new branches carry direction + idx context. `TryApplyScriptPinDefinition` inline checks left in place as defense-in-depth for future callers. **Three progressive Codex scope-pass rounds (v1→v2→v3)** widened the surface from "3 TryApply branches" to full destructive-path coverage (pin `name`/`nick`/`current_name`/`access`/`description` + root `guid`/`nick`/`inputs`/`outputs`). **Three empirical-probe findings during live test authoring, all test-harness only:** (1) `gh_create_python_script` returns `component_guid`, not `guid`; (2) `gh_connect` is NOT a registered MCP tool — wiring uses raw POST /gh/connect (GrasshopperHandler.cs:5193); (3) **`/gh/connections` JSON is camelCase, not PascalCase** — ASP.NET's default `JsonNamingPolicy.CamelCase` silently lowercases C# anonymous-type properties in transit. Both original test code and Codex static review assumed PascalCase from the C# source; without correction every rejection test would have passed blindly on empty `Inputs` — the exact false-positive mode Codex's Finding #1 warned about pre-implementation. Live-test authoring was load-bearing. **Non-goals:** SDK-driven `CreateParameter returned null` paths still bail mid-flight (Option B deferred); root `description` skipped (already ValueKind-guarded; silent-ignore → explicit-reject reserved for future PR). **Codex residual-risk:** invalid `outputs` payloads covered structurally by pre-validate loop but lack dedicated live case with downstream recipient — captured as in-scope extension of #41. 11/11 live tests + 45/45 intent_runtime = 56/56 green (98.84s live). | 2026-04-21 |
| #78 | Forward declared pin `type` to `/gh/script-params` payload (closes #38). 1-line tuple-element addition at `_gh_script_pin_defs_to_payload` ([mcp_server/src/rook/server.py:601](mcp_server/src/rook/server.py#L601)) + 4-line forward-compat comment. Field was already normalized on input, merged via `_merge_gh_script_pin_metadata`, and reconstructed from `/gh/component` reads via `_gh_component_params_to_script_pin_defs` — only the write-out hop dropped it. Round-trip (read via `/gh/component` → edit → send via `gh_set_script_pins`) now preserves declared pin type instead of losing it. C# side unchanged — `TryApplyScriptPinDefinition` at `GrasshopperHandler.cs:1024` reads only known properties via `TryGetProperty`, so the additive JSON field is a safe no-op there; future consumers (RhinoCode param subclass selection, Chirp metadata cache) can opt in. Test deltas: 3 rich-pin-creation `assert payload == {...}` expectations updated with the now-forwarded `type` field (csharp + python + chirp); existing `test_gh_set_script_pins_merges_existing_component_params` extended with `typeName:"Curve"` + `typeName:"Brep"` in the component fixture + corresponding `type` assertions in the forwarded payload — covers the second acceptance bullet (round-trip preservation) with less churn than a separate test per Codex scope-pass adjustment. Contract hardening 34/34 + intent runtime 45/45 = 79/79 green locally; Codex verified both suites independently. Script-pin cluster opener; next up #39. | 2026-04-21 |
| #77 | `rhino_block_reset_scale_batch` + **6-handler envelope-shape hygiene bundled** (closes #9 + Parked envelope-hygiene entry). Native feature (direct-sdk, no ABI bump); managed hygiene slice (6 `TryGetProperty`-on-non-object guards in BlocksHandler.cs with exact `"Request body must be a JSON object"` assertion per Codex). Schema `{ids:[uuid-str,...]}`; response `{modifiedCount, instances:[...]}` with request-order preserved; errors match HandleBlockReplaceInstanceBatch (`invalid_id` / `not_found` / `not_instance` / `recreate_failed`). GUID-preservation contract pinned (`oldInstanceId == newInstanceId` per successful entry; duplicate id → both succeed). Three Codex review rounds landed pre-merge: (1) rotation-preservation — both single + batch paths extracted only translation from oldXform, silently zeroing rotation; fixed via new `BuildResetScaleXform` column-length decomposition helper applied to both routes (backfilled the pre-existing single-instance bug to avoid diverging route behavior); (2) `DeleteObject` return value unchecked — both routes now fail before recreating rather than allowing duplicate instances + success reports with wrong oldInstanceId (stronger-recreate-semantics per BlocksHandler.cpp:657); (3) empty `ids:[]` not a true no-op — batch now short-circuits inside dispatcher lambda before UndoScope construction + Redraw, matching UserTextHandler.cpp:710 rhythm. Test pin for #3 measures `NextUndoRecordSerialNumber` delta (empirical finding: `UndoRecordCount` does NOT exist on RhinoDoc in RhinoCommon 8). Explorer/registry HTTP_MAPPINGS backfilled for both single (pre-existing gap) + batch. 14 batch live + 12 envelope-hygiene (6 routes × 2 non-object body types) + 24 block-family regression + 45 intent_runtime = 95/96 green (1 pre-existing skip). | 2026-04-20 |
| #76 | `/usertext/object-delete` + `/usertext/document-delete` typed routes. Closes the Phase 2 usertext family — PR-9 (object set/get) + PR-10 (document set/get + reserved-prefix denylist) forward-referenced these routes in their empty-string-rejection messages; the forward-references now point at shipped surfaces. Schema `{keys: [string, ...]}` (+ `id` for object) — mirrors BlocksHandler delete convention; unambiguous vs set routes' `userStrings: {k:v}`. Delete sentinel: empty string at both levels (attribute + document; both characterized in-tree before this PR per test_usertext_*_live.py). `deletedKeys` derived from pre/post state diff — no dependency on SDK return-value semantics, which made the PR self-contained on already-characterized behavior. Duplicates silently deduplicated preserving **first-seen caller order** (Codex scope-pass rejected `std::set` alphabetization — audit-field contract is caller-order). Idempotent (absent key → not in deletedKeys, success). Empty `keys: []` = idempotent no-op. Reserved-prefix denylist gates doc-delete writes; error message anchors **`/block/user-strings with action=delete`** (Codex scope-pass: action hint load-bearing, not just route name). Wholesale rejection with sneak-delete guard test. Codex first review round landed 3 findings pre-PR-open (fixed in first commit): (a) survived-existing-key failure detection — requested key present pre AND post-mutation surfaces `operation_failed`; (b) purge helper replaced with proven `StringTable.GetKey(i)` pattern + asserts `rhino_execute` result; (c) stale "delete not shipped" wording cleared from PR-9/PR-10 set-route MCP tool descriptions + RouteSpecs. Second Codex review: clean sign-off. 19/19 delete + 35/35 set/get regression + 105/105 unit (intent_runtime + Phase 2 audit). Phase 2 sibling audit extended with 2 delete intents — all 6 usertext verbs audit-covered. | 2026-04-20 |
| #75 | `/curve/boolean` typed route (Phase 2 surface/curve PR-4 — **campaign close**). Three intent keys (`curve_boolean_{union,difference,intersection}`) → one `/curve/boolean` endpoint via `operation` discriminator; mirrors Phase 1 brep-boolean pattern at `intent_runtime.py:615`. Plural-contract route on managed-bridge (reuse) substrate; zero ABI bump. **Native helper promotion bundled**: `EmitNormalized` + `DispatchToManagedCreate` moved from SurfaceHandler.cpp anon namespace to new `Infrastructure/ManagedCreateDispatch.{h,cpp}` (third-caller rule fires on PR-4); BlendCurves local-mirror tail (~65 LOC) and SurfaceHandler anon block (~95 LOC) collapsed — net −60 LOC source. New plural-curve helper `InsertCurvesAsPluralResponse` parallel to Brep helper (generalization deferred to third-caller). **Four empirical-probe findings** deviated from plan-doc: (1) factory has NO `out int error` — binary failure = `Length==0`; dropped plan-doc's `non_planar_input` code in favor of collapsed `operation_failed` (PR-3 precedent); (2) `tolerance<=0` NOT silently coerced; worker-thread rejects as `invalid_input` for diagnostic clarity; (3) difference `(curveA, IEnumerable<subtractors>, tol)` overload exists — deferred; PR-4 ships exactly-2 pair form; (4) nested union (outer+inner→outer envelope absorbing inner) pinned as `test_factory_permissive_smoke`. Empty-diff pin (`inner - outer`→`operation_failed`) added per Codex alongside disjoint-intersection — closes both canonical empty-result branches. Sibling audit `test_capability_router_phase2_surface_curve_coverage.py` with **tuple-derived exact-endpoint inventory** (NOT prefix-based — avoids overcapturing Phase 1 + pre-Phase-1 routes) covers all 7 PR-1..PR-4 intents × 4 discovery surfaces + symmetric inventory. First Codex review flagged a gap: operation-injection unpinned on both dispatch surfaces (MCP executor + agent-direct); fixup added 3 live MCP-tool pins (discriminating disjoint-circles geometry — union=2/difference=1/intersection=operation_failed) + 3 parametrized unit pins against `TRANSFORM_FUNCTIONS`. Second review: clean sign-off. **Phase 2 surface/curve campaign COMPLETE** — 4 PRs (#72–#75), 5 endpoints, 7 intent keys, zero ABI bumps, `creation` count 20→39, `curves` count 12→16. 238/238 green (23 PR-4 + 57 Phase 2 regression + 83 Phase 1 regression + 75 unit). | 2026-04-20 |
| #74 | `/surface/network` typed route (Phase 2 surface/curve PR-3). `NurbsSurface.CreateNetworkSurface` — fit NURBS through a curve network, auto-detect (`curveIds`) or explicit (`uCurveIds`+`vCurveIds`). Singular-contract managed-bridge (reuse); output wrapped via `Brep.CreateFromSurface` for `/surface/*` response parity. Zero ABI bump. **Empirical overload finding:** explicit overload takes 4 continuity slots (`uContStart/uContEnd/vContStart/vContEnd`), not 2 as plan-doc assumed — single `continuity` applied to all 4 (asymmetric deferred). **Error-code collapse:** factory's `out int error` empirically binary (0 success / 1 any failure) across 4+ pathological inputs; proposed `network_build_failed` route-specific code dropped in favor of `operation_failed` with raw code in message. Factory silently coalesces continuity values (≥1 and <0 identical; only `cont=0` observably distinct) + silently accepts zero/negative tolerances — worker-thread rejection load-bearing per `feedback_pr_scoping_rhythm`. **Continuity-distinctness pin runs on explicit `uCurveIds`/`vCurveIds` form** so test simultaneously exercises overload 2 reachability AND continuity honoring; `cont=0` vs `cont=1` on curved-edge network → area delta >0.01 asserted. Worker-thread rejects full XOR edge-branch set: both forms (`input_form_conflict`), either side-only (`input_form_incomplete`), empty explicit arrays (left/right/both — 3 pinned variants), neither form (`invalid_input`), `curveIds`<2 (`invalid_input`), out-of-range continuity (`invalid_continuity`), any tolerance≤0 (`invalid_input`). All 4 agent discovery surfaces wired. Bundled hygiene: `creation` count 38→39. 20 live NetworkSrf + 17 PR-2 + 11+9 PR-1 + 45 intent_runtime = **102/102 green** in 184.8s. 5 Codex review rounds caught: 4-slot continuity finding, 3 XOR edge branches, interior tolerance omission, explicit-form pin refinement, and late MCP tool description drift (single-curve listed as operation_failed when actually invalid_input via min-count) — fixed as commit `780104e` before merge. | 2026-04-20 |
| #73 | `/surface/patch` typed route (Phase 2 surface/curve PR-2). `Brep.CreatePatch` — fit a brep surface through curves / points / point clouds, optionally seeded by a starting surface. Singular-contract, managed-bridge (reuse). Zero ABI bumps. **Route dispatches between two RhinoCommon overloads based on `startingSurfaceId` presence** — empirical pre-implementation probe 2026-04-20 found overload 3 with null-seed produces ~49% larger surfaces than overload 2 on identical inputs, forcing the route-level branch (classic "one level subtler" scope risk Codex flagged pre-empt). Three new shared managed resolvers landed: `ResolveGeometryStrict` / `ResolveGeometriesStrict` (class-aware whitelist — Patch accepts Curve/Point/PointCloud; factory would accept meshes/breps silently but route pre-filters for contract clarity) + `ResolveSeedSurfaceStrict` (accepts Surface directly OR auto-extracts `UnderlyingSurface` from single-face Brep; multi-face Brep or wrong class → `invalid_seed_class`). **Worker-thread rejections load-bearing** — factory silently coerces `uSpans=0`, `flexibility=-1`, `tolerance=0` into garbage geometry without erroring, so the handler rejects them explicitly. Dependency rule: `flexibility` / `surfacePull` require `startingSurfaceId`; factory ignores them silently in the no-seed path, route rejects with `invalid_input` for user-model clarity. `surfacePull` unconstrained (type-check only) per empirical finding that factory accepts any real. **Real Rhino-side failure found empirically** — single-point-only input returns null, satisfies Common Plan amendment (`rook_docs/2026-04-17-typed-route-phase1-plan.md:257`) without needing `test_factory_permissive_smoke`. All 4 agent discovery surfaces wired (`rhino_create_patch` MCP tool, `BRIDGE_ROUTES`, `context.TOOL_CATEGORIES`, `explorer/registry.py` TOOL_CATEGORIES + HTTP_MAPPINGS). Bundled hygiene: `test_all_geometry_creation_ops` count 37→38. 17 live-Rhino Patch tests + 20 PR-1 regression + 45 intent_runtime = 82/82 green in 166s. **Empirical-probe-as-hard-gate + factory-silent-coercion-rejection refinements captured to `feedback_pr_scoping_rhythm.md` memory.** 4 Codex review rounds caught 4 pre-implementation issues: (1) empirical probe needed before final scope; (2) shared resolver scope under-specified; (3) permissiveness-smoke mandatory absent real failure; (4) test matrix needed mesh + surfacePull-no-seed + wrong-class-seed cases. | 2026-04-20 |
| #72 | `/surface/edge` + `/curve/blend` typed routes (Phase 2 surface/curve PR-1). Worked example on both `/surface/*` and `/curve/*` namespaces simultaneously on the Phase 1 managed-bridge (reuse) substrate via `CreateGeometry`. Zero ABI bumps. `/surface/edge`: `Brep.CreateEdgeSurface` from 2-4 boundary curves, singular-contract. `/curve/blend`: `Curve.CreateBlendCurve` overload 1 (curveA, curveB, continuity), singular-contract. **First mixed-substrate route in `CurvesHandler.cpp`** (12 existing routes are direct-sdk; this one is managed-bridge reuse). **First strict-attribute creator whose factory returns a Curve (not a Brep)** — serialization parity with brep creators under `_strictAttributes` pinned live via `test_blend_curves_live.py` (f) + (h). **Common Plan amendment landed at `rook_docs/2026-04-17-typed-route-phase1-plan.md:257`** — "Rhino-side failure when reachable; otherwise permissiveness-smoke" — triggered by empirical probing 2026-04-20 showing both factories extremely permissive (zero-length / coincident / disjoint / 3D non-planar all succeed). Applied retroactively to Phase 1 + Phase 2; no re-shipment required. All four agent discovery surfaces wired: MCP tools (`rhino_create_edge_srf`, `rhino_blend_curves`), `BRIDGE_ROUTES` in `tool_dispatcher.py`, `TOOL_CATEGORIES` in `context.py` + `explorer/registry.py`, `HTTP_MAPPINGS` in registry. Bundled hygiene: `test_all_geometry_creation_ops` count drift fix (20→37; 20 was stale from Phase 2 annotation campaign shipping without updating the constant) + `test_all_curve_ops` (12→13). 20 live-Rhino tests (11 EdgeSrf + 9 BlendCurves) passing in 63.8s; 45/45 `test_intent_runtime.py` regression green. **API-discovery finding during implementation:** my spike-memo + plan-doc initially specced a 6-arg `CreateBlendCurve(c1, rev1, cont1, c2, rev2, cont2)` signature that does NOT exist in RhinoCommon. Actual overloads: (1) 3-arg simple, (2) 5-arg with bulge, (3) 8-arg full-control with t-on-curve + reverse + asymmetric continuity. Corrected mid-scope-pass; PR-1 ships overload 1; overloads 2/3 deferred per Non-goals. 5 Codex review rounds caught 7 pre-merge issues. | 2026-04-20 |
| #71 | `/usertext/document-set` + `/usertext/document-get` + Phase 2 campaign close (PR-10). Document-level user-text routes extending the `UserTextHandler` substrate established in PR-9 (no re-declare, acceptance gate #1). New route-local error code **`reserved_namespace`** — first Phase 2 PR to introduce one; prior Phase 2 PRs used only the base set `{invalid_input, not_found, operation_failed}`. Reserved-prefix denylist table `kReservedPrefixes` in anon namespace (currently `RookBlock::` → redirects callers to `/block/user-strings`); validated on worker thread BEFORE `UndoScope`/UI dispatch (per Codex directive — pure JSON/key inspection). **Wholesale-atomic rejection on mixed maps:** `{allowed:"v", "RookBlock::X":"v"}` fails AND does not write `allowed` either; sneak-delete guard test pins atomicity by pre-seeding an allowed key and asserting it survives unchanged. Reads bypass the denylist so operators can inspect reserved keys (`test_reserved_prefix_readable_via_get` pins via `rhino_execute` seed — deterministic, isolated). **Empirical pre-flight worked as designed:** probed `RhinoDoc.Strings.SetString(k,"")` against running Rhino BEFORE writing scope; confirmed doc-level empty-string deletes same as attribute-level — PR-10 inherited PR-9's rejection posture with NO mid-PR contract corrections required (contrast PR-9 where the same finding landed mid-merge). 4-surface CapabilityRouter Phase 2 audit (`test_capability_router_phase2_coverage.py`) closes §"Campaign exit criterion": 12 intents × 4 surfaces + symmetric inventory reciprocity + `/select` additive-shape pin (both legacy `namePattern`/`bboxMin`/`bboxMax` AND additive `name`/`bbox` shapes asserted in MCP tool description). **Audit caught two real gaps pre-merge:** (a) annotation intents PR-1..PR-7 shipped without `CATEGORIES["creation"]` membership — backfilled 8 intents in-scope; (b) `rhino_select` MCP tool description only advertised "ids or layer" — PR #62 additive contract was never exposed in the tool surface. Both expanded in-scope. **Test-helper finding during implementation:** `RhinoCommon StringTable.SetString(k, None)` does NOT delete (returns current value without mutating) — the actual managed delete sentinel is empty string, identical to native. Purge helper fixed in lockstep. 209 total tests passing: 19 PR-10 live + 16 PR-9 regression + 88 annotation + 34 Phase 1 audit + 52 Phase 2 audit. Single Codex review round, no code findings. **Phase 2 typed-route campaign COMPLETE — coverage 62% → ~70% per plan §4 target.** | 2026-04-19 |
| #70 | `/usertext/object-set` + `/usertext/object-get` typed routes (Phase 2 PR-9). First usertext family PR — new `UserTextHandler.{h,cpp}` scaffold on direct-sdk native substrate (no ABI bump); PR-10 extends the same handler with doc-level routes. Full post-mutation echo response `{id, userStrings: {...}}` read back via `LookupObject`-after-`ModifyObjectAttributes` (mirrors `BlocksHandler.cpp:2565` rhythm), NOT request-body synthesis. Empty `userStrings: {}` is an idempotent no-op that short-circuits before `UndoScope` / `Redraw`. **Implementation-time contract correction:** empirical SDK finding 2026-04-19 showed `ON_3dmObjectAttributes::SetUserString(key, "")` is the attribute-level delete sentinel — removes the key rather than persisting empty value (attribute-level analog of doc-level `SetUserString(k, nullptr)` at `BlocksHandler.cpp:3090`). Scope-pass contract "empty-string writes empty string, NOT a delete sentinel" was incompatible with SDK reality; corrected mid-PR to reject empty-string values with structured `invalid_input` (key-specific message with delete rationale) to prevent sneak-delete before `/usertext/object-delete` lands. Correction landed in lockstep: handler, header, MCP tool schema (`additionalProperties.minLength: 1`), RouteSpec description, plan doc, tests. Dedicated `test_empty_string_value_does_not_sneak_delete_existing_key` pins rejection never reaches `ModifyObjectAttributes`. Four Codex review rounds resolved: (1) scope pass → full-echo response + CapabilityRouter-entries-in-PR-9 + minimal ObjectSnapshot posture; (2) agent-direct dispatch wiring (`BRIDGE_ROUTES`, `explorer/registry.py` `TOOL_CATEGORIES`+`HTTP_MAPPINGS`, `context.py` mapping); (3) MCP schema `minLength: 1` + header doc-drift; (4) final-unit pass + plan-doc sibling-repo PR-body clarification. 16 live-Rhino tests; 88/88 annotation-family regression green. **Deferred:** `/usertext/object-delete`, doc-level routes (PR-10), annotation-family `BRIDGE_ROUTES`/`explorer`/`context` backfill, `SerializeAttributeUserStrings` promotion to shared helper (fires on 3rd caller). | 2026-04-19 |
| #69 | `/annotation/dot` typed route (Phase 2 PR-7). **Last annotation-family route — closes the annotation side of Phase 2.** ON_TextDot is ON_Geometry (not ON_Annotation), so response has `type: "TextDot"` (per DocumentHelpers.h:127's `ON::text_dot` mapping) and **no `annotationType` field**. Construction: `ON_TextDot` populated + `CRhinoTextDot(attrs).SetDot(dot)` + `pDoc->AddObject` (no doc-level convenience exists). Effective-value echo for `text` / `secondaryText` / `heightInPoints` / `fontFace` read from live geometry; load-bearing `dynamic_cast` fails loud with `StructuredError("operation_failed")` per Codex review. Default `fontFace` pinned to exact `"Arial"` after targeted runtime probe (resolved SDK header ambiguity between `"Arial"` and `"Arial Bold"`). Verification is route-response-only — `GeometryHandler` has no ON_TextDot branch yet, so `/geometry` doesn't surface dot detail (deferred hygiene). `heightInPoints` enforces JSON integer only (rejects `24.0`) per `is_number_integer()` posture. 12 live-Rhino tests; 88/88 full-family regression green. Three Codex scope-pass + two PR-body findings all resolved. | 2026-04-19 |
| #68 | `/annotation/leader` typed route (Phase 2 PR-6). Text label + polyline pointing at an annotated location. First point = arrow tip; last point = text anchor. Single-call SDK path via `CRhinoDoc::AddLeaderObject(text, plane, ptCount, points, dimStyle, attrs)` — much simpler than dim-family routes. Minimum 2 points. **Empirical contract correction mid-merge:** scope pass relied on `rhinoSdkDoc.h:3190` docstring claim that "3d points (will be projected to the plane)" and wrote a flattening test pinning `bbox.z=[0,0]`. Live verification proved the claim wrong (input Z passes through, bbox.min.z came back as 5.0 for Z=5 input). Contract corrected in lockstep across handler header + MCP description + RouteSpec description + test: `World_xy` is the text/dim-style orientation plane; Z behavior of input points is SDK-governed, explicitly NOT part of Rook's contract. Flattening test replaced with `test_non_zero_z_input_accepted` (pins positive, deliberately doesn't assert specific Z). No `measuredValue` field (leaders have no dimensioned numeric quantity). plainText pinned by exact equality (tight-assertion rhythm from PR-5 lesson). 11 live-Rhino tests; 76/76 full-family regression green. | 2026-04-19 |
| #67 | `/annotation/dim-angle` typed route (Phase 2 PR-5). Angular dimension via 3-point contract (`center` + `start` + `end` + `point`). Empirical finding: 3-point `ON_DimAngular::Create` overload produces `ON::AnnotationType::Angular3pt` (wire: `"Angular3ptDimension"`), not the bare `Angular` (line-based). **Contract correction during review:** initial lax `90° OR 270°` assertion for the `point` semantic test masked real behavior — Rhino 8's `Measurement()` DOES honor `point` for span selection (principal vs reflex). Right-angle vertex returns 90° when `point` sits in the principal span, 270° in the reflex span. Handler doc + MCP description + test corrected in lockstep before merge. `measuredValue` sourced from `ON_DimAngular::Measurement()` accessor, converted radians→degrees. Colinear-ray fallback chains through `cross(rayA,rayB)` → `cross(rayA,Z)` → `cross(rayA,Y)` (no X attempt needed; explained inline). 13 live-Rhino tests; 65/65 full-family regression green. Three Codex scope-pass findings + two PR-body findings resolved. | 2026-04-19 |
| #66 | `/annotation/dim-radius` + `/annotation/dim-diameter` bundled (Phase 2 PR-4). First bundled route of Phase 2 — two handlers (`HandleDimRadius` + `HandleDimDiameter`) share `DispatchRadialDim` factory + `ExtractArcFromCurveId` helper; differ only by `ON::AnnotationType` enum and measuredValue math (radius vs 2·radius). SDK construction path: no `AddDimRadialObject` convenience exists, so manual `ON_DimRadial::Create(type, style_id, plane, center, radius_pt, dimline_pt)` + `CRhinoDimRadial(attrs); SetDimension(dim); pDoc->AddObject(rhinoDim)`. `ON_Curve::IsArc` unifies arc + closed-circle handling (single accessor; circle = `m_angle == 2π`). Introduced `StructuredError` type (mirrored from ArrayHandler.cpp:46-63) with dedicated catch branch — `curveId` referring to no object surfaces as structured `not_found`, not collapsed to `invalid_input`. **Promoted** `ON::AnnotationType → wire-string` mapping from static-in-GeometryHandler.cpp to `Rook::Serializer::AnnotationTypeToString` in `Serialization/RhinoSerializer.{h,cpp}`; 4 callers (GeometryHandler + HandleDimLinear + HandleDimAligned + HandleDimRadius/Diameter) now share one source. Closes the parked 3-copy hygiene item. Default `point = arc.MidPoint` pinned by explicit test. 15 new live-Rhino tests; 52/52 full-family regression green. Three Codex scope-pass findings resolved pre-implementation. | 2026-04-19 |
| #65 | `/annotation/dim-aligned` typed route (Phase 2 PR-3). ALIGNED dimension — direct Euclidean distance between start and end (`ON::AnnotationType::Aligned`, wire: `"AlignedDimension"`), dim plane tilts to match start→end. Uses `AddDimLinearObject` `ON_3dPoint` overload (`rhinoSdkDoc.h:3410`) — the same one legacy `/create?type=DIMENSION_LINEAR` uses, empirically confirmed by PR-2 to produce Aligned. `direction` parameter explicitly rejected with `invalid_input` + pointer to `/annotation/dim-linear` (preserves the LINEAR/ALIGNED route split). Third local copy of the `annotationType` wire-mapping accepted as conscious debt (Codex 2026-04-19); promotion trigger fires at PR-4. `measuredValue = (end - start).Length()` from geometry math, not PlainText. Cross-surface invariant holds. 13 live-Rhino tests; 38/38 family regression green. | 2026-04-19 |
| #64 | `/annotation/dim-linear` typed route (Phase 2 PR-2). LINEAR dimension — projected distance of `(end-start)` onto user `direction` (default world X), producing `ON::AnnotationType::Rotated` (wire: `"LinearDimension"`). Empirical finding: `CRhinoDoc::AddDimLinearObject` has two overloads; the `ON_3dPoint dim_line_point` overload (`rhinoSdkDoc.h:3410`) produces ALIGNED, the `ON_Line dimension_line` overload (`:3444`) produces Rotated. Legacy `/create?type=DIMENSION_LINEAR` uses the `ON_3dPoint` overload and actually ships ALIGNED despite the name; PR-2 uses the `ON_Line` overload for true LINEAR. Legacy untouched. `measuredValue` computed from geometry math (never PlainText-parsed — presentation is unsafe for numeric contract). Cross-surface invariant: POST response and `GET /geometry` agree on `annotationType` string via shared `AnnotationTypeToString` mapping (`GeometryHandler.cpp:19-36`). 13 live-Rhino tests; PR-1 regression suite (12) passes. Three Codex scope-pass cautions resolved pre-implementation. | 2026-04-19 |
| #63 | `/annotation/text` typed route (Phase 2 PR-1). Worked-example for the annotation family: new `AnnotationHandler.cpp` with substrate declaration + `ApplyCommonAttributesStrict` + local `invalidInput` lambda pattern. Direct-sdk native substrate (no bridge, no ABI bump). Preserves annotation-level override persistence via near-verbatim copy of the native TEXT factory (`SetTextHeight`/`SetAnnotationFont`/`SetAnnotationBold`/`SetAnnotationItalic`/`SetAnnotationFacename`). Strict attribute handling by default — malformed type on `name`/`layer`/`visible`/`font`/`bold`/`italic`/`color` → structured invalid_input. Font-characteristic failures fall back to the document default; response payload echoes the EFFECTIVE applied typography (not request echo) so fallback cases are observable without a follow-up GET /geometry. `point` requires exactly 3 numbers. Legacy `/create?type=TEXT` untouched. Two Codex review rounds: (1) layer-resolution determinism / read-surface selection / effective-value echo cautions addressed inline before PR open; (2) `name`/`layer` silent type-drop + overlong `point` truncation + default-font doc drift addressed as a second commit. 12 live-Rhino tests. | 2026-04-19 |
| #62 | `/select` additive compatibility (Phase 2 PR-0). Accepts router-advertised shapes (`name` exact case-insensitive, nested `bbox:{min,max}`) alongside legacy `namePattern` + flat `bboxMin`/`bboxMax`. Mixed `name` + `namePattern` → structured `invalid_input`; malformed nested `bbox` → `invalid_input` (parity with flat-path's throw-on-bad-input). Flat takes precedence over nested. Codex caught two pre-merge: exact-name was case-sensitive vs `namePattern`'s `towlower` → fixed via `CompareNoCase`; malformed nested `bbox` silently no-op'd → fixed with worker-thread shape validation. 13 live-Rhino tests. Structured-error emission scoped to new paths only (Decision Record §6 drift cleanup deferred). **Campaign gate — Phase 2 unblocked.** | 2026-04-19 |
| #61 | CapabilityRouter Phase 1 four-surface coverage audit — pins plan §Exit criterion 7 as a runtime-tested invariant. Per-intent assertions across 4 surfaces (Tool registration, executor case-arm endpoint match, RouteSpec presence + endpoint match, creation-category membership) plus 2 directional inventory checks (route table → audit list, server.py executor endpoints → route table + audit list). 34 tests, ~9s, no Rhino dependency. Codex review caught executor-endpoint paste-error blind spot + symmetric inventory gap; both addressed in `3c55390` before merge. | 2026-04-18 |
| #60 | `/array/polar` typed route — last Phase 1 PR (Phase 1 PR-6). Inherits PR-5 ArrayHandler substrate (no new helpers). Signed full-circle predicate `abs(abs(angle)-360.0) < 1e-9` with sign-preserving step formula (`+360/count` or `-360/count`); negative angles accepted as reverse-direction sweep; `-360` is reverse full circle, NOT partial sweep. `count==1` short-circuits step computation + axis validation but NOT source pre-flight (bogus uuids surface as `not_found` at count=1). `rotate=false` orbits each source's tight bounding-box center via `GetTightBoundingBox` + `BoundingBox().Center()` fallback. `axis` defaults to literal world `[0,0,1]` (no CPlane dependency, no `no_active_view` failure mode). New error codes `degenerate_axis`, `angle_zero_with_count`. 18 live tests pass. **Phase 1 typed-route migration COMPLETE.** | 2026-04-18 |
| #59 | `/array/linear` + `/array/rectangular` typed routes (Phase 1 PR-5); first `direct-sdk` Phase 1 routes — no managed bridge, no ABI bump. Mutation-class envelope `{createdCount, sourceIds, ids, mode, ...}`; source-major id ordering; `createdCount = len(sourceIds) * (count-1)` (linear) and `* (xCount*yCount*zCount-1)` (rectangular). Atomic rollback on mid-loop `TransformObject` null via explicit `DeleteObject` cleanup before throwing. Rectangular captures CPlane on UI thread at dispatch and echoes `planeUsed`; null active view returns `no_active_view`. Internal `POST /array/_debug/fail-next-copy` test seam gated on `ROOK_ENABLE_DEBUG_ROUTES=1` with `ScopedDebugArmReset` RAII guard against inter-test counter bleed. 23 live tests (12 linear + 11 rect). | 2026-04-18 |
| #58 | `/surface/revolve` typed route — singular-contract template variant (Phase 1 PR-4); separate `CreateRevolveStrict` method because legacy/typed schemas are genuinely different operations (legacy silent Z-axis default vs typed required axisStart/axisEnd). `curve_intersects_axis` deferred. 16 live tests. | 2026-04-17 |
| #57 | `/surface/sweep1` + `/surface/sweep2` typed routes (Phase 1 PR-3); two separate handlers per Rule 3 sharing managed plural plumbing. `roadlikeUp:[x,y,z]` not `roadlikeFrame` (RhinoCommon API correction). `rails_disconnected` deferred (heuristic too eager). Extracted `DispatchToManagedCreate`, `ResolveCurveStrict`, `InsertBrepsAsPluralResponse` helpers. 28 live tests. | 2026-04-17 |
| #56 | `/surface/loft` + plural-contract seam (Phase 1 PR-2); first plural route. `_strictAttributes`-gated early dispatch block in `CreateGeometry`; bundled Pipe `not_found` fix. `tolerance` omitted (API limitation). Post-loop empty-snapshot guard. 22 live tests. | 2026-04-17 |
| #55 | `/surface/pipe` typed route — worked-example template (Phase 1 PR-1); establishes `managed-bridge (reuse)` substrate with native error-envelope normalization. `_strictAttributes` convention codified in SurfaceHandler.cpp header. 16 live tests. | 2026-04-17 |
| #54 | Retire dead `rhino_loft`/`rhino_sweep` MCP tools (Phase 1 PR-0); full sweep across MCP surface + Python direct-dispatch + 7 runtime registries (Codex-flagged scope expansion from initial narrow attempt). | 2026-04-17 |
| #52 | `MutateDefinitionLocalFrame` + `RewriteDefinitionGeometry` helper extraction; HandleBlockRebase + recursive leaf + recursive parent migrations; orphaned `partialRebase` envelope dropped (closes #26) | 2026-04-16 |
| #51 | live-Rhino characterization for rebase invariants (12 tests; pins three-part invariant pre-#26 helper extraction) | 2026-04-16 |
| #49 | Python preamble reads raw wrappers via ghenv.VolatileData (closes #45) | 2026-04-16 |
| #48 | SetReference stamps ReferenceID on wrappers; aligns Set/Get semantics (partial #45) | 2026-04-16 |
| #46 | access-aware Python preamble coercion for list/tree pins (closes #40) | 2026-04-16 |
| #44 | persist script pin descriptions across VariableParameterMaintenance / SetSource recompile (closes #42) | 2026-04-16 |
| #43 | rich GH script pin editing (access/optional/description/nick/hidden) + `gh_set_script_pins` + wire preservation across rename + camelCase fix for `/gh/component` Python parsing | 2026-04-16 |
| #36 | live-Rhino coverage for link/refresh/merge basePoint UserData preservation (closes #35) | 2026-04-15 |
| #33 | basePoint metadata → Rook-owned `ON_UserData` channel with legacy fallback + 10 live-Rhino tests + env-var-gated debug routes (closes #28) | 2026-04-15 |
| #32 | live-Rhino regression coverage for replace-object-geometry (closes #31) | 2026-04-15 |
| #30 | normalize world-coord source in companion replace-object-geometry (closes #27) | 2026-04-15 |
| #25 | `rhino_block_create` non-origin basePoint fix (local-coord definition storage + `rook_block_base_point` metadata + rebase sync; closes #24) | 2026-04-15 |
| #23 | `rhino_block_replace_instance_batch` + block-tool compatibility hardening | 2026-04-14 |
| #22 | `rhino_block_transform_object_batch` (companion, ABI v12) | 2026-04-14 |
| #21 | `rhino_block_replace_object_geometry_batch` (companion, ABI v11) | 2026-04-14 |
| #20 | CapabilityRouter sweep for historical block tools (12 ops) | 2026-04-14 |
| #19 | `explorer/registry.py` block + layer mutation tool categorization (14 entries) | 2026-04-14 |
| #18 | CapabilityRouter coverage for batch tools + paired singles (15 ops) | 2026-04-14 |
| #17 | `explorer/registry.py` parity backfill (`rhino_split_disjoint_breps`) | 2026-04-14 |
| #16 | MCP discovery-surface backfill for prior batch tools | 2026-04-14 |
| #15 | `rhino_layer_set_properties_batch` (native, best-effort) | 2026-04-14 |
| #14 | `rhino_block_transform_instance_batch` (companion, ABI v10) | 2026-04-14 |
| #13 | Block-batch family (5 tools, ABI v8+v9) | 2026-04-14 |
| #12 | `rhino_split_disjoint_breps` (native) | 2026-04-14 |

---

## Update protocol

When you finish an item:
1. Move it to `Recently shipped` with PR number + merge date.
2. Promote `Next` → `Now`.
3. Pick a new `Next` from `Parked` per the ordering rule (or from new work
   that triaged in as `queue next`).
4. Sweep `Blocked` to see if the merge unblocks anything; promote if so.

When something new appears mid-work:
1. Triage immediately to `do now` / `queue next` / `park`.
2. If `park`: write the entry into the table here BEFORE continuing current
   work. The `trigger_to_revisit` field is mandatory.
3. If `queue next`: bump current `Next` to `Parked` with its own re-entry
   trigger; promote the new item.
